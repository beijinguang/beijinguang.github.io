<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>markee</name>
    <email>wangjinguang66@163.com</email>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <icon>https://www.gravatar.com/avatar/359848338f5d45edb6ced16050776b23</icon>
  <id>https://code.idea4j.com/</id>
  <link href="https://code.idea4j.com/" rel="alternate"/>
  <link href="https://code.idea4j.com/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, markee</rights>
  <subtitle>预测未来最好的方法是在现在创造未来</subtitle>
  <title>码客Mark</title>
  <updated>2026-09-25T00:01:00.000Z</updated>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="安全" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/"/>
    <category term="GitHub" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/GitHub/"/>
    <category term="DevOps" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/GitHub/DevOps/"/>
    <category term="供应链安全" scheme="https://code.idea4j.com/tags/%E4%BE%9B%E5%BA%94%E9%93%BE%E5%AE%89%E5%85%A8/"/>
    <category term="GitHub Enterprise Cloud" scheme="https://code.idea4j.com/tags/GitHub-Enterprise-Cloud/"/>
    <category term="SSO" scheme="https://code.idea4j.com/tags/SSO/"/>
    <category term="Proof of Presence" scheme="https://code.idea4j.com/tags/Proof-of-Presence/"/>
    <category term="Entra ID" scheme="https://code.idea4j.com/tags/Entra-ID/"/>
    <category term="MFA" scheme="https://code.idea4j.com/tags/MFA/"/>
    <content>
      <![CDATA[<p>企业账号已经登录，不代表眼前的操作者此刻仍是本人。被盗的会话 Cookie 或长期有效的认证凭据，可能让攻击者绕过一次性的登录检查，继续尝试创建令牌、修改 Webhook 或调整组织安全设置。GitHub 在 2026 年 9 月 24 日公布 Proof of Presence（PoP，存在性证明）预览：在部分高影响操作发生前，把成员送回企业身份提供方重新验证，确认这次操作有新鲜的身份凭据支撑。</p><p>PoP 不是每点一次按钮都重新登录，也不是新的通用 MFA 服务。它扩展了 GitHub 的 sudo mode，把“这次操作前是否刚刚验证过”提升为企业可配置的安全策略。对 DevOps 团队来说，关键问题是哪些账号和操作真正受支持、身份提供方会要求什么挑战，以及自动化流程是否依赖这些交互式操作。</p><span id="more"></span><h2 id="这次更新的范围先要看清"><a href="#这次更新的范围先要看清" class="headerlink" title="这次更新的范围先要看清"></a>这次更新的范围先要看清</h2><p>GitHub 公告将 PoP 描述为 GitHub Enterprise Cloud 的企业级设置，但当前公开预览有明确边界：适用于使用 Enterprise Managed Users（EMU）的企业账号，部署在 github.com 或 GitHub Enterprise Cloud with data residency（GHEC-DR），并使用 Microsoft Entra ID 作为 SSO 身份提供方；SAML 和 OIDC 都在公告列出的范围内。</p><p>因此，不能把它理解成所有 GitHub 账号、所有 Enterprise Cloud 组织都已经可以启用的通用开关。GitHub 文档说明该能力仍处于 public preview，身份提供方支持范围目前是 Microsoft Entra ID。准备试用前，应以企业自己的管理页面和最新文档确认资格，不要仅凭套餐名称判断是否适用。</p><p>公告举出的受保护操作包括：创建令牌、编辑 Webhook、修改组织安全设置、查看恢复代码。GitHub 文档进一步说明，PoP 与 sudo mode 使用同一套受保护操作和会话模型。它关注的是需要较高信任等级的管理动作，而不是把普通浏览、代码查看等所有操作都变成重新登录流程。</p><h2 id="PoP-如何确认“此刻有人在操作”"><a href="#PoP-如何确认“此刻有人在操作”" class="headerlink" title="PoP 如何确认“此刻有人在操作”"></a>PoP 如何确认“此刻有人在操作”</h2><p>可以把一次受保护操作简化成以下流程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">成员已登录 GitHub</span><br><span class="line">       ↓</span><br><span class="line">尝试执行受保护的高影响操作</span><br><span class="line">       ↓</span><br><span class="line">GitHub 将成员重定向到企业 IdP（预览期为 Entra ID）</span><br><span class="line">       ↓</span><br><span class="line">IdP 执行企业策略要求的重新认证或 MFA</span><br><span class="line">       ↓</span><br><span class="line">验证通过：返回 GitHub 并继续操作；未通过：操作不执行</span><br></pre></td></tr></table></figure><p>这条链路的重点不是“GitHub 再保存一份密码”，而是由企业已经配置的身份提供方判断本次认证是否满足策略。企业可以选择两种要求：</p><ul><li><strong>重新认证（Re-authentication）</strong>：成员再次向 IdP 证明身份。具体挑战取决于 Entra ID 策略；在某些策略下，重新输入密码就可能满足要求。</li><li><strong>多因素认证（MFA）</strong>：除重新认证外，还必须通过额外因子，例如验证器应用或生物识别。是否出现哪种挑战由企业 IdP 策略决定。</li></ul><p>公告还提到，企业可以通过 IdP 策略检查设备合规性等条件。也就是说，PoP 把“什么时候需要重新验证”的触发点放在 GitHub，把“用什么条件证明身份”的规则交给企业身份系统。它让 GitHub 的高风险动作能复用现有的身份治理，而不是再维护一套孤立的认证规则。</p><h2 id="它与-SSO、MFA-和-sudo-mode-的区别"><a href="#它与-SSO、MFA-和-sudo-mode-的区别" class="headerlink" title="它与 SSO、MFA 和 sudo mode 的区别"></a>它与 SSO、MFA 和 sudo mode 的区别</h2><table><thead><tr><th>机制</th><th>主要回答的问题</th><th>典型触发时机</th></tr></thead><tbody><tr><td>SSO 登录</td><td>用户是否能进入企业账号</td><td>建立或恢复登录会话时</td></tr><tr><td>MFA 策略</td><td>这次认证需要哪些身份因子</td><td>由 IdP 在认证流程中判定</td></tr><tr><td>GitHub sudo mode</td><td>近期是否进行过敏感操作所需的重新认证</td><td>用户尝试受保护操作时</td></tr><tr><td>Proof of Presence</td><td>企业能否要求敏感操作重新经过企业 IdP，并满足指定策略</td><td>成员发起高影响操作时</td></tr></tbody></table><p>PoP 可视为企业把 IdP 策略接入 sudo mode 的扩展。成员通过一次挑战后，后续高影响操作沿用同一浏览器会话的 sudo-mode 时间窗口；GitHub 公告给出的窗口是两小时。它不是每次操作都执行一次 MFA，因此也不是“持续认证”。管理员需要把两小时窗口纳入威胁模型，而不能把一次挑战等同于整天都安全。</p><p>还要区分“要求重新认证”和“要求 MFA”。如果企业只选择重新认证，而 IdP 策略允许密码单独满足要求，那么 PoP 提供的是身份新鲜度，不一定增加第二个认证因子。需要更强保证的团队应核对 Entra ID 的条件访问与认证强度策略，确保 GitHub 发起的挑战确实要求预期的 MFA 或设备状态。</p><h2 id="管理员如何启用，以及上线前要测什么"><a href="#管理员如何启用，以及上线前要测什么" class="headerlink" title="管理员如何启用，以及上线前要测什么"></a>管理员如何启用，以及上线前要测什么</h2><p>GitHub 文档给出的配置入口是企业账号中的 <strong>Settings → Authentication security → Proof of presence</strong>，管理员选择重新认证或 MFA。开启后，策略适用于整个企业，不是仅对某一个仓库或某个组织单独生效。</p><p>上线前可以按下面的顺序验证：</p><ol><li><strong>先确认资格与身份链路。</strong> 核对企业是否属于预览覆盖范围，Entra ID 的 SAML&#x2F;OIDC SSO 是否已正确配置，负责维护 IdP 策略的团队是否参与测试。</li><li><strong>选定挑战强度。</strong> 明确选择重新认证还是 MFA，并在 Entra ID 中确认密码、验证器、设备合规等条件的实际组合。不要把“菜单里选了 MFA”当成策略已经验证完成。</li><li><strong>用测试账号覆盖代表性动作。</strong> 分别检查令牌创建、Webhook 修改、组织安全设置和恢复代码等公告列出的场景，确认未通过挑战时操作确实被阻止，通过后能正常完成。</li><li><strong>验证两小时内外的会话表现。</strong> 检查一次成功验证后哪些动作可以继续进行，以及会话过期后的重新挑战体验；同时确认浏览器切换、IdP 超时和用户无法访问第二因子时的支持流程。</li><li><strong>盘点人工与自动化边界。</strong> 如果运维手册要求管理员临时创建令牌或修改 Webhook，应明确由谁完成交互式认证。不要让无人值守脚本依赖人工身份验证；自动化应使用适当的专用凭据与最小权限，并按现有凭据轮换和审计要求管理。</li></ol><p>由于设置会作用于整个企业，最好先在专门的测试企业或可控的预览环境验证，而不是直接在生产企业里试探配置。还应安排紧急访问和 IdP 故障的恢复流程，避免身份提供方短时不可用时，管理员无法完成必要的安全维护。</p><h2 id="它能降低什么风险，又不能替代什么"><a href="#它能降低什么风险，又不能替代什么" class="headerlink" title="它能降低什么风险，又不能替代什么"></a>它能降低什么风险，又不能替代什么</h2><p>PoP 针对的是一种常见的凭据风险：攻击者拿到仍有效的会话或长期凭据后，尝试执行需要更高权限的操作。把敏感动作前的认证重新交给企业 IdP，可以增加“仅有旧会话还不够”的一道门槛，也能复用企业关于 MFA 和设备合规的策略。</p><p>但它不是对账户接管的完整防护，也不会自动消除已经泄露的密钥或令牌。企业仍需限制令牌权限和有效期，审计令牌创建与 Webhook 变更，妥善管理恢复代码，并对异常操作设置告警。PoP 保护的是特定交互式动作的验证环节；它不能替代权限最小化、凭据轮换、审计和事件响应。</p><p>公告指出，GitHub 正在计划为 Pull Request 合并增加 PoP 支持，但截至公告发布时这项能力尚未提供。现阶段不应假设合并请求已经受到这项新策略保护；需要针对关键仓库继续使用现有分支保护、审批规则和部署环境门禁。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>GitHub Proof of Presence 的意义，在于把企业 IdP 的新鲜认证要求放到高影响操作的执行时刻。它让被盗会话面对一个额外障碍，也让企业能把 MFA 或设备策略复用到令牌、Webhook 和安全设置等关键动作上。</p><p>不过，PoP 的价值取决于边界是否理解准确：当前是范围有限的公开预览，启用后按企业范围生效，验证通过后仍有两小时会话窗口，而且 PR 合并支持尚在后续计划中。先核对覆盖对象，再在测试环境验证 IdP 挑战、运维流程和故障恢复，才是把这项能力安全落地的稳妥做法。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ol><li>GitHub Changelog： <a href="https://github.blog/changelog/2026-09-24-require-proof-of-presence-for-high-impact-actions/">Require proof of presence for high-impact actions</a></li><li>GitHub Docs： <a href="https://docs.github.com/en/enterprise-cloud@latest/admin/configuring-settings/hardening-security-for-your-enterprise/configuring-proof-of-presence">Configuring Proof of Presence</a></li><li>GitHub Docs： <a href="https://docs.github.com/en/enterprise-cloud@latest/authentication/keeping-your-account-and-data-secure/sudo-mode">Sudo mode</a></li></ol>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/25/2026-GitHub%E9%AB%98%E5%BD%B1%E5%93%8D%E6%93%8D%E4%BD%9C%E5%BC%95%E5%85%A5Proof-of-Presence/</id>
    <link href="https://code.idea4j.com/2026/09/25/2026-GitHub%E9%AB%98%E5%BD%B1%E5%93%8D%E6%93%8D%E4%BD%9C%E5%BC%95%E5%85%A5Proof-of-Presence/"/>
    <published>2026-09-25T00:01:00.000Z</published>
    <summary>GitHub Enterprise Cloud 开始预览 Proof of Presence，在创建令牌、修改 Webhook 等高影响操作前要求成员重新通过身份提供方验证。本文梳理预览范围、认证流程、两小时窗口和企业落地检查点。</summary>
    <title>GitHub 企业版为高影响操作引入 Proof of Presence：会话有效不等于本人在场</title>
    <updated>2026-09-25T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="人工智能" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="可观测性" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/%E5%8F%AF%E8%A7%82%E6%B5%8B%E6%80%A7/"/>
    <category term="GitHub Copilot" scheme="https://code.idea4j.com/tags/GitHub-Copilot/"/>
    <category term="AI Agent" scheme="https://code.idea4j.com/tags/AI-Agent/"/>
    <category term="DevOps" scheme="https://code.idea4j.com/tags/DevOps/"/>
    <category term="OpenTelemetry" scheme="https://code.idea4j.com/tags/OpenTelemetry/"/>
    <category term="OTel" scheme="https://code.idea4j.com/tags/OTel/"/>
    <category term="企业治理" scheme="https://code.idea4j.com/tags/%E4%BC%81%E4%B8%9A%E6%B2%BB%E7%90%86/"/>
    <content>
      <![CDATA[<p>一个会读代码、调用工具并修改工作区的 Agent，出了问题之后很难只靠一行“请求失败”来排查。我们需要知道它调用了哪个模型，走过哪些工具，在哪一步重试或偏离了预期，以及用户最后是否接受了修改。9 月 23 日，GitHub Changelog 宣布 GitHub Copilot 应用支持通过企业托管设置配置 OpenTelemetry（OTel），把这类 Agent 活动接入组织现有的可观测性系统。</p><p>这项变化的价值不在于又增加了一种日志格式，而在于 Agent 的执行过程开始拥有一条可以被采集、关联和分析的证据链。GitHub 文档把数据分成 traces、metrics 和 events 三类，并明确说明默认情况下不包含 Prompt、响应内容和工具参数。对平台团队来说，真正要设计的不是“是否打开遥测”，而是如何在看清 Agent 行为的同时，守住代码、提示词和凭据的边界。</p><span id="more"></span><h2 id="这次更新具体带来了什么"><a href="#这次更新具体带来了什么" class="headerlink" title="这次更新具体带来了什么"></a>这次更新具体带来了什么</h2><p>GitHub 的更新并没有把 Copilot 变成一个新的观测平台，而是提供了与 OTel 兼容的导出能力。企业可以指定接收数据的 OTLP 端点，再把数据送入现有的 Collector、日志平台或监控系统。</p><table><thead><tr><th>数据类型</th><th>能回答的问题</th><th>官方文档给出的例子</th></tr></thead><tbody><tr><td>Traces</td><td>一次 Agent 会话按什么顺序执行</td><td>模型调用、<code>readFile</code> 工具调用、再次调用模型</td></tr><tr><td>Metrics</td><td>一段时间内的使用模式是什么</td><td>模型调用消耗的输入和输出 token 数</td></tr><tr><td>Events</td><td>某个时间点发生了什么动作</td><td>用户接受或拒绝 Agent 编辑的反馈事件</td></tr></tbody></table><p>这三类数据放在一起，才接近 Agent 的真实运行过程。Trace 负责把一次会话中的步骤串起来，Metric 适合做趋势和成本分析，Event 则保留离散的用户反馈。它们分别对应“过程”“数量”和“动作”，不能用其中一种数据替代另外两种。</p><h2 id="Agent-为什么需要自己的可观测性"><a href="#Agent-为什么需要自己的可观测性" class="headerlink" title="Agent 为什么需要自己的可观测性"></a>Agent 为什么需要自己的可观测性</h2><p>传统 Web 服务通常可以用请求、响应、状态码和耗时描述一次调用。Agent 的一次任务则更像一个动态工作流：它可能先读取目录，调用模型制定计划，再读取几个文件，执行测试，遇到错误后重新规划，最后生成修改建议。不同任务走过的工具链可能完全不同。</p><p>可以把一次会话抽象成下面这条链路：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">用户请求</span><br><span class="line">   └─ Agent session trace</span><br><span class="line">        ├─ 模型调用：理解任务</span><br><span class="line">        ├─ 工具调用：readFile</span><br><span class="line">        ├─ 模型调用：决定下一步</span><br><span class="line">        ├─ 工具调用：运行测试或编辑文件</span><br><span class="line">        └─ 用户反馈：接受或拒绝编辑</span><br></pre></td></tr></table></figure><p>如果只记录最终回答，平台团队只能知道“结果看起来成功”或“结果失败”。有了调用链，才可以进一步区分几种常见情况：</p><ol><li>模型本身响应很快，但 Agent 在工具调用之间反复重试。</li><li>工具调用失败主要来自权限或网络，而不是模型推理质量。</li><li>token 消耗不断上升，却没有带来更高的编辑接受率。</li><li>某一类工具调用经常导致用户撤销或拒绝修改。</li></ol><p>这些观察结果不能自动证明某个模型或某个用户“效率更高”，但可以帮助团队定位流程瓶颈，把优化从猜测变成有证据的比较。</p><h2 id="配置入口：企业托管的-telemetry"><a href="#配置入口：企业托管的-telemetry" class="headerlink" title="配置入口：企业托管的 telemetry"></a>配置入口：企业托管的 <code>telemetry</code></h2><p>GitHub 文档说明，企业可以通过托管设置在支持的 Copilot 客户端中统一配置遥测。<code>telemetry</code> 属性包含启用开关、OTLP 端点、传输协议、内容捕获策略以及资源属性等字段。下面是一个用于说明字段关系的示例，端点和令牌均为占位值：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;telemetry&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;enabled&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;endpoint&quot;</span><span class="punctuation">:</span> <span class="string">&quot;https://otel-collector.example.com&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;protocol&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http/protobuf&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;captureContent&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;lockCaptureContent&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;serviceName&quot;</span><span class="punctuation">:</span> <span class="string">&quot;copilot&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;resourceAttributes&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;deployment.environment&quot;</span><span class="punctuation">:</span> <span class="string">&quot;production&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;headers&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;Authorization&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Bearer &lt;collector-token&gt;&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这里有几个容易被忽略的点：</p><ul><li><code>enabled</code> 控制是否导出遥测；<code>endpoint</code> 指向组织的 OTLP Collector 或兼容后端。</li><li><code>protocol</code> 支持 <code>http/json</code> 和 <code>http/protobuf</code>。具体选择应以 Collector 和网络设备的兼容性为准。</li><li><code>serviceName</code> 与 <code>resourceAttributes</code> 用来给数据增加服务和环境维度，方便在后端筛选。</li><li><code>headers</code> 可携带接收端所需的 HTTP 头，但真实令牌不应直接提交到仓库或写进公开文档。应使用组织已有的安全配置分发方式，并限制令牌权限和有效期。</li><li><code>lockCaptureContent</code> 可以锁定内容捕获设置，防止用户自行改变组织设定。</li></ul><p>企业托管设置的意义，是把每台开发机上的个人配置提升为平台策略。GitHub 文档明确表示，这类设置会在用户客户端中强制执行，用户不能覆盖。也因此，管理员在打开之前必须先确认端点的访问控制、数据留存和故障处理方式，而不能把它当作一个普通的本地调试开关。</p><h2 id="默认不采集内容，不等于没有隐私问题"><a href="#默认不采集内容，不等于没有隐私问题" class="headerlink" title="默认不采集内容，不等于没有隐私问题"></a>默认不采集内容，不等于没有隐私问题</h2><p>这次更新中最值得强调的安全边界，是 Copilot 默认不把 Prompt、响应和工具参数放进遥测数据。默认配置下，团队可以先观察会话路径、token 使用和编辑反馈，而不必立即把代码内容发送到观测后端。</p><p>GitHub 也提供了 <code>captureContent</code> 选项，但打开它可能把代码、文件内容和用户提示词带入遥测负载。对企业而言，这相当于扩大了数据处理范围，至少需要重新检查以下问题：</p><ol><li>Collector 是否启用了 TLS，接收端是否验证了身份。</li><li>观测平台的访问权限是否与源代码权限分离，普通排障人员是否真的需要看到内容。</li><li>数据保留多久，备份、导出和跨区域复制是否受同一套策略约束。</li><li>是否有脱敏、采样和删除机制，能否在误采集后快速止损。</li><li>是否应该只对测试组织或脱敏仓库开启内容捕获，而不是对全企业打开。</li></ol><p>通常更稳妥的路线是先保持 <code>captureContent: false</code>，使用结构化的 traces、metrics 和 events 回答“Agent 做了什么”和“流程哪里慢”。只有当一个经过审批的诊断场景确实需要内容上下文时，再在短时间、有限范围内启用，并提前定义关闭条件。</p><h2 id="如何分阶段落地"><a href="#如何分阶段落地" class="headerlink" title="如何分阶段落地"></a>如何分阶段落地</h2><h3 id="先从问题清单开始，而不是从仪表盘开始"><a href="#先从问题清单开始，而不是从仪表盘开始" class="headerlink" title="先从问题清单开始，而不是从仪表盘开始"></a>先从问题清单开始，而不是从仪表盘开始</h3><p>团队应该先写出想回答的问题。例如：工具调用失败率是否在某次客户端升级后上升？一次会话的 token 成本是否集中在某些仓库？用户拒绝编辑主要发生在什么类型的任务？问题明确后，再决定需要哪些字段和聚合维度，避免采集了大量数据却没有可执行结论。</p><h3 id="用-Collector-做边界层"><a href="#用-Collector-做边界层" class="headerlink" title="用 Collector 做边界层"></a>用 Collector 做边界层</h3><p>如果后端不能直接接收 OTLP，GitHub 文档建议部署 OpenTelemetry Collector。Collector 不只是一个转发器，还可以成为组织的边界层：统一做身份校验、路由、采样、属性补充和必要的过滤，把客户端与具体观测产品解耦。</p><p>客户端不应直接暴露给多个内部系统，也不应为了方便排障而把接收端口开放到不必要的网络范围。先让所有数据经过一个受控入口，后续更换后端或调整保留策略时，影响面会小很多。</p><h3 id="先试点，再形成基线"><a href="#先试点，再形成基线" class="headerlink" title="先试点，再形成基线"></a>先试点，再形成基线</h3><p>可以选择一个开发团队和少量非敏感仓库，连续观察一到两周，记录：</p><ul><li>会话数量、模型调用数量和 token 趋势；</li><li>工具调用失败、重试和超时情况；</li><li>Agent 编辑被接受、修改或拒绝的比例；</li><li>遥测发送失败时，客户端是否仍能正常工作；</li><li>不同客户端版本产生的数据是否兼容。</li></ul><p>这些指标适合用来改进工具和流程，不应该直接变成员工个人绩效排名。Agent 的使用量会受到任务类型、代码库规模和团队规范影响，脱离上下文比较很容易把复杂任务误判为低效率。</p><h3 id="把遥测健康度也纳入监控"><a href="#把遥测健康度也纳入监控" class="headerlink" title="把遥测健康度也纳入监控"></a>把遥测健康度也纳入监控</h3><p>接入 OTel 后，平台团队还要观察遥测本身：Collector 队列是否堆积，导出是否频繁重试，端点证书是否即将过期，数据量是否突然异常增加。可观测性系统如果自身不可用，不能反过来阻塞开发者的正常编码工作。建议把“遥测失败是否影响 Copilot 会话”作为上线前的故障演练项目。</p><h2 id="对开发者意味着什么"><a href="#对开发者意味着什么" class="headerlink" title="对开发者意味着什么"></a>对开发者意味着什么</h2><p>对于使用 Copilot 的开发者，这项变化首先应该被理解为组织级运行策略，而不是新的 Prompt 技巧。开发者需要知道：哪些活动会被记录，哪些内容默认不会被记录，数据由谁访问，以及出现误报或隐私疑虑时应该向谁反馈。</p><p>对于平台团队，Agent 的可观测性意味着现有的日志、指标和链路追踪体系需要增加一类新的工作负载。它既有模型调用的成本问题，也有工具执行的权限问题，还会涉及源代码和用户输入的敏感性。最重要的设计原则不是“采得越多越好”，而是让每一类采集都对应一个明确的运维或治理目的。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>GitHub Copilot 应用接入 OpenTelemetry，把 Agent 从一个只输出结果的黑盒，向可分析的执行系统推进了一步。Traces 让团队看到模型和工具如何串联，metrics 让成本与使用趋势有了统一入口，events 则补上了用户是否接受修改这类反馈信号。</p><p>但可观测性不会自动带来更好的代码，也不会替代人工审查。它真正提供的是一套更可靠的提问方式：问题发生在哪一步，证据是什么，修复之后是否真的改善。先从默认不采集内容开始，经过 Collector 和权限边界，再用小范围试点验证指标价值，Agent 才能在被看见的同时不越过数据治理的边界。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://github.blog/changelog/2026-09-22-opentelemetry-in-the-github-copilot-app">OpenTelemetry in the GitHub Copilot app - GitHub Changelog</a></li><li><a href="https://docs.github.com/en/enterprise-cloud@latest/copilot/concepts/enterprise/opentelemetry">OpenTelemetry for agent monitoring - GitHub Docs</a></li><li><a href="https://docs.github.com/en/enterprise-cloud@latest/copilot/reference/enterprise-administrators/enterprise-managed-settings">Enterprise managed settings - GitHub Docs</a></li><li><a href="https://opentelemetry.io/docs/what-is-opentelemetry/">What is OpenTelemetry? - OpenTelemetry</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/24/2026-GitHub-CopilotOpenTelemetryAgent%E5%8F%AF%E8%A7%82%E6%B5%8B%E6%80%A7/</id>
    <link href="https://code.idea4j.com/2026/09/24/2026-GitHub-CopilotOpenTelemetryAgent%E5%8F%AF%E8%A7%82%E6%B5%8B%E6%80%A7/"/>
    <published>2026-09-24T00:01:00.000Z</published>
    <summary>GitHub Copilot 应用开始支持通过企业托管设置配置 OpenTelemetry。本文拆解 Agent 的 traces、metrics 和 events，说明默认内容边界、配置项与分阶段落地方法。</summary>
    <title>GitHub Copilot 接入 OpenTelemetry：Agent 可观测性开始进入企业治理</title>
    <updated>2026-09-24T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="人工智能" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="GitHub Copilot" scheme="https://code.idea4j.com/tags/GitHub-Copilot/"/>
    <category term="Copilot CLI" scheme="https://code.idea4j.com/tags/Copilot-CLI/"/>
    <category term="C++" scheme="https://code.idea4j.com/tags/C/"/>
    <category term="代码智能" scheme="https://code.idea4j.com/tags/%E4%BB%A3%E7%A0%81%E6%99%BA%E8%83%BD/"/>
    <category term="Language Server" scheme="https://code.idea4j.com/tags/Language-Server/"/>
    <category term="索引" scheme="https://code.idea4j.com/tags/%E7%B4%A2%E5%BC%95/"/>
    <content>
      <![CDATA[<p>GitHub 宣布，GitHub Copilot CLI 的 C++ 代码智能开始支持 Whole Codebase Indexing（WCI，全代码库索引）。它会为整个 C++ 项目建立可复用的符号索引，包括当前没有打开的源文件和头文件，让定义跳转、引用查找、实现定位和符号搜索不必每次都重新理解项目结构。</p><p>这项更新的价值主要体现在大型 C++ 仓库：代码量大、头文件关系复杂、构建配置分支多时，代码智能最慢的部分往往不是生成回答，而是先弄清楚“这个类型来自哪里、这个宏在哪个配置下生效、这个符号和哪些文件相关”。WCI 用一次性的索引成本换取后续复用，但它也会带来首次启动时间、内存占用和索引一致性问题。对团队来说，真正要关注的是如何让索引拿到正确的编译信息，以及如何判断它是否已经完成并跟上代码变化。</p><span id="more"></span><h2 id="先明确范围：这是-Copilot-CLI-的-C-能力"><a href="#先明确范围：这是-Copilot-CLI-的-C-能力" class="headerlink" title="先明确范围：这是 Copilot CLI 的 C++ 能力"></a>先明确范围：这是 Copilot CLI 的 C++ 能力</h2><p>GitHub 的公告描述的是 GitHub Copilot CLI 中的 C++ 代码智能。它不是把 GitHub 网页端的所有代码导航实现都替换成 WCI，也不意味着其他语言会自动获得同样的索引机制。</p><p>GitHub 文档中的代码导航本来就支持 C++，能够在仓库中查找定义和引用；WCI 解决的是 Copilot CLI 使用 Microsoft C++ Language Server 时，大型 C++ 项目反复解析和查找关系的效率问题。两者都涉及“找到符号”，但运行位置、触发方式和服务对象不同，不能混为一谈。</p><p>对使用 Copilot CLI 的 C++ 开发者来说，最直接的体验变化是：打开一个项目后，代码智能会在后台建立索引；完成初次索引后，后续请求可以复用这份符号关系，而不是从当前打开的几个文件重新开始。</p><h2 id="WCI-做的事情：把一次次询问变成可复用的项目知识"><a href="#WCI-做的事情：把一次次询问变成可复用的项目知识" class="headerlink" title="WCI 做的事情：把一次次询问变成可复用的项目知识"></a>WCI 做的事情：把一次次询问变成可复用的项目知识</h2><h3 id="没有持久化索引时，导航请求会重复理解上下文"><a href="#没有持久化索引时，导航请求会重复理解上下文" class="headerlink" title="没有持久化索引时，导航请求会重复理解上下文"></a>没有持久化索引时，导航请求会重复理解上下文</h3><p>一个 C++ 仓库中的函数调用可能跨越多个头文件、宏分支、模板定义和编译选项。只看当前文件，工具很难准确回答以下问题：</p><ul><li>这个类型的真实定义在哪个头文件；</li><li>一个同名函数在当前编译配置下指向哪一个实现；</li><li>某个宏会改变哪些字段、模板或包含关系；</li><li>一个接口被哪些模块引用，引用来自源码还是生成文件。</li></ul><p>如果每一次代码智能请求都重新扫描相关项目关系，仓库越大，等待时间就越明显。尤其在拥有数百万行代码、头文件联系紧密的工程中，局部文件并不能代表完整上下文。</p><h3 id="WCI-在后台建立符号图"><a href="#WCI-在后台建立符号图" class="headerlink" title="WCI 在后台建立符号图"></a>WCI 在后台建立符号图</h3><p>WCI 会为整个 C++ 项目创建持久化的符号索引。Microsoft C++ Language Server 会利用项目的编译信息解析类型、符号、包含关系以及文件之间的关联，再把结果保存下来供后续请求复用。</p><p>可以把它理解成三层数据：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">项目编译信息</span><br><span class="line">    ↓</span><br><span class="line">类型、符号、宏和 include 关系的解析</span><br><span class="line">    ↓</span><br><span class="line">可持久化、可增量更新的代码智能索引</span><br><span class="line">    ↓</span><br><span class="line">定义 / 引用 / 实现 / 符号搜索</span><br></pre></td></tr></table></figure><p>这里的关键不是简单地把所有文本做一次全文搜索，而是让语言服务器拥有更完整的语义关系。这样 Copilot CLI 在处理一个符号时，能更快定位到项目中与它相关的文件，即使这些文件当前没有被用户打开。</p><h2 id="“默认开启”不等于“第一次没有成本”"><a href="#“默认开启”不等于“第一次没有成本”" class="headerlink" title="“默认开启”不等于“第一次没有成本”"></a>“默认开启”不等于“第一次没有成本”</h2><p>GitHub 公告给出的默认行为很明确：WCI 默认启用，第一次打开 C++ 项目时，语言服务器会加载并建立索引。首次索引可能增加等待时间，并暂时提高内存使用，仓库越大、构建关系越复杂，这种成本越明显。</p><p>初次索引完成后，索引会被复用并动态更新，因此额外开销主要集中在首次建立和代码结构发生较大变化的阶段。这种模型和数据库的初始建表、增量维护很像：第一次需要扫描全量数据，之后主要处理变化。</p><p>团队在评估这项能力时，不应只看“打开项目后多久能得到第一个回答”。更完整的指标应该包括：</p><table><thead><tr><th>阶段</th><th>建议观察的指标</th><th>可能的问题</th></tr></thead><tbody><tr><td>初次打开</td><td>首次索引耗时、峰值内存、CPU 使用</td><td>远程开发机内存不足，索引一直未完成</td></tr><tr><td>索引完成后</td><td>定义跳转、引用查找、符号搜索延迟</td><td>索引存在但项目配置不正确</td></tr><tr><td>修改代码后</td><td>增量更新时间、结果是否包含新符号</td><td>索引更新滞后或分支切换后失配</td></tr><tr><td>大规模切换分支</td><td>重新索引时间和资源峰值</td><td>生成文件、编译选项或头文件集合变化过大</td></tr></tbody></table><h2 id="正确的编译信息比“索引开关”更重要"><a href="#正确的编译信息比“索引开关”更重要" class="headerlink" title="正确的编译信息比“索引开关”更重要"></a>正确的编译信息比“索引开关”更重要</h2><p>C++ 的代码关系高度依赖编译配置。同一个头文件在不同宏定义、包含路径、编译标准和目标架构下，可能表现出不同的类型和符号关系。WCI 能否给出可靠结果，首先取决于语言服务器拿到的项目编译信息是否接近真实构建。</p><p>落地时可以从以下几个方面检查：</p><h3 id="1-让开发环境和真实构建使用同一套配置"><a href="#1-让开发环境和真实构建使用同一套配置" class="headerlink" title="1. 让开发环境和真实构建使用同一套配置"></a>1. 让开发环境和真实构建使用同一套配置</h3><p>如果本地代码智能使用的编译标准、include 路径或宏定义与 CI、开发机实际构建不同，索引会非常快地得到一个“看起来合理但不适用”的项目模型。</p><p>对于使用 CMake、Bazel 或其他构建系统的项目，应该确认语言服务器接入的是项目实际生成的编译信息，而不是手工维护的一份过期配置。修改构建选项后，观察索引是否重新处理了受影响的文件，并用几个已知符号测试跳转结果。</p><h3 id="2-把生成代码和第三方目录纳入明确策略"><a href="#2-把生成代码和第三方目录纳入明确策略" class="headerlink" title="2. 把生成代码和第三方目录纳入明确策略"></a>2. 把生成代码和第三方目录纳入明确策略</h3><p>大型仓库经常包含生成的头文件、供应商代码、多个平台目录和历史构建产物。它们既可能是正确索引所必需的，也可能让首次索引变得非常昂贵。</p><p>不要直接删除所有生成目录来“加快索引”。更稳妥的做法是先区分：哪些目录决定真实类型关系，哪些只是编译输出或不参与当前目标，再按照 Microsoft C++ Language Server 的索引配置文档设置范围。修改范围后要同时验证索引速度和跳转准确性。</p><h3 id="3-把分支切换视为索引输入变化"><a href="#3-把分支切换视为索引输入变化" class="headerlink" title="3. 把分支切换视为索引输入变化"></a>3. 把分支切换视为索引输入变化</h3><p>切换分支可能改变宏定义、依赖版本、生成头文件和编译数据库。此时旧索引即使还存在，也不一定代表当前分支。</p><p>排查“为什么 Copilot CLI 跳到了不存在的实现”时，应该先确认当前分支、构建配置和索引状态，再怀疑模型回答。对于频繁切换分支的团队，最好把“切换后重新生成编译信息并等待索引完成”写进开发习惯，而不是在结果错误时临时清理所有目录。</p><h2 id="如何观察索引是否真的完成"><a href="#如何观察索引是否真的完成" class="headerlink" title="如何观察索引是否真的完成"></a>如何观察索引是否真的完成</h2><p>GitHub 公告说明，可以使用 Copilot CLI 的 <code>/lsp logs</code> 查看索引进度。遇到首次启动很慢、定义跳转为空或引用结果明显不全时，可以按下面的顺序排查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">1. 打开 Copilot CLI 并进入目标 C++ 项目</span><br><span class="line">2. 执行 /lsp logs</span><br><span class="line">3. 确认语言服务器是否正在建立或更新索引</span><br><span class="line">4. 检查当前项目的编译信息是否可用</span><br><span class="line">5. 用一个已知类型分别测试定义、引用和实现跳转</span><br></pre></td></tr></table></figure><p>测试时不要只选最简单的单文件函数。应覆盖跨头文件类型、模板、条件编译和生成代码中的符号，才能判断索引是否真正理解了项目关系。</p><p>如果日志显示索引已经完成，但结果仍然异常，可以再检查：当前工作目录是否是预期项目根目录、分支切换是否刚刚发生、编译信息是否引用了不存在的路径，以及仓库是否超出了工具的实际可处理范围。</p><h2 id="什么时候应该暂时关闭-WCI"><a href="#什么时候应该暂时关闭-WCI" class="headerlink" title="什么时候应该暂时关闭 WCI"></a>什么时候应该暂时关闭 WCI</h2><p>WCI 的默认开启适合大多数 C++ 项目，但不是所有环境都应该无条件接受首次索引开销。下面几种情况可以考虑临时关闭或缩小索引范围：</p><ul><li>远程开发容器的内存预算很小，索引会影响编译和测试；</li><li>仓库包含大量生成代码或第三方代码，当前任务只需要一个很小的子项目；</li><li>项目编译信息还没有稳定，索引结果经常与真实构建不一致；</li><li>团队正在验证不同架构和分支，频繁全量重建的成本高于收益。</li></ul><p>关闭 WCI 的目的不是放弃代码智能，而是避免把资源消耗和错误结果混在一起。GitHub 公告提供了索引文档和临时禁用方式，团队应在确认工具版本和配置入口后再调整，而不是直接删除语言服务器的缓存目录。</p><h2 id="对大型-C-团队的实践建议"><a href="#对大型-C-团队的实践建议" class="headerlink" title="对大型 C++ 团队的实践建议"></a>对大型 C++ 团队的实践建议</h2><h3 id="把索引纳入开发环境基线"><a href="#把索引纳入开发环境基线" class="headerlink" title="把索引纳入开发环境基线"></a>把索引纳入开发环境基线</h3><p>项目文档除了说明如何编译，还可以说明如何让 Copilot CLI 获得正确的编译信息、如何查看 <code>/lsp logs</code>，以及在分支切换后何时需要等待更新。这样新人遇到“跳转不准”时，不会把所有问题归咎于 AI。</p><h3 id="把首次索引当作可观测的初始化阶段"><a href="#把首次索引当作可观测的初始化阶段" class="headerlink" title="把首次索引当作可观测的初始化阶段"></a>把首次索引当作可观测的初始化阶段</h3><p>首次索引慢并不一定是故障，但长期没有完成、内存持续增长或索引完成后结果为空，就需要进入排查流程。团队可以在远程开发镜像中预留合理内存，并在问题报告中记录仓库规模、构建系统、架构和索引日志摘要。</p><h3 id="用真实重构任务验证收益"><a href="#用真实重构任务验证收益" class="headerlink" title="用真实重构任务验证收益"></a>用真实重构任务验证收益</h3><p>不要只测“能不能跳到定义”。更有价值的验证任务包括：跨模块查找一个接口的全部实现、定位模板实例化来源、修改公共头文件后追踪受影响调用方，以及在不同构建配置下比较符号结果。</p><p>这些任务更接近实际开发，也能暴露编译信息缺失、条件编译不一致和增量更新滞后等问题。只有当索引带来的等待减少没有换来更多误跳转，团队才算真正获得了收益。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Whole Codebase Indexing 把 C++ 代码智能从“每次询问时临时理解项目”推进到“先建立可复用的项目索引，再持续更新”。对于大型 C++ 仓库，这会明显改善定义、引用、实现和符号搜索的等待体验；代价则是第一次索引的时间、内存以及对正确编译信息的依赖。</p><p>这项能力最值得采用的方式，不是打开之后就期待所有跳转立刻变快，而是把它当作开发环境的一项基础设施：让构建配置可信，观察索引日志，验证分支切换后的结果，并在资源不够或项目结构不适合时知道如何关闭。索引越接近真实项目，Copilot CLI 才越像是在理解整个代码库，而不是在当前文件附近猜测答案。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ol><li>GitHub Changelog： <a href="https://github.blog/changelog/2026-09-22-faster-c-code-intelligence-with-whole-codebase-indexing">Faster C++ code intelligence with whole codebase indexing</a></li><li>Microsoft C++ Language Server： <a href="https://github.com/microsoft/cpp-language-server/blob/main/docs/indexing.md">Indexing documentation</a></li><li>GitHub Docs： <a href="https://docs.github.com/en/repositories/working-with-files/using-files/navigating-code-on-github">Navigating code on GitHub</a></li></ol>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/23/2026-GitHubCopilotCLI-C++%E5%85%A8%E4%BB%A3%E7%A0%81%E5%BA%93%E7%B4%A2%E5%BC%95/</id>
    <link href="https://code.idea4j.com/2026/09/23/2026-GitHubCopilotCLI-C++%E5%85%A8%E4%BB%A3%E7%A0%81%E5%BA%93%E7%B4%A2%E5%BC%95/"/>
    <published>2026-09-23T00:01:00.000Z</published>
    <summary>GitHub Copilot CLI 为 C++ 代码智能加入 Whole Codebase Indexing，通过持久化符号索引减少大型仓库中重复解析项目关系的开销。本文解释它的工作方式、首次索引成本和团队落地时的排查重点。</summary>
    <title>GitHub Copilot CLI 加速 C++ 代码智能：Whole Codebase Indexing 让大型仓库先建索引再理解</title>
    <updated>2026-09-23T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="安全" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/"/>
    <category term="Rust" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/Rust/"/>
    <category term="DevOps" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/Rust/DevOps/"/>
    <category term="Rust" scheme="https://code.idea4j.com/tags/Rust/"/>
    <category term="供应链安全" scheme="https://code.idea4j.com/tags/%E4%BE%9B%E5%BA%94%E9%93%BE%E5%AE%89%E5%85%A8/"/>
    <category term="CI/CD" scheme="https://code.idea4j.com/tags/CI-CD/"/>
    <category term="GitHub Actions" scheme="https://code.idea4j.com/tags/GitHub-Actions/"/>
    <category term="Miri" scheme="https://code.idea4j.com/tags/Miri/"/>
    <category term="Secrets" scheme="https://code.idea4j.com/tags/Secrets/"/>
    <category term="缓存安全" scheme="https://code.idea4j.com/tags/%E7%BC%93%E5%AD%98%E5%AE%89%E5%85%A8/"/>
    <content>
      <![CDATA[<p>Rust 安全响应团队在 2026 年 9 月 21 日发布公告：Miri 会把环境变量写入 <code>target/</code>，如果 GitHub Actions 又缓存了这个目录，且运行 <code>cargo miri</code> 的作业可以读取 Secrets，那么这些敏感信息可能随着缓存留存，并被 Pull Request 流程读取。</p><p>这不是一个需要所有 Rust 项目立刻恐慌的漏洞公告。Rust 团队明确描述的是一个组合风险：Miri 的环境变量持久化行为，碰上了 CI 缓存的跨运行复用和 Pull Request 可读范围，才形成了泄露路径。真正需要马上做的是检查自己的工作流是否同时满足这些条件，清理已经生成的缓存，并在必要时轮换可能暴露的凭据。</p><span id="more"></span><h2 id="风险是怎样形成的"><a href="#风险是怎样形成的" class="headerlink" title="风险是怎样形成的"></a>风险是怎样形成的</h2><h3 id="Miri-为什么会接触环境变量"><a href="#Miri-为什么会接触环境变量" class="headerlink" title="Miri 为什么会接触环境变量"></a>Miri 为什么会接触环境变量</h3><p>Miri 是 Rust 的解释器和未定义行为检测工具，通常通过 <code>cargo miri</code> 运行测试。为了让后续运行能够保留构建所需的信息，当前实现会把环境变量写入 <code>target/</code> 中的构建产物或辅助文件。</p><p>单独看，这种行为并不等于秘密泄露：如果工作目录只在一次 CI 任务中使用，任务结束后被销毁，环境变量不会自动出现在外部。但 CI 经常会缓存 <code>target/</code> 来减少 Rust 项目的编译时间，一旦这些文件跨运行保存，环境变量就获得了更长的生命周期。</p><h3 id="GitHub-Actions-缓存把问题变成了跨运行问题"><a href="#GitHub-Actions-缓存把问题变成了跨运行问题" class="headerlink" title="GitHub Actions 缓存把问题变成了跨运行问题"></a>GitHub Actions 缓存把问题变成了跨运行问题</h3><p>典型的 Rust CI 会缓存 <code>target/</code>，或者缓存 <code>cargo install</code> 生成的工具。默认分支的工作流可以写入缓存，Pull Request 工作流往往可以读取已有缓存。这样做是为了加速构建，同时避免来自 Pull Request 的代码直接污染可信缓存。</p><p>问题在于，“不能写入”不代表“不能读取”。如果一个拥有 Secrets 的作业运行 Miri 并把 <code>target/</code> 保存进缓存，之后能够访问这个缓存的 Pull Request 就可能拿到已经落盘的环境变量内容。</p><p>风险路径可以概括为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">CI Secret 注入环境变量</span><br><span class="line">        ↓</span><br><span class="line">cargo miri 运行并把环境变量写入 target/</span><br><span class="line">        ↓</span><br><span class="line">Actions 缓存保存 target/</span><br><span class="line">        ↓</span><br><span class="line">Pull Request 任务读取缓存</span><br><span class="line">        ↓</span><br><span class="line">敏感信息可能出现在可被 PR 代码访问的文件中</span><br></pre></td></tr></table></figure><p>这条路径不依赖日志打印。即使日志里没有出现 Secret，只要它被写进了可复用的构建目录，缓存本身就可能成为泄露载体。</p><h2 id="哪些项目需要优先检查"><a href="#哪些项目需要优先检查" class="headerlink" title="哪些项目需要优先检查"></a>哪些项目需要优先检查</h2><p>Rust 官方建议检查自己的 GitHub Actions 配置。一个项目同时满足下面四项时，应当按受影响项目处理：</p><ol><li>工作流执行了 <code>cargo miri</code> 或其他会启动 Miri 的命令；</li><li>执行 Miri 的 Job 或 Step 通过 <code>env</code> 读取了 Secrets；</li><li>工作流使用 <code>actions/cache</code>、<code>swatinem/rust-cache</code> 或类似机制缓存了 <code>target/</code>；</li><li>这个缓存可以被 Pull Request 任务读取。</li></ol><p>可以先在仓库中做一次静态盘点：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">rg -n -i \</span><br><span class="line">  <span class="string">&quot;cargo miri|miri|target/|actions/cache|rust-cache|secrets\.&quot;</span> \</span><br><span class="line">  .github/workflows .github/actions 2&gt;/dev/null</span><br></pre></td></tr></table></figure><p>搜索结果需要人工结合上下文判断。比如，工作流可能只缓存 <code>~/.cargo/registry</code>，并没有缓存 <code>target/</code>；也可能在一个没有 Secrets 的 Job 里运行 Miri。反过来，Secret 不一定写成 <code>$&#123;&#123; secrets.NAME &#125;&#125;</code>，也可能通过 Job 级 <code>env</code>、环境文件或前置脚本间接进入进程环境。</p><h2 id="修复顺序：先阻断，再清理，最后轮换"><a href="#修复顺序：先阻断，再清理，最后轮换" class="headerlink" title="修复顺序：先阻断，再清理，最后轮换"></a>修复顺序：先阻断，再清理，最后轮换</h2><h3 id="1-先让-Miri-任务不再同时接触缓存和-Secrets"><a href="#1-先让-Miri-任务不再同时接触缓存和-Secrets" class="headerlink" title="1. 先让 Miri 任务不再同时接触缓存和 Secrets"></a>1. 先让 Miri 任务不再同时接触缓存和 Secrets</h3><p>最简单的短期做法，是让运行 Miri 的 Job 不使用缓存。下面是一个刻意保持保守的结构：Miri 任务不配置任何 Secret，也不保存 <code>target/</code> 缓存。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">jobs:</span></span><br><span class="line">  <span class="attr">miri:</span></span><br><span class="line">    <span class="attr">runs-on:</span> <span class="string">ubuntu-latest</span></span><br><span class="line">    <span class="attr">steps:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">uses:</span> <span class="string">actions/checkout@v5</span></span><br><span class="line"></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Run</span> <span class="string">Miri</span> <span class="string">without</span> <span class="string">secrets</span> <span class="string">or</span> <span class="string">target</span> <span class="string">cache</span></span><br><span class="line">        <span class="attr">run:</span> <span class="string">cargo</span> <span class="string">miri</span> <span class="string">test</span></span><br></pre></td></tr></table></figure><p>如果业务确实需要 Secret，例如从私有注册表下载依赖，应尽量把 Secret 限定在不运行 Miri 的那个 Step，而不是放在整个 Job 的 <code>env</code> 中。这样即使 Miri 或构建脚本把环境信息持久化，也能减少被写入的敏感范围。</p><h3 id="2-清理已经生成的缓存"><a href="#2-清理已经生成的缓存" class="headerlink" title="2. 清理已经生成的缓存"></a>2. 清理已经生成的缓存</h3><p>改完工作流后，旧缓存不会自动消失。应在仓库的 Actions 缓存管理页面检查与 <code>target/</code> 相关的缓存键，删除可能由受影响工作流创建的条目。GitHub 文档也提供了按分支筛选和删除缓存的方法，并支持通过 REST API 或 GitHub CLI 管理缓存。</p><p>清理缓存时不要只删除默认分支最新的一条。应根据缓存键、创建时间、分支和使用过该缓存的工作流一起检查，避免留下另一个仍然可以被 Pull Request 恢复的副本。</p><h3 id="3-评估并轮换可能暴露的凭据"><a href="#3-评估并轮换可能暴露的凭据" class="headerlink" title="3. 评估并轮换可能暴露的凭据"></a>3. 评估并轮换可能暴露的凭据</h3><p>如果受影响的工作流曾经执行过，不能因为没有看到异常日志就断定 Secret 没有泄露。应按照凭据类型评估风险：撤销短期令牌、重新生成长期令牌、轮换部署密钥，并检查相关服务的访问日志。</p><p>轮换顺序应优先覆盖能读取仓库、发布包、访问云资源或修改生产环境的凭据。轮换完成后，还要确认新凭据没有继续以 Job 级环境变量的形式暴露给会写入缓存的步骤。</p><h2 id="Rust-官方修复意味着什么"><a href="#Rust-官方修复意味着什么" class="headerlink" title="Rust 官方修复意味着什么"></a>Rust 官方修复意味着什么</h2><p>Rust 团队给出的短期修复，是让 Miri 只保留 <code>CARGO_*</code> 环境变量（排除 <code>CARGO_*_TOKEN</code>）和 <code>OUT_DIR</code>，而不是把整个环境写入 <code>target/</code>。公告同时说明，长期来看 Miri 和 Cargo 还需要寻找更可靠的方式传递真正与构建有关的变量；在补丁进入所使用的 nightly 之前，不能只依赖“升级 Rust”这句笼统建议。</p><p>公告写明，包含修复的 Miri 版本计划进入 2026 年 9 月 22 日的 nightly。升级前后仍建议在隔离分支中验证：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">rustup toolchain install nightly-2026-09-22</span><br><span class="line">rustup component add miri --toolchain nightly-2026-09-22</span><br><span class="line">cargo +nightly-2026-09-22 miri <span class="built_in">test</span></span><br></pre></td></tr></table></figure><p>日期工具链只是示例，实际可用性应以 Rust 官方发布的组件清单为准。即使新版本不再保存完整环境变量，也不应该把 Secret 无限制地暴露给所有构建步骤，因为项目自己的 <code>build.rs</code>、第三方构建工具或其他编译脚本仍可能把环境内容写进文件。</p><h2 id="即使不用-Miri，也应该重新审视缓存边界"><a href="#即使不用-Miri，也应该重新审视缓存边界" class="headerlink" title="即使不用 Miri，也应该重新审视缓存边界"></a>即使不用 Miri，也应该重新审视缓存边界</h2><p>这次事件的影响面不应被缩小成“Miri 用户专属问题”。Rust 官方特别提醒：能够写入公共缓存的 Job 不应同时拥有 Secrets。许多工具都会读取环境变量，甚至把配置、诊断信息或生成文件放进构建目录，但不会专门识别哪些值是秘密。</p><p>因此，CI 设计上最好遵循几个简单的边界：</p><ul><li>只给真正需要的 Step 注入 Secret，不使用整个 Job 级 <code>env</code>；</li><li>不缓存包含凭据、配置文件或未审查生成物的目录；</li><li>将可信分支的缓存和 Pull Request 缓存使用不同的键或不同的策略；</li><li>对缓存路径做内容检查，不只检查缓存命中率；</li><li>在修改缓存目录或构建工具后，主动清理旧缓存；</li><li>把缓存当作持久化数据处理，而不是临时磁盘。</li></ul><p>缓存的安全性取决于“谁能写、谁能读、里面可能有什么”，而不只是缓存键是否足够复杂。只要一个不可信输入能够读取由可信任务写入的目录，就应当假设该目录中的内容可能被带入后续逻辑。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Miri 与 GitHub Actions 的这次组合风险很有代表性：单独看，环境变量持久化和 CI 缓存都是为了让工具正常、让构建更快；放在一起，却可能把一次任务里的 Secret 变成跨运行可读取的数据。</p><p>对 Rust 项目来说，今天最值得做的不是盲目禁用所有缓存，而是准确盘点 <code>cargo miri</code>、<code>target/</code>、Secrets 和 Pull Request 访问范围之间的关系。先隔离敏感任务，再清理旧缓存，最后轮换可能泄露的凭据；即使项目不使用 Miri，也应把这次公告当作一次检查 CI 缓存边界的提醒。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ol><li>Rust Blog： <a href="https://blog.rust-lang.org/2026/09/21/github-actions-leaking-secrets-when-miri-output-is-cached/">GitHub Actions leaking secrets when Miri output is cached</a></li><li>GitHub Docs： <a href="https://docs.github.com/en/actions/how-tos/manage-workflow-runs/manage-caches">Managing caches</a></li><li>GitHub Docs： <a href="https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows">Dependency caching reference</a></li></ol>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/22/2026-RustMiriGitHubActions%E7%BC%93%E5%AD%98%E6%B3%84%E9%9C%B2Secrets/</id>
    <link href="https://code.idea4j.com/2026/09/22/2026-RustMiriGitHubActions%E7%BC%93%E5%AD%98%E6%B3%84%E9%9C%B2Secrets/"/>
    <published>2026-09-22T00:01:00.000Z</published>
    <summary>Rust 安全响应团队披露，Miri 会将环境变量写入 target 目录；如果 CI 同时缓存该目录且作业拥有 Secrets，Pull Request 可能通过缓存读到敏感信息。本文拆解影响条件、排查方法和修复顺序。</summary>
    <title>Rust 安全团队警告：Miri 与 GitHub Actions 缓存组合可能泄露 Secrets</title>
    <updated>2026-09-22T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="人工智能" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="GitHub Copilot" scheme="https://code.idea4j.com/tags/GitHub-Copilot/"/>
    <category term="开发工具" scheme="https://code.idea4j.com/tags/%E5%BC%80%E5%8F%91%E5%B7%A5%E5%85%B7/"/>
    <category term="Agent" scheme="https://code.idea4j.com/tags/Agent/"/>
    <category term="AI模型" scheme="https://code.idea4j.com/tags/AI%E6%A8%A1%E5%9E%8B/"/>
    <category term="代码补全" scheme="https://code.idea4j.com/tags/%E4%BB%A3%E7%A0%81%E8%A1%A5%E5%85%A8/"/>
    <category term="模型迁移" scheme="https://code.idea4j.com/tags/%E6%A8%A1%E5%9E%8B%E8%BF%81%E7%A7%BB/"/>
    <content>
      <![CDATA[<p>GitHub 宣布，2026 年 10 月 19 日起，将在 GitHub Copilot 的多个使用场景中弃用 6 个 AI 模型，范围包括 Copilot Chat、内联编辑、Ask 模式、Agent 模式和代码补全。官方同时给出了建议替代模型，但这不是一次简单的“把下拉框选项换掉”：模型切换可能改变响应速度、代码风格、工具调用路径、上下文使用方式和请求成本。</p><p>这次变更值得开发者提前处理的原因，是很多团队已经把模型选择写进了使用规范、Agent 提示词、评测记录和开发流程。模型名字从产品界面消失只是最后一步，真正需要迁移的是围绕旧模型形成的质量基线和使用预期。现在先盘点引用、建立小型回归集，通常比 10 月 19 日之后才发现 Agent 行为改变更从容。</p><span id="more"></span><h2 id="先看清楚：这次具体弃用了哪些模型"><a href="#先看清楚：这次具体弃用了哪些模型" class="headerlink" title="先看清楚：这次具体弃用了哪些模型"></a>先看清楚：这次具体弃用了哪些模型</h2><p>GitHub Changelog 给出了明确的弃用日期和建议替代模型：</p><table><thead><tr><th>将被弃用的模型</th><th>弃用日期</th><th>官方建议替代模型</th></tr></thead><tbody><tr><td>Gemini 3.7 Flash</td><td>2026-10-19</td><td>Gemini 3.8 Flash</td></tr><tr><td>GPT-5.5</td><td>2026-10-19</td><td>GPT-5.6 Sol</td></tr><tr><td>GPT-5.4</td><td>2026-10-19</td><td>GPT-5.6 Sol</td></tr><tr><td>GPT-5.4 mini</td><td>2026-10-19</td><td>GPT-5.6 Luna</td></tr><tr><td>GPT-5 mini</td><td>2026-10-19</td><td>GPT-5.6 Luna</td></tr><tr><td>Grok 4.5</td><td>2026-10-19</td><td>Grok 4.6</td></tr></tbody></table><p>这里有三个边界需要先说清楚。</p><p>第一，这不是 Copilot 整体停止服务，也不是所有模型同时退役，而是公告列出的 6 个模型在指定日期后不再继续提供。第二，表格里的替代模型是 GitHub 给出的建议，不意味着两者在代码风格、推理深度、延迟或计费权重上完全一致。第三，变更覆盖的不只是聊天窗口，也包括代码补全和 Agent 相关体验，因此“我平时不用 Copilot Chat”并不能自动排除影响。</p><h2 id="为什么模型迁移会影响工程结果"><a href="#为什么模型迁移会影响工程结果" class="headerlink" title="为什么模型迁移会影响工程结果"></a>为什么模型迁移会影响工程结果</h2><p>模型选择常被当作个人偏好：觉得某个模型写代码顺手，就在设置里一直选它。但一旦模型参与 Agent 或团队协作，它实际上已经成为工程流程的一部分。</p><h3 id="代码补全影响的是节奏和局部风格"><a href="#代码补全影响的是节奏和局部风格" class="headerlink" title="代码补全影响的是节奏和局部风格"></a>代码补全影响的是节奏和局部风格</h3><p>代码补全通常发生在编辑器的高频交互中。模型切换后，最先被感知的可能不是“能不能写出来”，而是建议出现的速度、补全长度、对局部上下文的取舍，以及对项目现有命名和风格的贴合程度。</p><p>如果团队把生成结果直接纳入日常开发，建议至少抽取一组真实但不含敏感信息的代码片段，比较新旧模型的接受率、修改次数和明显错误类型。不要只用一个算法题或一段人工编写的 Prompt，因为那样测到的更像模型演示效果，而不是团队自己的编辑器工作流。</p><h3 id="Agent-模式影响的是完整执行链"><a href="#Agent-模式影响的是完整执行链" class="headerlink" title="Agent 模式影响的是完整执行链"></a>Agent 模式影响的是完整执行链</h3><p>Agent 不只返回一段代码，它还可能读取文件、规划步骤、调用工具、运行测试，再根据结果继续修改。模型换代后，变化可能出现在：</p><ul><li>是否能准确理解仓库约束和已有实现；</li><li>是否会把一个简单任务拆成过多步骤；</li><li>工具调用参数是否更稳健，失败后是否能恢复；</li><li>是否倾向于修改更多文件，或留下更大的审查面；</li><li>测试失败时，是修复根因还是反复尝试表面改动。</li></ul><p>因此，Agent 的迁移不能只比较最终补丁是否能通过一次测试，还要记录修改范围、命令调用、失败次数和人工返工量。</p><h3 id="团队策略影响的是可用性和治理"><a href="#团队策略影响的是可用性和治理" class="headerlink" title="团队策略影响的是可用性和治理"></a>团队策略影响的是可用性和治理</h3><p>GitHub 公告说明，在默认模型启用的情况下，Copilot Business 和 Copilot Enterprise 客户的建议替代模型会自动启用，前提是管理员没有关闭全局默认设置或显式禁用对应模型。如果组织关闭了全局默认启用，则需要在 Copilot 设置中的模型策略里主动开放替代模型。</p><p>这意味着个人用户和组织管理员面对的迁移动作不完全一样：个人需要重新确认常用模型和工作习惯；管理员还要检查模型策略、默认模型、团队文档和内部培训材料，避免用户在弃用后才发现替代模型根本没有权限。</p><h2 id="一份不依赖具体平台配置的迁移清单"><a href="#一份不依赖具体平台配置的迁移清单" class="headerlink" title="一份不依赖具体平台配置的迁移清单"></a>一份不依赖具体平台配置的迁移清单</h2><h3 id="1-盘点旧模型出现在哪里"><a href="#1-盘点旧模型出现在哪里" class="headerlink" title="1. 盘点旧模型出现在哪里"></a>1. 盘点旧模型出现在哪里</h3><p>先不要急着修改所有文档，先把引用位置找出来。可以从仓库里的工作流、开发工具配置、脚本和文档开始：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">rg -n -i \</span><br><span class="line">  <span class="string">&quot;gpt-5(\.4|\.5)?|gpt-5 mini|gemini 3\.7 flash|grok 4\.5|model&quot;</span> \</span><br><span class="line">  .github .vscode scripts docs README* 2&gt;/dev/null</span><br></pre></td></tr></table></figure><p>仓库搜索只能覆盖代码和配置，不能代替平台设置检查。还应盘点：</p><ul><li>团队是否在 Copilot 使用规范中指定了某个模型；</li><li>Agent 或提示词模板是否暗含某个模型的能力假设；</li><li>评测报告和示例截图是否仍然以旧模型为基线；</li><li>组织或企业的模型策略是否关闭了建议替代模型；</li><li>自动化脚本、内部代理或网关是否保存了旧模型名称。</li></ul><p>不要把官方建议替代模型直接写成一个全局硬编码值。模型可用性还会受到 Copilot 计划、使用客户端、组织策略和平台发布节奏影响，应该以实际支持列表为准。</p><h3 id="2-给每类任务建立最小回归集"><a href="#2-给每类任务建立最小回归集" class="headerlink" title="2. 给每类任务建立最小回归集"></a>2. 给每类任务建立最小回归集</h3><p>迁移评测不需要一开始就做成复杂基准。一个有代表性的最小集合，通常包含：</p><table><thead><tr><th>任务类型</th><th>需要观察的结果</th></tr></thead><tbody><tr><td>补全一个已有函数</td><td>采纳率、无关改动、类型和边界错误</td></tr><tr><td>修改一个跨文件功能</td><td>依赖理解、修改范围、测试是否完整</td></tr><tr><td>修复一个带失败测试的缺陷</td><td>定位速度、修复根因、回归测试质量</td></tr><tr><td>编写脚本或查询</td><td>命令安全性、异常处理、可维护性</td></tr><tr><td>让 Agent 运行测试并迭代</td><td>工具调用、失败恢复、总步骤和总耗时</td></tr></tbody></table><p>每个任务保留输入提交、允许使用的工具、测试命令和验收标准。这样比较的是模型迁移前后的工程结果，而不是某一次回答是否“看起来更聪明”。</p><h3 id="3-把模型切换和代码变更分开"><a href="#3-把模型切换和代码变更分开" class="headerlink" title="3. 把模型切换和代码变更分开"></a>3. 把模型切换和代码变更分开</h3><p>迁移期间尽量不要同时升级框架、重写提示词、调整测试和更换模型。否则即使结果变好或变坏，也很难判断原因。</p><p>可以给团队保留一份简单的迁移记录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">任务：为订单查询增加分页</span><br><span class="line">旧模型：GPT-5.4</span><br><span class="line">建议替代：GPT-5.6 Sol</span><br><span class="line">输入提交：abc123</span><br><span class="line">验证命令：./gradlew test --tests OrderQueryTest</span><br><span class="line">旧模型结果：通过，修改 3 个文件</span><br><span class="line">新模型结果：通过，修改 4 个文件，新增 1 个无关重构</span><br><span class="line">人工结论：功能可接受，需收紧修改范围</span><br></pre></td></tr></table></figure><p>这种记录比一句“新模型也能用”更有价值。它可以帮助团队决定哪些任务适合自动迁移，哪些任务需要保留人工检查或改用另一个受支持模型。</p><h3 id="4-提前检查配额和计费影响"><a href="#4-提前检查配额和计费影响" class="headerlink" title="4. 提前检查配额和计费影响"></a>4. 提前检查配额和计费影响</h3><p>替代模型可能拥有不同的请求乘数、速率限制或上下文特性。即使功能结果相近，Agent 的多轮调用也可能放大成本差异。</p><p>在迁移评估中，建议同时记录：单任务请求次数、输入输出规模、总耗时、失败重试次数和实际计费指标。不要只比较一次回答的价格，尤其是 Agent 任务；真正的成本往往来自多轮规划、工具调用和失败后的再次执行。</p><h2 id="管理员和平台团队应该提前做什么"><a href="#管理员和平台团队应该提前做什么" class="headerlink" title="管理员和平台团队应该提前做什么"></a>管理员和平台团队应该提前做什么</h2><p>管理员可以把这次弃用当作一次模型治理演练，而不只是被动通知用户。</p><p>首先，在 10 月 19 日前确认组织和企业策略没有无意中阻止建议替代模型。其次，把模型弃用日期、替代关系和支持范围放进团队的变更记录。再次，为 Agent、代码审查和代码补全分别设置反馈渠道，因为同一个替代模型在这三个场景里的表现未必相同。</p><p>如果团队有内部质量门禁，还可以把“模型名称”从唯一的配置维度改成更稳定的任务能力描述。例如，文档可以记录“用于快速补全的低延迟模型”或“用于跨文件 Agent 的高质量模型”，同时保留当前实际选择的模型和评测日期。这样下一次模型退役时，迁移的是能力目标，而不是到处搜索一串旧名字。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>GitHub Copilot 的这次变更给了开发者一个很具体的提醒：模型不是永远存在的工具开关，而是有生命周期的工程依赖。官方已经给出了 2026 年 10 月 19 日这个明确日期和替代建议，剩下的工作是确认组织策略、盘点旧引用，并用自己的代码和 Agent 任务做一次小规模回归。</p><p>最稳妥的迁移方式，不是等旧模型从列表里消失后临时选择一个新模型，而是提前把“模型更换”变成可观察、可比较、可回滚的工程变更。这样模型升级带来的不确定性，才不会直接传导到代码质量和交付节奏上。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ol><li>GitHub Changelog： <a href="https://github.blog/changelog/2026-09-18-upcoming-deprecation-of-selected-github-copilot-models-in-mid-october">Upcoming deprecation of selected GitHub Copilot models in mid-October</a></li><li>GitHub Docs： <a href="https://docs.github.com/en/copilot/reference/ai-models/supported-models">Supported AI models in GitHub Copilot</a></li><li>GitHub Docs： <a href="https://docs.github.com/en/copilot/how-tos/administer-copilot/manage-for-organization/configure-access-to-ai-models">Managing access to AI models for your organization</a></li></ol>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/21/2026-GitHubCopilot%E6%A8%A1%E5%9E%8B%E5%BC%83%E7%94%A8%E8%BF%81%E7%A7%BB%E6%B8%85%E5%8D%95/</id>
    <link href="https://code.idea4j.com/2026/09/21/2026-GitHubCopilot%E6%A8%A1%E5%9E%8B%E5%BC%83%E7%94%A8%E8%BF%81%E7%A7%BB%E6%B8%85%E5%8D%95/"/>
    <published>2026-09-21T00:01:00.000Z</published>
    <summary>GitHub 宣布将于 2026 年 10 月 19 日在 Copilot 的多个使用场景中弃用 6 个模型，并给出替代模型。本文整理官方映射关系，分析对个人开发、团队策略和 Agent 回归测试的影响。</summary>
    <title>GitHub Copilot 将在 10 月 19 日弃用 6 个模型：迁移不只是改一个下拉框</title>
    <updated>2026-09-21T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="DevOps" scheme="https://code.idea4j.com/categories/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/DevOps/"/>
    <category term="Linux" scheme="https://code.idea4j.com/tags/Linux/"/>
    <category term="CI/CD" scheme="https://code.idea4j.com/tags/CI-CD/"/>
    <category term="GitHub Actions" scheme="https://code.idea4j.com/tags/GitHub-Actions/"/>
    <category term="Runner" scheme="https://code.idea4j.com/tags/Runner/"/>
    <category term="Ubuntu" scheme="https://code.idea4j.com/tags/Ubuntu/"/>
    <category term="可重复构建" scheme="https://code.idea4j.com/tags/%E5%8F%AF%E9%87%8D%E5%A4%8D%E6%9E%84%E5%BB%BA/"/>
    <content>
      <![CDATA[<p>GitHub Actions 的 Ubuntu 26.04 runner 已经结束 Public Preview，进入 General Availability。与此同时，<code>ubuntu-latest</code> 将迁移到 Ubuntu 26.04。对很多工作流来说，配置文件可能一行都不用改，但实际运行环境已经发生变化：系统库、预装工具、编译器、容器运行时和默认语言版本都可能跟着镜像更新。</p><p>这次变化值得关注的地方，不是“Ubuntu 又升级了一个版本”，而是 CI 中最常见的一个便利标签开始指向新的操作系统。只要工作流仍然使用 <code>runs-on: ubuntu-latest</code>，构建环境就不是完全固定的。开发者现在需要做的，是决定哪些流水线可以跟随迁移，哪些流水线必须先固定版本，并用一次可重复的探测和回归测试把差异找出来。</p><span id="more"></span><h2 id="背景：ubuntu-latest-本质上是一个会移动的别名"><a href="#背景：ubuntu-latest-本质上是一个会移动的别名" class="headerlink" title="背景：ubuntu-latest 本质上是一个会移动的别名"></a>背景：<code>ubuntu-latest</code> 本质上是一个会移动的别名</h2><p>GitHub-hosted runner 通常通过 <code>runs-on</code> 选择运行环境。最省事的写法是：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">jobs:</span></span><br><span class="line">  <span class="attr">build:</span></span><br><span class="line">    <span class="attr">runs-on:</span> <span class="string">ubuntu-latest</span></span><br></pre></td></tr></table></figure><p>它表达的是“使用 GitHub 当前推荐的 Ubuntu 镜像”，而不是“永远使用某一个 Ubuntu 版本”。当 GitHub 将别名从一个长期支持版本迁移到另一个版本时，工作流文件不变，底层执行环境却会改变。</p><p>这种设计对大多数项目很方便。团队可以自动获得新的系统补丁、工具链和硬件支持，不必手动维护每一台构建机。但它也带来一个容易被忽略的事实：<code>ubuntu-latest</code> 更像一个发布通道，而不是一个版本锁。</p><p>一个依赖系统环境的构建可能同时受到这些变化影响：</p><ul><li>C&#x2F;C++、Rust 或 Node.js 原生扩展使用的编译器、链接器和系统库；</li><li>Python、Node.js、Java 等预装运行时及其默认路径；</li><li>OpenSSL、libffi、图形库和数据库客户端等动态依赖；</li><li>Docker、缓存 Action 和包管理器对目录、权限或文件系统行为的假设；</li><li>ARM64 与 x64 架构下的二进制依赖、预编译包和测试结果。</li></ul><p>因此，“工作流没有改动”不等于“构建输入没有改动”。</p><h2 id="Ubuntu-26-04-runner-这次改变了什么"><a href="#Ubuntu-26-04-runner-这次改变了什么" class="headerlink" title="Ubuntu 26.04 runner 这次改变了什么"></a>Ubuntu 26.04 runner 这次改变了什么</h2><p>GitHub 在 2026 年 9 月 17 日的 Changelog 中宣布 Ubuntu 26.04 runner 正式可用，并将其纳入 <code>ubuntu-latest</code> 的迁移计划。对使用 GitHub-hosted runner 的项目，可以把变化拆成三层理解。</p><h3 id="1-ubuntu-26-04-从预览能力变成可用于生产的标签"><a href="#1-ubuntu-26-04-从预览能力变成可用于生产的标签" class="headerlink" title="1. ubuntu-26.04 从预览能力变成可用于生产的标签"></a>1. <code>ubuntu-26.04</code> 从预览能力变成可用于生产的标签</h3><p>Public Preview 阶段适合验证兼容性，但不应该被当作稳定的生产基线。现在 <code>ubuntu-26.04</code> 已经进入 GA，意味着团队可以把它写进明确的测试或生产工作流，并按照 GitHub-hosted runner 的镜像发布节奏进行维护。</p><p>这里的“正式可用”不代表镜像中的每个预装软件版本永远不变。Runner image 仍会持续更新补丁和工具版本。它解决的是操作系统标签和服务可用性的问题，不是替应用锁定完整的软件供应链。</p><h3 id="2-ubuntu-latest-会发生迁移，而不是新增一个静态别名"><a href="#2-ubuntu-latest-会发生迁移，而不是新增一个静态别名" class="headerlink" title="2. ubuntu-latest 会发生迁移，而不是新增一个静态别名"></a>2. <code>ubuntu-latest</code> 会发生迁移，而不是新增一个静态别名</h3><p>如果工作流使用 <code>ubuntu-latest</code>，它会随着 GitHub 的迁移计划转向 Ubuntu 26.04。这个别名适合愿意持续跟进平台变化、并且已经有充分回归测试的项目；对于发布库、构建桌面安装包或编译大量原生依赖的项目，不能只依赖“最新”二字来保证可重复构建。</p><p>迁移期间还要注意一个工程现实：标签切换通常是一个发布过程，不应假设所有任务在同一时刻、以完全相同的环境完成切换。只要一个项目的测试和发布链路跨越了迁移窗口，就应该把操作系统版本作为诊断信息记录下来。</p><h3 id="3-hosted-runner-与自托管-Runner-是两条不同的迁移路径"><a href="#3-hosted-runner-与自托管-Runner-是两条不同的迁移路径" class="headerlink" title="3. hosted runner 与自托管 Runner 是两条不同的迁移路径"></a>3. hosted runner 与自托管 Runner 是两条不同的迁移路径</h3><p><code>ubuntu-26.04</code> 是 GitHub-hosted runner 的镜像标签。使用自托管 Runner 的团队，不会因为把 GitHub Actions 的标签写成某个版本，就自动得到一台 Ubuntu 26.04 主机。</p><p>自托管环境仍然由团队负责操作系统升级、Runner 软件版本、工具链安装、缓存清理和回滚策略。如果工作流使用的是自定义标签，例如 <code>self-hosted</code>、<code>linux</code> 或 <code>arm64</code>，迁移时应先盘点这些标签背后到底有哪些机器，避免把 GitHub-hosted 的镜像公告误当作自有基础设施已经升级。</p><h2 id="不要只比较-Ubuntu-版本号，要比较构建输入"><a href="#不要只比较-Ubuntu-版本号，要比较构建输入" class="headerlink" title="不要只比较 Ubuntu 版本号，要比较构建输入"></a>不要只比较 Ubuntu 版本号，要比较构建输入</h2><p>操作系统迁移最容易出现的误区，是只在日志里找一行 <code>Ubuntu 26.04</code>，然后认为兼容性检查已经完成。真正有价值的比较应该覆盖构建实际使用的输入。</p><p>可以至少检查以下几类信息：</p><table><thead><tr><th>类别</th><th>建议记录的内容</th><th>常见风险</th></tr></thead><tbody><tr><td>操作系统</td><td><code>/etc/os-release</code>、内核、架构</td><td>包名称、默认行为或架构分支变化</td></tr><tr><td>编译工具</td><td>GCC&#x2F;Clang、Make、CMake、Rust</td><td>原生扩展编译失败或警告变成错误</td></tr><tr><td>语言运行时</td><td>Node.js、Python、Java、Go</td><td>默认版本改变导致依赖解析或语法行为变化</td></tr><tr><td>系统库</td><td>OpenSSL、glibc、libstdc++、图形库</td><td>动态链接失败、ABI 或 TLS 行为差异</td></tr><tr><td>容器环境</td><td>Docker Engine、Buildx、Compose</td><td>构建缓存、权限和多架构构建差异</td></tr><tr><td>Action 与缓存</td><td>Action 版本、缓存键、路径</td><td>缓存命中但内容不兼容，或旧 Action 行为异常</td></tr></tbody></table><p>GitHub 提供的 runner image 仓库会公开镜像内容和变更记录。排查问题时，应该把失败任务对应的镜像版本、工作流提交和依赖锁文件一起保存，而不是只保留最后一段报错。</p><h2 id="一套低风险的迁移方法"><a href="#一套低风险的迁移方法" class="headerlink" title="一套低风险的迁移方法"></a>一套低风险的迁移方法</h2><h3 id="第一步：先找出所有使用移动标签的工作流"><a href="#第一步：先找出所有使用移动标签的工作流" class="headerlink" title="第一步：先找出所有使用移动标签的工作流"></a>第一步：先找出所有使用移动标签的工作流</h3><p>不要只搜索默认分支。工作流可能位于复用工作流、发布分支和示例目录中，可以先做一次仓库级盘点：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">rg -n <span class="string">&quot;runs-on:|ubuntu-latest|ubuntu-[0-9]+\.[0-9]+&quot;</span> \</span><br><span class="line">  .github/workflows .github/actions 2&gt;/dev/null</span><br></pre></td></tr></table></figure><p>将结果分成三类：可以直接跟随迁移的普通测试、对原生依赖敏感的构建，以及发布或生产部署任务。第三类不应该因为标签方便就自动升级。</p><h3 id="第二步：用矩阵同时比较旧版和新版"><a href="#第二步：用矩阵同时比较旧版和新版" class="headerlink" title="第二步：用矩阵同时比较旧版和新版"></a>第二步：用矩阵同时比较旧版和新版</h3><p>在独立的验证工作流中，让相同的步骤分别运行在 <code>ubuntu-24.04</code> 和 <code>ubuntu-26.04</code>。下面的例子只探测关键环境，实际项目还应接上完整的编译、测试和打包步骤：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">name:</span> <span class="string">runner-compatibility</span></span><br><span class="line"></span><br><span class="line"><span class="attr">on:</span></span><br><span class="line">  <span class="attr">workflow_dispatch:</span></span><br><span class="line"></span><br><span class="line"><span class="attr">jobs:</span></span><br><span class="line">  <span class="attr">probe:</span></span><br><span class="line">    <span class="attr">strategy:</span></span><br><span class="line">      <span class="attr">fail-fast:</span> <span class="literal">false</span></span><br><span class="line">      <span class="attr">matrix:</span></span><br><span class="line">        <span class="attr">os:</span> [<span class="string">ubuntu-24.04</span>, <span class="string">ubuntu-26.04</span>]</span><br><span class="line">    <span class="attr">runs-on:</span> <span class="string">$&#123;&#123;</span> <span class="string">matrix.os</span> <span class="string">&#125;&#125;</span></span><br><span class="line">    <span class="attr">steps:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">uses:</span> <span class="string">actions/checkout@v5</span></span><br><span class="line"></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Inspect</span> <span class="string">runner</span></span><br><span class="line">        <span class="attr">run:</span> <span class="string">|</span></span><br><span class="line"><span class="string">          set -eux</span></span><br><span class="line"><span class="string">          echo &quot;runner=$&#123;&#123; matrix.os &#125;&#125;&quot;</span></span><br><span class="line"><span class="string">          cat /etc/os-release</span></span><br><span class="line"><span class="string">          uname -a</span></span><br><span class="line"><span class="string">          uname -m</span></span><br><span class="line"><span class="string">          node --version || true</span></span><br><span class="line"><span class="string">          python3 --version || true</span></span><br><span class="line"><span class="string">          gcc --version | head -n 1 || true</span></span><br><span class="line"><span class="string">          docker version || true</span></span><br><span class="line"><span class="string"></span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Run</span> <span class="string">project</span> <span class="string">verification</span></span><br><span class="line">        <span class="attr">run:</span> <span class="string">./ci/verify.sh</span></span><br></pre></td></tr></table></figure><p><code>actions/checkout</code> 的版本只是示例，团队应按自己的 Action 维护策略选择版本。更重要的是，验证矩阵中的两条路径必须运行同一套脚本，不能为了让新版“通过”而悄悄跳过测试。</p><h3 id="第三步：把容易漂移的依赖显式化"><a href="#第三步：把容易漂移的依赖显式化" class="headerlink" title="第三步：把容易漂移的依赖显式化"></a>第三步：把容易漂移的依赖显式化</h3><p>Runner 版本固定后，仍然有很多输入可能漂移。迁移时可以顺便检查：</p><ul><li>使用 <code>package-lock.json</code>、<code>poetry.lock</code>、<code>Cargo.lock</code> 等锁文件；</li><li>对 <code>setup-node</code>、<code>setup-python</code>、<code>setup-java</code> 等 Action 明确指定运行时版本；</li><li>固定 Docker 基础镜像的 digest，至少为发布构建保留可追溯版本；</li><li>为缓存键加入操作系统和架构，例如 <code>ubuntu-26.04-arm64</code>；</li><li>把生成物中的编译器、系统版本和 Git 提交写进构建元数据；</li><li>将发布构建与普通 PR 构建分开，给发布链路更长的验证窗口。</li></ul><p>尤其要小心缓存。缓存命中会让一次不兼容的旧产物继续参与构建，从而把“镜像升级造成的问题”伪装成随机失败。迁移初期宁可使用带新版本标识的缓存键，也不要让旧系统生成的二进制产物直接复用到新系统。</p><h3 id="第四步：决定哪些工作流应该固定版本"><a href="#第四步：决定哪些工作流应该固定版本" class="headerlink" title="第四步：决定哪些工作流应该固定版本"></a>第四步：决定哪些工作流应该固定版本</h3><p>并不是所有任务都需要同一种策略：</p><table><thead><tr><th>工作流类型</th><th>建议</th></tr></thead><tbody><tr><td>纯脚本、无原生依赖的快速检查</td><td>可以继续使用 <code>ubuntu-latest</code>，但保留失败诊断信息</td></tr><tr><td>编译原生扩展、生成安装包</td><td>在完成回归前使用显式版本标签</td></tr><tr><td>面向多个发行版的兼容性测试</td><td>把 <code>ubuntu-24.04</code> 和 <code>ubuntu-26.04</code> 都纳入矩阵</td></tr><tr><td>正式发布、合规构建、需要复现历史产物</td><td>固定版本，并记录镜像与工具链信息</td></tr><tr><td>自托管 Runner</td><td>单独维护操作系统、Runner 和工具链升级流程</td></tr></tbody></table><p>固定 <code>ubuntu-24.04</code> 不是拒绝升级，而是把升级从一次隐式漂移改成一次可以安排、评审和回滚的变更。等新版验证完成后，再由平台团队主动切换到 <code>ubuntu-26.04</code>，通常比等发布流水线突然失败更容易控制。</p><h2 id="对团队-CI-治理的启示"><a href="#对团队-CI-治理的启示" class="headerlink" title="对团队 CI 治理的启示"></a>对团队 CI 治理的启示</h2><p>这次迁移再次说明，CI Runner 不是一个透明的执行盒子，而是软件供应链的一部分。编译器、系统库、容器运行时和预装命令都会影响最终产物；只要它们没有被记录，构建就很难真正复现。</p><p>对于平台团队，比较实用的做法是建立一张 Runner 清单，至少包括工作流文件、操作系统标签、架构、语言运行时、缓存策略和升级负责人。对于应用团队，则应把“升级 Runner 后重新跑一次完整验证”写进变更流程，而不是把所有兼容性问题归结为某个依赖包偶发失效。</p><p>还要把日志可观测性提前做好。每次构建输出操作系统、架构、主要工具版本和工作流提交后，排查“同一个提交为什么今天失败”会简单很多。对于正式发布，最好把这些信息作为构建证明或制品元数据的一部分保存下来。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Ubuntu 26.04 runner 正式 GA 是一个值得采用的新基线，但 <code>ubuntu-latest</code> 的迁移也提醒我们：方便的别名不等于稳定的版本契约。短期内最稳妥的动作，是盘点移动标签、用矩阵比较新旧镜像、刷新缓存键，并把发布链路从隐式跟随迁移改成显式验证。</p><p>当 Runner 的操作系统、架构和工具链都能被记录，Ubuntu 26.04 就不再是一次不可预期的 CI 波动，而会成为一项可以测试、批准和回滚的基础设施升级。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ol><li>GitHub Changelog： <a href="https://github.blog/changelog/2026-09-17-ubuntu-26-generally-available-and-latest-migration">Ubuntu 26.04 now generally available and <code>ubuntu-latest</code> migration</a></li><li>GitHub Actions Runner Images： <a href="https://github.com/actions/runner-images">官方镜像仓库与变更记录</a></li><li>GitHub Docs： <a href="https://docs.github.com/en/actions/using-github-hosted-runners/about-github-hosted-runners">GitHub-hosted runners</a></li></ol>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/20/2026-GitHubActionsUbuntu2604Runner%E6%AD%A3%E5%BC%8FGA/</id>
    <link href="https://code.idea4j.com/2026/09/20/2026-GitHubActionsUbuntu2604Runner%E6%AD%A3%E5%BC%8FGA/"/>
    <published>2026-09-20T00:01:00.000Z</published>
    <summary>GitHub Actions 的 Ubuntu 26.04 hosted runner 已正式可用，并开始成为 ubuntu-latest 的迁移目标。本文解释镜像标签、运行环境和自托管 Runner 的差异，并给出一套低风险迁移与验证方法。</summary>
    <title>GitHub Actions Ubuntu 26.04 Runner 正式 GA：ubuntu-latest 迁移前如何守住构建可重复性</title>
    <updated>2026-09-20T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="质量保证" scheme="https://code.idea4j.com/categories/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/%E8%B4%A8%E9%87%8F%E4%BF%9D%E8%AF%81/"/>
    <category term="CI/CD" scheme="https://code.idea4j.com/tags/CI-CD/"/>
    <category term="GitHub" scheme="https://code.idea4j.com/tags/GitHub/"/>
    <category term="Code Quality" scheme="https://code.idea4j.com/tags/Code-Quality/"/>
    <category term="代码覆盖率" scheme="https://code.idea4j.com/tags/%E4%BB%A3%E7%A0%81%E8%A6%86%E7%9B%96%E7%8E%87/"/>
    <category term="Rulesets" scheme="https://code.idea4j.com/tags/Rulesets/"/>
    <category term="REST API" scheme="https://code.idea4j.com/tags/REST-API/"/>
    <content>
      <![CDATA[<p>9 月 18 日，GitHub 宣布可以通过正式可用的 REST API 管理代码覆盖率规则集条件。维护者现在不必逐个打开仓库设置页面，就能为 Pull Request 配置最低行覆盖率，或限制相对于默认分支允许下降的覆盖率百分点。</p><p>这项更新的重点不是“把覆盖率数字搬到了 API”，而是让质量门禁开始具备策略即代码的形态：规则可以和仓库清单、基础配置、审查流程一起版本化，也可以由平台团队批量同步到多个仓库。不过，覆盖率规则仍然需要 GitHub Code Quality 和覆盖率上传链路提供数据；规则的意义也不是把所有项目强行锁在同一个百分比上。门禁能否真正改善质量，取决于阈值是否建立在可信基线和可解释的工程约束之上。</p><span id="more"></span><h2 id="这次-API-更新了什么"><a href="#这次-API-更新了什么" class="headerlink" title="这次 API 更新了什么"></a>这次 API 更新了什么</h2><p>GitHub 的更新针对的是规则集中的 <code>Restrict code coverage</code> 条件。它可以用两种方式阻止 Pull Request 合并：</p><table><thead><tr><th>条件</th><th>判断方式</th><th>适合解决的问题</th></tr></thead><tbody><tr><td>最低行覆盖率</td><td>PR 分支的聚合行覆盖率低于设定百分比时阻止合并</td><td>防止新代码让整体基线跌破底线</td></tr><tr><td>最大覆盖率下降</td><td>相对默认分支下降超过设定百分点时阻止合并</td><td>允许不同仓库有不同基线，限制回归幅度</td></tr></tbody></table><p>REST API 的规则片段可以表达为：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;code_coverage&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;parameters&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;minimum_coverage&quot;</span><span class="punctuation">:</span> <span class="number">80</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;max_coverage_drop&quot;</span><span class="punctuation">:</span> <span class="number">3</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这只是规则集中的 <code>rules</code> 片段。完整的规则集还需要指定目标类型、匹配的分支或标签以及 <code>active</code>、<code>evaluate</code> 等执行状态，实际字段应以当前 API 文档为准。两个阈值表达的是不同策略：<code>80</code> 是绝对底线，<code>3</code> 是相对于默认分支允许下降的百分点，不能把“覆盖率下降 3%”简单理解成“下降 3 个百分点”。</p><p>过去，这项规则主要通过网页界面配置。现在，平台团队可以用 API 创建、读取和更新规则，把“哪些仓库需要什么门槛”放在配置文件或内部治理系统里，再由自动化程序调用仓库规则集接口完成同步。</p><h2 id="先把前置条件分清楚"><a href="#先把前置条件分清楚" class="headerlink" title="先把前置条件分清楚"></a>先把前置条件分清楚</h2><p>覆盖率规则不会凭空产生数据。GitHub 的公告和文档列出了两个关键前提：</p><ol><li>仓库已经启用 GitHub Code Quality；</li><li>CI 已经上传了代码覆盖率数据。</li></ol><p>因此，接入顺序应该是“先让数据稳定，再让规则阻断”。如果测试没有生成报告、报告格式不被识别、上传发生在规则评估之后，规则就很难提供可靠的合并判断。API 请求成功也不代表覆盖率门禁已经生效，仍要在真实 Pull Request 上验证从测试到上传、从上传到规则评估的完整链路。</p><p>这项能力目前可用于 GitHub Enterprise Cloud、包含 Data Residency 的 GitHub Enterprise Cloud，以及 GitHub Team；GitHub Enterprise Server 不在公告列出的支持范围内。混用 GitHub.com 与 GHES 的团队不能只复制同一份规则配置，还要在发布平台前先检查产品能力和 API 支持情况。</p><h2 id="两种阈值应该怎样选择"><a href="#两种阈值应该怎样选择" class="headerlink" title="两种阈值应该怎样选择"></a>两种阈值应该怎样选择</h2><h3 id="绝对最低覆盖率：适合定义底线"><a href="#绝对最低覆盖率：适合定义底线" class="headerlink" title="绝对最低覆盖率：适合定义底线"></a>绝对最低覆盖率：适合定义底线</h3><p><code>minimum_coverage</code> 适合用在有较稳定测试体系的服务或库中。例如，一个已有 85% 行覆盖率的核心模块，可以设定 80% 的最低值，避免后续合并把整个基线拖到一个明显更差的水平。</p><p>但新仓库不应一开始就随意设定一个漂亮的百分比。测试还不完整时，绝对门槛可能导致团队先补大量低价值测试，只为让数字达标。更稳妥的做法是先观察一段时间的覆盖率分布，确认测试报告稳定，再把当前可持续的水平转成门禁。</p><h3 id="最大下降幅度：适合控制回归"><a href="#最大下降幅度：适合控制回归" class="headerlink" title="最大下降幅度：适合控制回归"></a>最大下降幅度：适合控制回归</h3><p><code>max_coverage_drop</code> 更适合覆盖率已经存在差异的多仓库组织。它不要求所有仓库都达到同一个绝对值，而是约束每个 Pull Request 相对于自己默认分支的回退。</p><p>例如，A 仓库长期在 92%，B 仓库长期在 64%。如果统一设置 80%，B 仓库会立刻被大量历史问题卡住；如果只允许下降 2 个百分点，两个仓库都能先防止新的回归。这个策略不能自动消除 B 仓库的技术债，但不会把“改善历史基线”和“防止新问题”混成一次大迁移。</p><h3 id="两个条件可以组合，但不要掩盖数据含义"><a href="#两个条件可以组合，但不要掩盖数据含义" class="headerlink" title="两个条件可以组合，但不要掩盖数据含义"></a>两个条件可以组合，但不要掩盖数据含义</h3><p>绝对底线和相对下降可以同时使用：一个防止长期低于最低标准，一个防止单次变更造成明显回退。组合前要先确认团队理解的是“行覆盖率”还是分支、函数、条件覆盖率等其他指标。当前这条规则针对的是行覆盖率，不能把它当成完整的测试充分性证明。</p><h2 id="API-化之后，治理方式会发生什么变化"><a href="#API-化之后，治理方式会发生什么变化" class="headerlink" title="API 化之后，治理方式会发生什么变化"></a>API 化之后，治理方式会发生什么变化</h2><h3 id="1-规则可以进入基础设施代码"><a href="#1-规则可以进入基础设施代码" class="headerlink" title="1. 规则可以进入基础设施代码"></a>1. 规则可以进入基础设施代码</h3><p>平台仓库可以维护一份规则声明，记录每个仓库对应的目标范围、执行状态、最低覆盖率和允许下降幅度。变更经过 Pull Request 审查后，再由具有最小权限的 GitHub App 或自动化服务调用 API 同步。</p><p>这种方式比人工点击更容易复盘：团队可以回答“谁在什么时候把某个仓库的覆盖率门槛从 80 调成 75”，也可以把规则配置和应用迁移、测试框架升级放在同一个变更上下文中。</p><h3 id="2-规则漂移可以被发现"><a href="#2-规则漂移可以被发现" class="headerlink" title="2. 规则漂移可以被发现"></a>2. 规则漂移可以被发现</h3><p>批量管理的难点不是第一次写入，而是以后有人通过网页修改了单个仓库。同步程序可以定期读取仓库规则集，与期望配置做差异比较，并把漂移报告给平台团队。自动修复之前，应先区分有意的例外、临时迁移和未授权修改，避免一个全局同步任务覆盖正在进行的仓库升级。</p><h3 id="3-例外需要被显式管理"><a href="#3-例外需要被显式管理" class="headerlink" title="3. 例外需要被显式管理"></a>3. 例外需要被显式管理</h3><p>不同仓库的测试成本、代码类型和发布风险不同。例外并不一定是不合规，但它应该有负责人、原因、有效期和复查时间。把“这个仓库暂时不设置规则”留在人工记忆里，会比规则本身更难审计。</p><p>建议把这些信息放在配置层：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">repositories:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">payment-service</span></span><br><span class="line">    <span class="attr">coverage:</span></span><br><span class="line">      <span class="attr">minimum:</span> <span class="number">80</span></span><br><span class="line">      <span class="attr">max_drop:</span> <span class="number">2</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">legacy-adapter</span></span><br><span class="line">    <span class="attr">coverage:</span></span><br><span class="line">      <span class="attr">max_drop:</span> <span class="number">1</span></span><br><span class="line">      <span class="attr">exception:</span></span><br><span class="line">        <span class="attr">reason:</span> <span class="string">&quot;正在迁移覆盖率上传工具&quot;</span></span><br><span class="line">        <span class="attr">expires:</span> <span class="string">&quot;2026-10-15&quot;</span></span><br></pre></td></tr></table></figure><p>这里的 YAML 只是治理系统的示意格式，不是 GitHub REST API 的直接请求体。真正调用 API 时，仍应将它转换成仓库规则集接口需要的 JSON，并校验仓库、组织和产品计划是否支持该规则。</p><h2 id="把覆盖率门禁接进-CI-的正确顺序"><a href="#把覆盖率门禁接进-CI-的正确顺序" class="headerlink" title="把覆盖率门禁接进 CI 的正确顺序"></a>把覆盖率门禁接进 CI 的正确顺序</h2><p>一个可观察的流水线应该把覆盖率产生和规则评估明确分开：</p><pre class="mermaid">flowchart LR    A[检出 Pull Request] --> B[运行测试]    B --> C[生成覆盖率报告]    C --> D[上传覆盖率数据]    D --> E[规则集评估]    E --> F{达到阈值?}    F -- 是 --> G[允许进入审查或合并]    F -- 否 --> H[阻止合并并反馈差距]</pre><p>落地时要重点检查几个失败场景：</p><ul><li>测试失败时是否仍会错误地上传一份旧报告；</li><li>覆盖率上传失败时，规则是阻止合并还是只产生告警；</li><li>Pull Request 的基准分支是否发生变化，导致比较基线改变；</li><li>合并队列或重跑工作流时，覆盖率数据是否与当前提交对应；</li><li>monorepo 中只改一个子目录时，全仓库行覆盖率是否仍然是团队想要的指标。</li></ul><p>尤其要避免“为了过门禁而降低测试范围”这种反向激励。代码覆盖率应与测试失败、静态检查、代码审查和生产缺陷等信号一起观察；它能告诉团队哪些行没有被执行，却不能告诉团队测试断言是否有意义。</p><h2 id="建议用观察期完成迁移"><a href="#建议用观察期完成迁移" class="headerlink" title="建议用观察期完成迁移"></a>建议用观察期完成迁移</h2><p>如果组织准备为多个仓库统一配置规则，可以采用四个阶段：</p><ol><li><strong>盘点阶段</strong>：确认 Code Quality、覆盖率上传和默认分支状态，收集每个仓库最近一段时间的覆盖率基线。</li><li><strong>影子阶段</strong>：先用规则集的非阻断状态观察会被拦截的 Pull Request，统计缺失报告、历史低基线和真实回归分别占多少。</li><li><strong>分组启用</strong>：先选择测试稳定、发布风险高的服务启用阻断，再逐步扩展到普通仓库。</li><li><strong>持续复查</strong>：规则配置由 API 定期读取，报告漂移、例外到期和覆盖率数据中断，不要只在规则首次创建时验证一次。</li></ol><p>如果规则的执行状态和“影子阶段”能力要依赖具体产品配置，应以 GitHub 当前文档为准；不要在没有验证的情况下把 <code>evaluate</code> 当成所有仓库都拥有的完整模拟环境。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>GitHub 将代码覆盖率规则带进 REST API，真正改变的是质量门禁的管理方式：它可以从一个需要手工点击的仓库设置，变成一条可审查、可批量同步、可检测漂移的工程策略。</p><p>但 API 不会替团队决定合理的质量标准。先稳定覆盖率数据，再区分绝对底线和相对回退；先观察实际误报，再逐步阻断；给每个例外记录原因和期限。这样，覆盖率数字才会成为帮助团队发现回归的信号，而不是又一个需要想办法绕过的 CI 红灯。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://github.blog/changelog/2026-09-18-manage-the-code-coverage-ruleset-condition-with-the-rest-api">GitHub Changelog：Manage the code coverage ruleset condition with the REST API</a></li><li><a href="https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/available-rules-for-rulesets#restrict-code-coverage">GitHub Docs：Available rules for rulesets—Restrict code coverage</a></li><li><a href="https://docs.github.com/en/code-security/how-tos/maintain-quality-code/set-up-code-coverage">GitHub Docs：Set up code coverage</a></li><li><a href="https://docs.github.com/en/code-security/how-tos/maintain-quality-code/restrict-code-coverage">GitHub Docs：Restrict code coverage</a></li><li><a href="https://docs.github.com/en/rest/repos/rules">GitHub Docs：REST API endpoints for rules</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/19/2026-GitHub%E4%BB%A3%E7%A0%81%E8%A6%86%E7%9B%96%E7%8E%87%E8%A7%84%E5%88%99RESTAPI/</id>
    <link href="https://code.idea4j.com/2026/09/19/2026-GitHub%E4%BB%A3%E7%A0%81%E8%A6%86%E7%9B%96%E7%8E%87%E8%A7%84%E5%88%99RESTAPI/"/>
    <published>2026-09-19T00:01:00.000Z</published>
    <summary>GitHub 为 Restrict code coverage 规则新增正式可用的 REST API，可以用最低行覆盖率或最大覆盖率下降幅度约束 Pull Request。本文分析这项能力的前置条件、策略含义和渐进式落地方法。</summary>
    <title>GitHub 将代码覆盖率规则带进 REST API：质量门禁开始可以被当作代码管理</title>
    <updated>2026-09-19T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="云原生" scheme="https://code.idea4j.com/categories/%E4%BA%91%E5%8E%9F%E7%94%9F/"/>
    <category term="安全" scheme="https://code.idea4j.com/categories/%E4%BA%91%E5%8E%9F%E7%94%9F/%E5%AE%89%E5%85%A8/"/>
    <category term="Kubernetes" scheme="https://code.idea4j.com/tags/Kubernetes/"/>
    <category term="云原生" scheme="https://code.idea4j.com/tags/%E4%BA%91%E5%8E%9F%E7%94%9F/"/>
    <category term="安全" scheme="https://code.idea4j.com/tags/%E5%AE%89%E5%85%A8/"/>
    <category term="Linux" scheme="https://code.idea4j.com/tags/Linux/"/>
    <category term="容器" scheme="https://code.idea4j.com/tags/%E5%AE%B9%E5%99%A8/"/>
    <category term="存储" scheme="https://code.idea4j.com/tags/%E5%AD%98%E5%82%A8/"/>
    <category term="emptyDir" scheme="https://code.idea4j.com/tags/emptyDir/"/>
    <content>
      <![CDATA[<p>Kubernetes 官方在 9 月 16 日介绍了 1.37 中两项面向容器存储安全的能力：卷挂载可以设置 <code>noexec</code>、<code>nosuid</code>、<code>nodev</code> 等 bind mount 选项，<code>emptyDir</code> 也可以指定创建时的 Unix 权限模式。两项能力目前都是 Alpha，需要分别打开 <code>VolumeBindMountOptions</code> 和 <code>EmptyDirVolumeMode</code> 特性门控。</p><p>这次更新没有改变默认行为。省略新字段时，现有 Pod 仍按原来的方式工作；只有显式配置并满足节点、容器运行时和特性门控条件时，新的限制才会生效。它的价值在于，平台团队终于可以把“这个可写卷能不能执行程序、能不能解释设备文件、共享目录谁能删除谁的文件”写进 Pod 配置，而不必依赖 init container 里的 <code>chmod</code> 或节点上的手工挂载脚本。</p><span id="more"></span><h2 id="为什么可写卷是一个容易被忽略的安全边界"><a href="#为什么可写卷是一个容易被忽略的安全边界" class="headerlink" title="为什么可写卷是一个容易被忽略的安全边界"></a>为什么可写卷是一个容易被忽略的安全边界</h2><p>很多容器安全基线会设置 <code>readOnlyRootFilesystem: true</code>，但应用通常仍需要一个可写目录存放临时文件、构建产物、缓存或日志。这个目录可能来自 <code>emptyDir</code>、PersistentVolume、CSI 卷或其他投影卷。</p><p>如果可写卷默认允许执行文件，一个已经被入侵的进程就可能下载二进制文件、修改权限，然后从卷挂载点直接运行它。根文件系统只读，并不能阻止这条路径。类似地，如果挂载没有 <code>nosuid</code> 或 <code>nodev</code>，某些特殊文件和权限位也可能扩大进程能够利用的内核接口。</p><p>Linux 的三个挂载标志各自限制不同的行为：</p><table><thead><tr><th>选项</th><th>作用</th><th>典型用途</th></tr></thead><tbody><tr><td><code>noexec</code></td><td>不允许直接执行挂载点上的二进制文件</td><td>临时目录、上传目录、缓存目录</td></tr><tr><td><code>nosuid</code></td><td>不让 set-user-ID 或 set-group-ID 位生效</td><td>不信任的共享卷</td></tr><tr><td><code>nodev</code></td><td>不把设备文件解释为字符或块设备</td><td>应用数据卷、临时工作区</td></tr></tbody></table><p>这些限制并不是完整的沙箱。比如 <code>noexec</code> 主要阻止直接执行文件，不能替代 seccomp、Linux capabilities、用户命名空间或网络隔离。攻击者仍可能把脚本交给解释器运行，所以它应该被当成缩小攻击面的一层，而不是“一开就安全”的总开关。</p><h2 id="Kubernetes-1-37-新增了什么"><a href="#Kubernetes-1-37-新增了什么" class="headerlink" title="Kubernetes 1.37 新增了什么"></a>Kubernetes 1.37 新增了什么</h2><h3 id="1-在卷挂载上声明-bind-mount-选项"><a href="#1-在卷挂载上声明-bind-mount-选项" class="headerlink" title="1. 在卷挂载上声明 bind mount 选项"></a>1. 在卷挂载上声明 bind mount 选项</h3><p>新的 <code>bindMountOptions</code> 字段位于 <code>volumeMounts</code> 中，可以把挂载时的安全选项和具体容器路径绑定起来：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Pod</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">hardened-volume</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">containers:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">app</span></span><br><span class="line">      <span class="attr">image:</span> <span class="string">example/app:1.0</span></span><br><span class="line">      <span class="attr">volumeMounts:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">scratch</span></span><br><span class="line">          <span class="attr">mountPath:</span> <span class="string">/tmp</span></span><br><span class="line">          <span class="attr">bindMountOptions:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">noexec</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">nosuid</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">nodev</span></span><br><span class="line">  <span class="attr">volumes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">scratch</span></span><br><span class="line">      <span class="attr">emptyDir:</span> &#123;&#125;</span><br></pre></td></tr></table></figure><p>官方说明称，这个能力可以用于 <code>emptyDir</code>、PersistentVolume、CSI 卷、ConfigMap、Secret 和其他卷类型，镜像卷是明确不支持的例外。它解决的是“卷被怎样 bind mount 到容器里”，和 PersistentVolume 的 <code>mountOptions</code> 不是同一层：后者通常由 CSI 驱动在节点上设置文件系统级选项，不一定能传递到容器内实际看到的 bind mount。</p><h3 id="2-为-emptyDir-指定创建权限"><a href="#2-为-emptyDir-指定创建权限" class="headerlink" title="2. 为 emptyDir 指定创建权限"></a>2. 为 <code>emptyDir</code> 指定创建权限</h3><p><code>emptyDir</code> 过去默认以 <code>0777</code> 创建目录。应用可以通过 init container 再执行一次 <code>chmod</code>，但这种方式增加了启动步骤，也不容易被策略系统稳定检查。现在可以直接在 <code>emptyDir</code> 上声明模式：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">shared-workspace</span></span><br><span class="line">    <span class="attr">emptyDir:</span></span><br><span class="line">      <span class="attr">mode:</span> <span class="number">01777</span></span><br></pre></td></tr></table></figure><p><code>01777</code> 中多出来的最高位是 sticky bit。它适合多个容器共享临时目录的场景：每个容器都可以写入自己的文件，但不能随意删除其他用户创建的文件。对于只需要某个用户和组访问的临时数据，也可以考虑更窄的模式，例如 <code>0750</code>。</p><p>这个字段支持普通磁盘、<code>Memory</code> 和 <code>HugePages</code> 等 <code>emptyDir</code> medium。需要注意的是，如果 Pod 的安全上下文配置了 <code>fsGroup</code>，组权限可能会覆盖 <code>mode</code> 中对应的设置，最终权限仍应在实际节点上用 <code>ls -ld</code> 验证。</p><h2 id="Alpha-能力不是打开字段就够了"><a href="#Alpha-能力不是打开字段就够了" class="headerlink" title="Alpha 能力不是打开字段就够了"></a>Alpha 能力不是打开字段就够了</h2><p>两项功能都需要在 API Server 和 kubelet 上打开对应特性门控：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">VolumeBindMountOptions</span><br><span class="line">EmptyDirVolumeMode</span><br></pre></td></tr></table></figure><p>此外，<code>bindMountOptions</code> 依赖容器运行时支持 CRI 的 <code>mount_options</code> 字段，并且能够通过 <code>runtimeFeatures</code> 对外声明能力。Kubernetes 会利用节点声明的功能来避免把 Pod 调度到不支持的节点；如果 Pod 最终到达了不兼容的节点，kubelet 应拒绝它，而不是悄悄忽略挂载选项。</p><p><code>emptyDir.mode</code> 的版本偏差行为则更容易让人误判：如果 API Server 已打开特性门控，但 kubelet 没有打开，API 对象可能被接受，kubelet 却会忽略这个字段并回退到 <code>0777</code>。这意味着“对象成功写进 API Server”不等于“目录已经按预期加固”。</p><p>因此，启用前至少要同时确认：</p><ul><li>API Server 和 kubelet 使用的是预期的 1.37 配置；</li><li>所有可能承载目标 Pod 的节点运行时支持 CRI 挂载选项；</li><li>节点池没有混用会静默忽略 <code>emptyDir.mode</code> 的旧 kubelet；</li><li>Windows 节点不会被误当成 Linux 挂载能力的等价环境。</li></ul><p>这些标志和 Unix 权限是 Linux 概念。官方文档明确说明，Windows 节点不会应用 <code>bindMountOptions</code>，<code>emptyDir</code> 的 <code>mode</code> 在 Windows 上也会跳过。</p><h2 id="对哪些工作负载最有帮助"><a href="#对哪些工作负载最有帮助" class="headerlink" title="对哪些工作负载最有帮助"></a>对哪些工作负载最有帮助</h2><h3 id="临时工作目录和上传目录"><a href="#临时工作目录和上传目录" class="headerlink" title="临时工作目录和上传目录"></a>临时工作目录和上传目录</h3><p>编译缓存、解压目录和用户上传目录往往需要写权限，却没有理由允许直接运行其中的程序。对这些挂载使用 <code>noexec</code>、<code>nosuid</code>，通常比依赖应用代码“记得不要执行上传文件”更容易形成基础约束。</p><h3 id="多容器-Pod-共享工作区"><a href="#多容器-Pod-共享工作区" class="headerlink" title="多容器 Pod 共享工作区"></a>多容器 Pod 共享工作区</h3><p>CI&#x2F;CD Pod 经常让构建容器、日志 sidecar 和产物收集容器共享一个 <code>emptyDir</code>。如果所有容器都能删除任意文件，一个被攻破的 sidecar 就可能破坏构建产物，甚至干扰其他容器的工作。<code>01777</code> 可以提供更接近传统 <code>/tmp</code> 的 sticky bit 语义，但仍应结合用户、组和容器之间的实际协作关系来设计。</p><h3 id="需要较强数据隔离的临时存储"><a href="#需要较强数据隔离的临时存储" class="headerlink" title="需要较强数据隔离的临时存储"></a>需要较强数据隔离的临时存储</h3><p>数据库、编译器或数据处理任务有时会创建只供特定用户和组访问的临时目录。把权限模式写在 Pod 规格中，比让每个镜像自己执行一套不一致的初始化脚本更容易审查，也更适合通过准入策略检查。</p><h2 id="上线前不要直接对全局工作负载放行"><a href="#上线前不要直接对全局工作负载放行" class="headerlink" title="上线前不要直接对全局工作负载放行"></a>上线前不要直接对全局工作负载放行</h2><p>新能力仍在 Alpha 阶段，最稳妥的做法是从一个可回滚的节点池和少量工作负载开始：</p><ol><li><strong>先盘点卷用途</strong>：列出所有 <code>emptyDir</code> 和其他可写卷，区分临时文件、构建产物、上传内容和确实需要执行文件的目录。</li><li><strong>先做兼容测试</strong>：在测试节点上验证二进制是否仍从预期路径运行，尤其关注 JIT、构建系统、脚本解释器和 sidecar。</li><li><strong>再启用特性门控</strong>：让 API Server、kubelet 和运行时配置保持一致，确认节点功能声明已被集群识别。</li><li><strong>检查失败方式</strong>：观察不支持节点上的调度、kubelet 事件和工作负载重启，不要只看 Pod 对象是否被接受。</li><li><strong>建立逐步策略</strong>：先保护上传目录和普通临时目录，再根据应用证据扩大范围；确实需要执行文件的卷不要强行套用 <code>noexec</code>。</li></ol><p>还要把它和其他防护组合起来。只读根文件系统、非 root 用户、seccomp、AppArmor&#x2F;SELinux、最小 capabilities、网络策略和镜像供应链检查，解决的是不同层次的问题。卷挂载选项能减少一条利用路径，却不能替代这些基础控制。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Kubernetes 1.37 的这两项 Alpha 能力，补上了容器卷安全中一个长期不够顺手的缺口：平台终于可以直接声明挂载的执行和设备语义，也可以在创建 <code>emptyDir</code> 时给出明确的 Unix 权限，而不必把安全要求藏在 init container 和节点脚本里。</p><p>对开发者而言，最值得先做的不是马上给所有卷加上 <code>noexec</code>，而是弄清每个可写目录的真实用途。能写不等于能执行，能共享也不等于任何容器都能删除彼此的文件。把这些边界写进 Pod 配置，再用运行时能力、版本偏差和实际测试验证它们，才是这次更新真正适合落地的方式。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://kubernetes.io/blog/2026/09/16/kubernetes-v1-37-hardening-container-storage/">Kubernetes Blog：Kubernetes v1.37—Hardening Container Storage with Bind Mount Options and EmptyDir Permissions</a></li><li><a href="https://kubernetes.io/docs/tasks/configure-pod-container/configure-bind-mount-options/">Kubernetes Docs：Configure bind mount options</a></li><li><a href="https://kubernetes.io/docs/concepts/storage/volumes/#emptydir">Kubernetes Docs：Volumes—emptyDir</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/18/2026-Kubernetes137%E5%AE%B9%E5%99%A8%E5%AD%98%E5%82%A8%E5%AE%89%E5%85%A8/</id>
    <link href="https://code.idea4j.com/2026/09/18/2026-Kubernetes137%E5%AE%B9%E5%99%A8%E5%AD%98%E5%82%A8%E5%AE%89%E5%85%A8/"/>
    <published>2026-09-18T00:01:00.000Z</published>
    <summary>Kubernetes 1.37 引入 VolumeBindMountOptions 和 EmptyDirVolumeMode 两个 Alpha 能力，为卷挂载增加 noexec、nosuid、nodev 和 Unix 权限模式。本文分析它们解决的问题、兼容条件和落地注意事项。</summary>
    <title>Kubernetes 1.37 给容器卷挂载加上 noexec：可写目录终于有了原生安全边界</title>
    <updated>2026-09-18T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="安全" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="GitHub" scheme="https://code.idea4j.com/tags/GitHub/"/>
    <category term="GitHub Enterprise Cloud" scheme="https://code.idea4j.com/tags/GitHub-Enterprise-Cloud/"/>
    <category term="SSO" scheme="https://code.idea4j.com/tags/SSO/"/>
    <category term="SAML" scheme="https://code.idea4j.com/tags/SAML/"/>
    <category term="GitHub App" scheme="https://code.idea4j.com/tags/GitHub-App/"/>
    <category term="凭据管理" scheme="https://code.idea4j.com/tags/%E5%87%AD%E6%8D%AE%E7%AE%A1%E7%90%86/"/>
    <content>
      <![CDATA[<p>GitHub 在 9 月 16 日发布更新：GitHub Enterprise Cloud 的企业管理员现在可以通过企业安装的 GitHub App，为已有的经典 Personal Access Token（PAT）和 SSH Key 批量完成 SSO 授权。一次请求最多覆盖 50 个组织，开发者不必再逐个打开组织页面手动确认。</p><p>这不是“让一个 App 代替用户登录”的宽泛自动化，而是给企业凭据授权流程增加了一条有边界的管理通道。App 只能在企业显式开启委托后调用 API，GitHub 会先检查企业、凭据所有者和目标组织之间的关系，而且请求中使用的是 PAT 的标识或 SSH Key 指纹，不是凭据秘密本身。对平台团队来说，真正值得关注的是：SSO 授权终于可以跟凭据轮换、组织变更和审计流程连在一起。</p><span id="more"></span><h2 id="为什么-SSO-授权会成为自动化的瓶颈"><a href="#为什么-SSO-授权会成为自动化的瓶颈" class="headerlink" title="为什么 SSO 授权会成为自动化的瓶颈"></a>为什么 SSO 授权会成为自动化的瓶颈</h2><p>在启用 SSO 的企业里，凭据通常同时受到两套关系约束：一套是用户或服务账号拥有这个 PAT、SSH Key；另一套是这个凭据是否被允许访问某个组织。凭据本身没有变化，组织范围却可能随着团队拆分、项目迁移和新组织加入而变化。</p><p>过去，成员需要对每个组织分别授权。组织数量一多，手工操作就会带来三个问题：</p><ol><li><strong>轮换难以准时完成</strong>：团队为了避免重复点选，可能拖延 PAT 或 SSH Key 的替换。</li><li><strong>授权状态难以盘点</strong>：平台团队很难用统一方式知道一个自动化凭据已经覆盖哪些组织。</li><li><strong>人员操作无法稳定复现</strong>：离职、值班交接或服务账号没有交互式登录能力时，授权流程容易卡住。</li></ol><p>这也是为什么 GitHub 的公告把“避免为了躲开轮换而使用长生命周期令牌”作为这次更新的背景。自动化的目标不是让凭据活得更久，而是让凭据可以按计划轮换，同时把新凭据的授权动作做成可记录、可重试的流程。</p><h2 id="新能力的工作方式"><a href="#新能力的工作方式" class="headerlink" title="新能力的工作方式"></a>新能力的工作方式</h2><p>GitHub 新增的是企业级凭据授权 API。企业安装的 GitHub App 可以用一个企业安装访问令牌发起请求，指定凭据标识、凭据类型和目标组织列表：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">POST /enterprises/&#123;enterprise&#125;/credential-authorizations</span><br><span class="line"><span class="attribute">Authorization</span><span class="punctuation">: </span>Bearer &lt;ENTERPRISE_INSTALLATION_TOKEN&gt;</span><br><span class="line"><span class="attribute">X-GitHub-Api-Version</span><span class="punctuation">: </span>2026-03-10</span><br><span class="line"><span class="attribute">Content-Type</span><span class="punctuation">: </span>application/json</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">  &quot;credential_id&quot;: 12345678,</span><br><span class="line">  &quot;credential_type&quot;: &quot;classic_pat&quot;,</span><br><span class="line">  &quot;organizations&quot;: [&quot;platform&quot;, &quot;service-a&quot;, &quot;service-b&quot;]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果授权对象是 SSH Key，凭据类型改为 <code>ssh_key</code>，标识使用已经验证的、用户所有的 SSH Key 的 SHA-256 指纹。这里的 <code>credential_id</code> 是 PAT 的编号，不是 PAT 字符串；请求也不应该携带任何令牌秘密。</p><p>GitHub 在处理请求前会检查至少三类关系：</p><ul><li>目标组织确实属于指定企业；</li><li>凭据所有者是每一个目标组织的成员；</li><li>企业使用企业级 SSO，且当前凭据类型满足授权条件。</li></ul><p>已有有效授权的组织会被安全跳过，不需要脚本先把所有状态拉下来再自行去重。这让 API 具备了比较好的幂等性：轮换任务失败后可以重试，而不会因为重复授权把正常状态当成异常。</p><h2 id="权限边界并不在-API-调用本身"><a href="#权限边界并不在-API-调用本身" class="headerlink" title="权限边界并不在 API 调用本身"></a>权限边界并不在 API 调用本身</h2><p>要启用这条路径，企业需要主动打开“Allow GitHub Apps to authorize credentials”设置，并满足一组前置条件：</p><table><thead><tr><th>项目</th><th>要求</th></tr></thead><tbody><tr><td>产品范围</td><td>GitHub Enterprise Cloud</td></tr><tr><td>SSO 形态</td><td>企业级 SSO</td></tr><tr><td>App 归属</td><td>企业或企业内组织拥有的 GitHub App</td></tr><tr><td>App 权限</td><td><code>Enterprise credentials</code> 写权限</td></tr><tr><td>API 身份</td><td>企业安装访问令牌，不是用户令牌或组织安装令牌</td></tr><tr><td>凭据范围</td><td>经典 PAT，或已验证且用户所有的 SSH Key</td></tr></tbody></table><p>这里最容易被忽略的是安装访问令牌。官方文档要求 App 使用企业安装访问令牌调用 API，组织安装令牌、用户访问令牌和个人 PAT 都不支持这条授权操作。安装令牌本身默认只存活一小时，平台服务应当在需要时用 App 的 JWT 和企业安装信息重新生成，而不是把安装令牌当成长效秘密保存。</p><p>另外，<code>Enterprise credentials</code> 写权限不是“只允许给这个 API”的临时开关。它代表 App 可以改变企业凭据的 SSO 授权状态，因此应该单独设置 App、单独保存私钥，并把调用日志与变更工单关联起来。不要把这个权限附加到已经拥有大量仓库或组织管理能力的通用 App 上。</p><h2 id="把它放进凭据轮换流水线"><a href="#把它放进凭据轮换流水线" class="headerlink" title="把它放进凭据轮换流水线"></a>把它放进凭据轮换流水线</h2><p>这项功能最适合服务账号、自动化机器人和跨多个组织运行的构建系统。一个较稳妥的轮换流程可以拆成下面几步：</p><pre class="mermaid">flowchart LR    A[创建新 PAT 或 SSH Key] --> B[保存凭据秘密]    B --> C[记录 token ID 或 SHA-256 指纹]    C --> D[校验组织清单与成员关系]    D --> E[App 批量授权 SSO]    E --> F[执行只读 API 与 Git 拉取测试]    F --> G[切换业务使用的新凭据]    G --> H[撤销旧凭据授权并删除旧秘密]</pre><p>顺序上要先让新凭据可用，再撤销旧凭据。对于 GitHub Actions、构建服务和发布平台，还应该在切换前做一次真实路径验证：用实际运行环境调用 API、读取仓库或完成一次最小化的 Git 操作，而不是仅检查授权接口返回成功。</p><p>如果企业新增了组织，也可以在组织加入企业后触发一次授权同步。同步任务应以企业和组织的当前状态为准，不要只根据上一次运行时保存的列表做追加。组织可能已被移出企业，成员资格也可能发生变化，服务应把授权失败记录为需要人工处理的状态。</p><h2 id="标识符、秘密和审计记录要分开"><a href="#标识符、秘密和审计记录要分开" class="headerlink" title="标识符、秘密和审计记录要分开"></a>标识符、秘密和审计记录要分开</h2><p>这次 API 的一个重要设计是“使用标识符，不传凭据秘密”。但这不代表标识符可以随意暴露。PAT 的 token ID 和 SSH Key 指纹仍然能够帮助定位一个高权限凭据，应该和组织清单、操作者、调用时间以及授权结果一起纳入访问控制。</p><p>建议至少记录以下字段：</p><ul><li>企业、App、安装 ID 和调用者身份；</li><li>PAT 的 token ID 或 SSH Key 的 SHA-256 指纹；</li><li>请求中的目标组织，以及 GitHub 实际授权或跳过的组织；</li><li>API 版本、请求结果、失败原因和重试次数；</li><li>关联的轮换工单、凭据版本和切换时间。</li></ul><p>凭据秘密应该继续存放在专门的秘密管理系统里，不能写入 GitHub App 日志、CI 输出或普通业务数据库。脚本也不应通过“打印当前 token”来确认流程成功，而应使用最小权限的实际调用验证新凭据是否已经能完成目标操作。</p><h2 id="撤销路径要提前设计"><a href="#撤销路径要提前设计" class="headerlink" title="撤销路径要提前设计"></a>撤销路径要提前设计</h2><p>自动授权如果没有对应的撤销路径，就容易变成新的权限积累入口。官方文档提供了撤销企业委托授权的 API；关闭“允许 GitHub Apps 授权凭据”设置，只会阻止新的授权，不会自动撤销已经存在的授权。已经建立的授权仍需显式撤销，或者等凭据被删除、撤销、用户失去组织成员资格时失效。</p><p>因此，平台团队至少应该准备两种撤销动作：按某个 App 的委托记录撤销，以及在安全事件中批量撤销企业范围内的委托授权。批量撤销属于异步操作，系统要记录排队结果并在后台任务结束后复核，而不是把 <code>202 Accepted</code> 直接当成所有授权已经消失。</p><h2 id="这项能力不适合解决什么问题"><a href="#这项能力不适合解决什么问题" class="headerlink" title="这项能力不适合解决什么问题"></a>这项能力不适合解决什么问题</h2><p>它解决的是“同一个凭据需要在多个 SSO 组织获得授权”的流程摩擦，不是凭据治理的全部答案。以下问题仍然需要单独处理：</p><ul><li>新建凭据时如何审批权限范围和过期时间；</li><li>服务账号是否真的需要经典 PAT，能否改用更细粒度的凭据或 GitHub App；</li><li>SSH 私钥如何托管、轮换和在疑似泄露后快速吊销；</li><li>组织成员资格变化后，授权同步如何收敛；</li><li>机器人执行失败时，谁能看到日志并完成复核。</li></ul><p>如果一个任务只需要访问少数仓库，优先选择范围更小、生命周期更短的身份方式。批量授权降低的是操作成本，不应成为扩大长期凭据权限的理由。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>GitHub Enterprise Cloud 这次更新的核心，不是多了一个“批量点确认”的接口，而是把 SSO 凭据授权从人工页面操作推进到了企业级身份自动化。它同时给出了比较清晰的安全边界：企业主动开启、App 使用专门权限、GitHub 校验企业和成员关系、调用只引用凭据标识而不传秘密。</p><p>对平台团队来说，比较合适的落地顺序是先建立凭据清单和轮换流程，再为一个低风险服务账号试点，最后把授权、验证、切换、撤销和审计串成完整闭环。这样，自动化带来的不是更多“默认有效”的权限，而是一条更容易观察、更容易回滚的授权路径。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://github.blog/changelog/2026-09-16-automate-sso-authorization-for-classic-pats-and-ssh-keys">GitHub Changelog：Automate SSO authorization for classic PATs and SSH keys</a></li><li><a href="https://docs.github.com/enterprise-cloud@latest/authentication/authenticating-with-single-sign-on/authorizing-credentials-for-single-sign-on-with-a-github-app">GitHub Docs：Authorizing credentials for single sign-on with a GitHub App</a></li><li><a href="https://docs.github.com/enterprise-cloud@latest/rest/enterprise-admin/credential-authorizations">GitHub Docs：REST API endpoints for enterprise credential authorizations</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/17/2026-GitHub-EnterpriseCloud-SSO%E5%87%AD%E6%8D%AE%E6%89%B9%E9%87%8F%E6%8E%88%E6%9D%83/</id>
    <link href="https://code.idea4j.com/2026/09/17/2026-GitHub-EnterpriseCloud-SSO%E5%87%AD%E6%8D%AE%E6%89%B9%E9%87%8F%E6%8E%88%E6%9D%83/"/>
    <published>2026-09-17T00:01:00.000Z</published>
    <summary>GitHub Enterprise Cloud 新增企业级 API，允许企业安装的 GitHub App 为经典 PAT 和 SSH Key 批量授权 SSO。本文梳理前置条件、权限边界和凭据轮换中的落地方法。</summary>
    <title>GitHub Enterprise Cloud 开放 SSO 凭据批量授权：把手工点选变成可审计的身份自动化</title>
    <updated>2026-09-17T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="安全" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="供应链安全" scheme="https://code.idea4j.com/tags/%E4%BE%9B%E5%BA%94%E9%93%BE%E5%AE%89%E5%85%A8/"/>
    <category term="GitHub" scheme="https://code.idea4j.com/tags/GitHub/"/>
    <category term="SHA-1" scheme="https://code.idea4j.com/tags/SHA-1/"/>
    <category term="HTTPS" scheme="https://code.idea4j.com/tags/HTTPS/"/>
    <category term="TLS" scheme="https://code.idea4j.com/tags/TLS/"/>
    <category term="Git" scheme="https://code.idea4j.com/tags/Git/"/>
    <content>
      <![CDATA[<p>GitHub 在 2026 年 9 月 15 日发布公告，宣布按计划停用 github.com 和合作 CDN 在 HTTPS 中对 SHA-1 的使用，范围包括 GitHub Enterprise Cloud 及其 Data Residency 版本。GitHub Enterprise Server 不受这次变更影响。</p><p>这条消息容易引起一个误解：GitHub 停用的是 HTTPS&#x2F;TLS 连接中的 SHA-1，不是把所有 Git 仓库里的 SHA-1 提交对象一夜之间改成另一种哈希。真正可能出问题的，是旧浏览器、旧 API 客户端、旧 Git、老操作系统组件，或者仍在使用过时 TLS 能力的企业代理。对开发者来说，现在更重要的工作是盘点连接链路，而不是批量重写仓库历史。</p><span id="more"></span><h2 id="这次变更到底影响哪一层"><a href="#这次变更到底影响哪一层" class="headerlink" title="这次变更到底影响哪一层"></a>这次变更到底影响哪一层</h2><p>SHA-1 曾经广泛用于数字签名、证书和其他安全协议。随着碰撞攻击研究不断推进，现代 HTTPS 配置已经逐步减少对它的依赖。GitHub 这次停用的对象，是访问 GitHub 网站、调用 GitHub API，以及通过 HTTPS 推送和拉取代码时所使用的 TLS 能力。</p><p>可以把一次常见的代码拉取拆成几层来看：</p><pre class="mermaid">flowchart LR    A[git clone / API 请求] --> B[Git 或 HTTP 客户端]    B --> C[TLS 握手与证书校验]    C --> D[github.com 或 GitHub CDN]    A -.仓库对象哈希.-> E[提交、树和文件对象]</pre><p>本次停用主要发生在中间的 TLS 连接层。客户端如果只支持旧的 SHA-1 相关算法，可能在建立 HTTPS 连接时失败；客户端即使能够使用 SHA-256 计算 Git 对象，也不代表它的 TLS 库足够新。反过来，一个支持现代 TLS 的客户端，仍然可以操作历史上使用 SHA-1 对象格式的仓库。</p><p>所以，看到“SHA-1 被停用”时，先不要执行下面这些没有针对性的操作：</p><ul><li>不要因为 HTTPS 报错就删除 <code>.git</code> 目录或重新初始化仓库；</li><li>不要把 Git 提交哈希全部改写来解决 TLS 连接问题；</li><li>不要通过关闭证书校验、降低安全级别或跳过代理检查来“修复”问题。</li></ul><p>后两种做法不仅可能无效，还会把连接问题变成更明显的供应链和中间人攻击风险。</p><h2 id="最容易被遗漏的四类连接"><a href="#最容易被遗漏的四类连接" class="headerlink" title="最容易被遗漏的四类连接"></a>最容易被遗漏的四类连接</h2><p>GitHub 的旧公告明确提到，浏览器、使用 GitHub API 的软件，以及通过 HTTPS 通信的 Git 客户端都在影响范围内。实际排查时，不能只在自己的笔记本上打开一次 GitHub 页面就宣布完成。</p><h3 id="1-浏览器和开发者工具"><a href="#1-浏览器和开发者工具" class="headerlink" title="1. 浏览器和开发者工具"></a>1. 浏览器和开发者工具</h3><p>现代浏览器一般已经具备新的 TLS 能力，但长期不升级的操作系统、嵌入式浏览器和远程桌面环境仍可能使用旧组件。GitHub 建议访问 <code>https://github.dev</code> 做兼容性测试，因为该站点已经禁用了 SHA-1。如果浏览器无法建立连接，应优先更新浏览器和操作系统，不要在浏览器里临时放宽安全设置。</p><h3 id="2-Git-客户端与-HTTPS-后端"><a href="#2-Git-客户端与-HTTPS-后端" class="headerlink" title="2. Git 客户端与 HTTPS 后端"></a>2. Git 客户端与 HTTPS 后端</h3><p><code>git clone</code> 看起来只是一个命令，实际会依赖 Git 版本、TLS 后端和操作系统提供的加密库。Linux 上的 Git 可能使用 OpenSSL，其他平台则可能使用不同的系统 TLS 实现。版本过旧时，报错可能只表现为“无法访问远端”或“TLS 握手失败”，不一定直接提到 SHA-1。</p><p>可以先记录客户端和运行环境：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git --version</span><br><span class="line">git config --show-origin --get http.sslBackend</span><br><span class="line">curl -Iv https://github.com</span><br></pre></td></tr></table></figure><p>其中，<code>git --version</code> 只能告诉你 Git 本身的版本，不能完全说明 TLS 后端版本。对 CI Runner、构建镜像和开发容器，也要在对应环境中执行检查，不能只检查宿主机。</p><h3 id="3-API-SDK-和自研集成"><a href="#3-API-SDK-和自研集成" class="headerlink" title="3. API SDK 和自研集成"></a>3. API SDK 和自研集成</h3><p>很多发布脚本、机器人、代码扫描器和内部平台并不直接调用 Git 命令，而是通过某个语言的 HTTP SDK 访问 GitHub API。SDK 版本、运行时和底层证书库中的任何一个过旧，都可能在新连接策略启用后暴露问题。</p><p>排查时应从实际发起请求的进程入手，记录运行时版本、HTTP 库、TLS 后端、代理地址和证书链。只升级业务依赖而忽略基础镜像，或者只升级基础镜像却没有重建锁定的运行时，往往会让问题继续留在生产环境里。</p><h3 id="4-企业代理和-TLS-检查设备"><a href="#4-企业代理和-TLS-检查设备" class="headerlink" title="4. 企业代理和 TLS 检查设备"></a>4. 企业代理和 TLS 检查设备</h3><p>企业网络中的 HTTPS 连接可能经过代理或安全设备。此时，客户端看到的证书和 TLS 行为不一定直接来自 GitHub，代理自身也需要支持现代算法并正确转发连接。个人设备测试正常、CI 或办公网络测试失败，通常值得优先检查这条链路。</p><p>不要把代理证书加入信任库当成通用解决方案。信任企业 CA 与允许过时 TLS 算法是两个不同的问题，应该分别确认代理的协议配置、证书链和升级计划。</p><h2 id="GitHub-Enterprise-Server-为什么不在范围内"><a href="#GitHub-Enterprise-Server-为什么不在范围内" class="headerlink" title="GitHub Enterprise Server 为什么不在范围内"></a>GitHub Enterprise Server 为什么不在范围内</h2><p>9 月 15 日的 GitHub 公告特别说明，GitHub Enterprise Server 不受影响。这并不表示自建实例可以永久保留旧的加密配置，而是说明这次 GitHub.com 及合作 CDN 的集中式变更没有直接修改企业自托管产品。</p><p>如果团队使用的是 Enterprise Server，仍然应该按自己的升级和安全基线检查 TLS 配置。更不能因为现网自建实例暂时可用，就假设访问 GitHub.com 的开发机、构建节点和外部 SaaS 集成也没有问题。两条连接路径可能使用不同的域名、证书、代理和加密库。</p><h2 id="一份更稳妥的迁移清单"><a href="#一份更稳妥的迁移清单" class="headerlink" title="一份更稳妥的迁移清单"></a>一份更稳妥的迁移清单</h2><p>这类基础设施变更最适合按“客户端清单、分层验证、逐步切换”的方式处理：</p><ol><li><strong>先盘点出口</strong>：列出浏览器、开发机、CI Runner、构建镜像、API 服务、发布脚本和企业代理中所有访问 GitHub 的入口。</li><li><strong>再记录版本</strong>：保存 Git、OpenSSL 或系统 TLS、语言运行时、HTTP SDK 和操作系统版本，避免只记录应用版本。</li><li><strong>做真实路径测试</strong>：用实际的 CI 镜像和代理链路访问 <code>github.com</code>、GitHub API，并执行一次只读的 Git 拉取；不要只在本地执行 <code>curl</code>。</li><li><strong>优先升级组件</strong>：升级 Git、运行时、HTTP 依赖、基础镜像和代理设备，随后重新构建并验证锁文件与发布产物。</li><li><strong>保留回滚证据</strong>：记录升级前后的 TLS 日志、请求结果和错误样本，出现问题时可以判断是客户端、代理还是服务端路径。</li></ol><p>如果服务必须在短期内兼容旧系统，应该把兼容范围和退出时间写进维护计划，而不是永久降低 TLS 安全级别。安全迁移最怕“先临时绕过，之后再处理”，因为绕过配置往往比正常升级更难被发现和清理。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>GitHub 停用 HTTPS 中的 SHA-1，是一次面向连接基础设施的兼容性收紧。它提醒开发者，代码托管平台的安全变更不仅影响网页访问，也会沿着 API、Git 客户端、CI 镜像和企业代理一路传导。</p><p>最值得先做的检查很简单：确认真实运行环境里的 Git、TLS 库和代理链路是否足够新，再把它和 Git 对象哈希问题分开处理。只要不把两个层次混在一起，升级路径通常比重写仓库历史更短，也更容易验证。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://github.blog/changelog/2026-09-15-sha-1-in-https-on-github-sunset">GitHub Changelog：SHA-1 in HTTPS on GitHub sunset</a></li><li><a href="https://github.blog/changelog/2026-04-20-sunsetting-sha-1-in-https-on-github/">GitHub Changelog：Sunsetting SHA-1 in HTTPS on GitHub</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/16/2026-GitHub%E5%81%9C%E7%94%A8HTTPS%E4%B8%AD%E7%9A%84SHA1/</id>
    <link href="https://code.idea4j.com/2026/09/16/2026-GitHub%E5%81%9C%E7%94%A8HTTPS%E4%B8%AD%E7%9A%84SHA1/"/>
    <published>2026-09-16T00:01:00.000Z</published>
    <summary>GitHub 已按计划停用 github.com 及相关 CDN 在 HTTPS 中对 SHA-1 的使用。本文解释它影响的连接层、与 Git 对象哈希的区别，以及浏览器、API 客户端和 CI 环境的排查方法。</summary>
    <title>GitHub 停用 HTTPS 中的 SHA-1：开发者先别把它和 Git 提交哈希混为一谈</title>
    <updated>2026-09-16T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="人工智能" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/tags/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="AI Agent" scheme="https://code.idea4j.com/tags/AI-Agent/"/>
    <category term="OpenAI" scheme="https://code.idea4j.com/tags/OpenAI/"/>
    <category term="Perplexity" scheme="https://code.idea4j.com/tags/Perplexity/"/>
    <category term="GPT-6 Astra" scheme="https://code.idea4j.com/tags/GPT-6-Astra/"/>
    <category term="生产系统" scheme="https://code.idea4j.com/tags/%E7%94%9F%E4%BA%A7%E7%B3%BB%E7%BB%9F/"/>
    <content>
      <![CDATA[<p>9 月 14 日，OpenAI 发布了一篇 Perplexity 的案例文章，称 Perplexity 正在使用 GPT-6 Astra 编写沟通内容、修改软件和监控生产系统，而且相比早期模型，工程师不需要频繁介入。这条消息的价值不在于又出现了一个模型名称，而在于它把 AI Agent 的工作范围从“生成一段答案”推进到了持续运行的工程系统。</p><p>这里需要保留一个事实边界：上述信息来自 OpenAI 的案例介绍，不是独立机构发布的横向基准测试，也没有公开足够细节让外部读者复现实验。它更适合被当作一个工程方向的信号来分析。一个 Agent 如果真的能在较少人工检查的情况下修改软件、观察生产状态，就必须同时解决状态保存、工具权限、结果验证、变更回滚和运行审计，而这些问题比模型能否写出代码更难。</p><span id="more"></span><h2 id="从“会回答”到“能把事情做完”"><a href="#从“会回答”到“能把事情做完”" class="headerlink" title="从“会回答”到“能把事情做完”"></a>从“会回答”到“能把事情做完”</h2><p>传统的模型调用通常是一次请求对应一次响应：应用把上下文拼进 Prompt，模型返回文本，程序再决定是否采纳。代码助手虽然可以多轮对话，但开发者仍然是主要的调度者，负责选择文件、执行命令、查看错误并决定下一步。</p><p>端到端 Agent 的工作方式不同。它接收一个目标后，需要自己读取上下文，选择工具，执行动作，观察反馈，再决定是否继续。一个更贴近工程现实的循环大致是：</p><pre class="mermaid">flowchart LR    A[目标与约束] --> B[读取上下文]    B --> C[规划下一步]    C --> D[调用工具或修改系统]    D --> E[获取测试与运行反馈]    E --> F{是否满足验收条件}    F -- 否 --> C    F -- 是 --> G[提交证据并结束]</pre><p>这个循环的重点不在于模型连续调用了多少次，而在于每一轮是否有可信的反馈。编写沟通内容时，反馈可能是格式检查和人工审批；修改软件时，反馈应包括测试、静态检查和代码差异；监控生产系统时，反馈则来自指标、日志、告警和变更记录。没有这些外部信号，所谓“端到端”很容易退化成一段较长的自动生成文本。</p><h2 id="“少检查几次”背后的三个技术条件"><a href="#“少检查几次”背后的三个技术条件" class="headerlink" title="“少检查几次”背后的三个技术条件"></a>“少检查几次”背后的三个技术条件</h2><p>OpenAI 对 Perplexity 案例的概括是，使用 GPT-6 Astra 后，工程师可以比使用早期模型时更少检查 Agent 的进展。这个表述不等于“可以无人值守”，更合理的理解是：Agent 能够带着状态完成更多连续步骤，并在需要时提供可检查的结果。</p><h3 id="1-状态必须是持久的"><a href="#1-状态必须是持久的" class="headerlink" title="1. 状态必须是持久的"></a>1. 状态必须是持久的</h3><p>长任务不能把所有信息都塞进当前上下文。至少要保存任务目标、已完成动作、工具输出、文件差异、失败原因、待办事项和验收证据。这样，Agent 下次恢复时读取的是结构化状态，而不是依赖一段越来越长、越来越难核对的对话记录。</p><p>这也要求任务有明确的阶段和退出条件。例如一次软件变更可以依次经过“理解问题”“修改代码”“运行测试”“生成差异摘要”几个状态。每个阶段都应该有可观察的产物，失败时能够停在某个检查点，而不是从头猜测之前发生了什么。</p><h3 id="2-工具调用要受到业务约束"><a href="#2-工具调用要受到业务约束" class="headerlink" title="2. 工具调用要受到业务约束"></a>2. 工具调用要受到业务约束</h3><p>Agent 的能力来自工具，但风险也从工具开始。读取仓库、修改工作区、执行测试、访问生产指标、发布版本，这些动作的风险等级并不相同，不能都放进一个没有区分的工具箱。</p><p>可以把权限拆成几层：</p><table><thead><tr><th>能力</th><th>适合的默认策略</th><th>需要的证据</th></tr></thead><tbody><tr><td>读取代码、文档和指标</td><td>默认开放给任务范围内的资源</td><td>访问日志、资源范围</td></tr><tr><td>在隔离工作区修改文件</td><td>允许 Agent 生成候选变更</td><td>差异、测试结果</td></tr><tr><td>合并代码或改变线上配置</td><td>需要独立审批或受限服务账号</td><td>审批记录、变更编号</td></tr><tr><td>删除数据、扩大权限或发布高风险版本</td><td>默认禁止，必须人工确认</td><td>风险说明、回滚方案</td></tr></tbody></table><p>“少检查几次”只有在权限被提前收窄时才有意义。否则，减少人工介入只是把更多故障机会交给了一个很难预测的执行循环。</p><h3 id="3-验收标准不能由-Agent-自己定义"><a href="#3-验收标准不能由-Agent-自己定义" class="headerlink" title="3. 验收标准不能由 Agent 自己定义"></a>3. 验收标准不能由 Agent 自己定义</h3><p>Agent 可以生成测试，也可以解释测试结果，但不能单独决定什么叫“已经完成”。确定性的检查应该尽量交给编译器、测试框架、静态分析器、部署系统和监控平台；Agent 负责理解失败信息、调整方案并提交下一次候选变更。</p><p>对于生产系统，最小的验证链路至少应包括：</p><ol><li>修改前记录当前版本和关键指标；</li><li>在隔离环境或测试环境运行可重复的检查；</li><li>通过小流量或有限范围的方式观察变更；</li><li>发现异常时自动停止后续动作，并保留回滚入口；</li><li>把每次工具调用、文件修改和决策结果写入审计日志。</li></ol><p>这套链路的目标不是让 Agent 永远不犯错，而是让错误停留在可发现、可回退的范围内。</p><h2 id="三类工作，不应使用同一种自动化强度"><a href="#三类工作，不应使用同一种自动化强度" class="headerlink" title="三类工作，不应使用同一种自动化强度"></a>三类工作，不应使用同一种自动化强度</h2><p>从 Perplexity 案例提到的三类工作看，编写沟通内容、修改软件和监控生产系统其实有不同的风险结构。</p><p>编写内容通常可以先生成草稿，再由人检查事实、语气和对外承诺。软件修改需要加入代码审查和自动化测试，尤其要防止 Agent 为了让测试通过而改变测试本身。生产监控则应优先从只读开始，把告警解释和排障建议交给 Agent，把重启服务、修改配置或扩大资源等动作放进明确的审批流程。</p><p>因此，团队可以用“观察、建议、执行”三个级别逐步放权：</p><ul><li><strong>观察</strong>：Agent 读取数据，生成摘要和异常线索，不修改系统。</li><li><strong>建议</strong>：Agent 在隔离环境中提出补丁、配置或操作计划，由人确认后执行。</li><li><strong>执行</strong>：只对低风险、可回滚、边界明确的动作开放自动执行，并设置时限和资源上限。</li></ul><p>同一个 Agent 也不必在所有任务中拥有相同权限。权限应该跟着任务类型和当前阶段变化，而不是跟着模型名称永久绑定。</p><h2 id="落地时先测什么"><a href="#落地时先测什么" class="headerlink" title="落地时先测什么"></a>落地时先测什么</h2><p>如果团队准备把类似能力接入内部研发或运维流程，第一步不应是追求最长的无人值守时间，而是建立一组能反映实际风险的指标：</p><ul><li>任务完成率，以及首次完成后仍需要人工返工的比例；</li><li>Agent 修改范围是否超出任务声明的文件和资源；</li><li>测试失败、回滚和人工接管的次数；</li><li>从发现问题到提交可验证产物所需的时间；</li><li>每个任务消耗的模型调用、计算资源和人工检查时间；</li><li>出现异常后，系统从停止到恢复所需的时间。</li></ul><p>这些指标能帮助团队分辨两件容易混在一起的事：Agent 是否真的减少了重复劳动，以及它是否只是把检查和收尾工作推迟到了流程末端。只有在低风险任务上获得稳定结果，才有理由扩大工具范围或延长连续运行时间。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Perplexity 使用 GPT-6 Astra 的案例，展示了 Agent 从“协助完成一步”走向“持续推进一项工作”的可能性。它同时提醒工程团队，模型能力只是这条链路的一部分。持久状态让任务能够恢复，外部验证让结果可以被相信，最小权限和回滚机制则决定错误会停在哪里。</p><p>真正值得借鉴的不是“让 Agent 少被人打断”这一句话，而是为每一个自动动作准备边界、证据和退出路径。只读观察、隔离变更、确定性验证和逐步放权，可能比单纯追求更强的模型更快把端到端 Agent 变成可靠的工程工具。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://openai.com/index/perplexity-improving-accuracy-with-astra">OpenAI：Perplexity trusts GPT-6 Astra with end-to-end systems</a></li><li><a href="https://openai.com/index/gpt-6-astra">OpenAI：GPT-6 Astra—A new generation of intelligence</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/15/2026-OpenAI-Perplexity-GPT6Astra%E7%AB%AF%E5%88%B0%E7%AB%AFAgent/</id>
    <link href="https://code.idea4j.com/2026/09/15/2026-OpenAI-Perplexity-GPT6Astra%E7%AB%AF%E5%88%B0%E7%AB%AFAgent/"/>
    <published>2026-09-15T00:01:00.000Z</published>
    <summary>OpenAI 介绍 Perplexity 使用 GPT-6 Astra 编写沟通内容、修改软件和监控生产系统。本文从工具调用、状态管理、验证与权限边界分析端到端 Agent 的落地条件。</summary>
    <title>Perplexity 把 GPT-6 Astra 用到端到端系统：Agent 进入生产环境后，工程边界如何重画</title>
    <updated>2026-09-15T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="人工智能" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="GitHub Copilot" scheme="https://code.idea4j.com/tags/GitHub-Copilot/"/>
    <category term="AI Agent" scheme="https://code.idea4j.com/tags/AI-Agent/"/>
    <category term="开发工具" scheme="https://code.idea4j.com/tags/%E5%BC%80%E5%8F%91%E5%B7%A5%E5%85%B7/"/>
    <category term="DevOps" scheme="https://code.idea4j.com/tags/DevOps/"/>
    <category term="自动化" scheme="https://code.idea4j.com/tags/%E8%87%AA%E5%8A%A8%E5%8C%96/"/>
    <category term="VS Code" scheme="https://code.idea4j.com/tags/VS-Code/"/>
    <content>
      <![CDATA[<p>VS Code 1.137 带来了一项很容易被低估的能力：Agent Automations 进入 Public Preview。开发者可以把一个带有 Prompt、工作区、模型和权限配置的 Agent 任务保存下来，手动运行，也可以按小时、每天或每周自动运行。</p><p>它适合处理“重复但需要判断”的工作，例如汇总最近的代码变化、整理 Issue、寻找潜在缺陷或检查文档。关键变化不是多了一个定时器，而是定时器会再次启动一个拥有文件读取、命令执行甚至修改权限的 Agent 会话。只要任务进入无人值守状态，Prompt 质量就不再是唯一问题，工作区、权限、运行主机、幂等性和用量预算都需要一起设计。</p><span id="more"></span><h2 id="Agent-Automations-具体能做什么"><a href="#Agent-Automations-具体能做什么" class="headerlink" title="Agent Automations 具体能做什么"></a>Agent Automations 具体能做什么</h2><p>VS Code 官方更新说明把 Automations 定义为重复运行的 Agent 任务。打开 Agents window 后，侧边栏会出现 Automations 入口；可以从模板开始，也可以自己填写任务名称、Prompt 和调度计划。</p><p>目前的调度方式包括：</p><table><thead><tr><th>调度方式</th><th>适合的工作</th></tr></thead><tbody><tr><td>Manual</td><td>先验证 Prompt 和首次运行结果</td></tr><tr><td>Hourly</td><td>需要较短反馈周期的仓库巡检或状态汇总</td></tr><tr><td>Daily</td><td>每日变更摘要、Issue 初筛、文档检查</td></tr><tr><td>Weekly</td><td>周报、依赖观察、长期问题盘点</td></tr><tr><td>On demand</td><td>不改变计划，临时手动触发一次</td></tr></tbody></table><p>这项功能仍在 Preview，并且会逐步向用户开放。需要先启用 <code>chat.automations.enabled</code> 设置，然后在 Agents window 中进入 Automations。预览状态意味着设置入口、运行行为和可用 Agent 可能继续变化，不适合在没有回滚方案的情况下作为关键生产流程的唯一依赖。</p><h2 id="它不是普通的-Cron"><a href="#它不是普通的-Cron" class="headerlink" title="它不是普通的 Cron"></a>它不是普通的 Cron</h2><p>传统 Cron 通常只负责在某个时间启动脚本，脚本的权限和执行环境由服务器固定提供。Agent Automation 的配置对象更丰富：它会保存 Prompt、目标工作区、所选 Agent、模型和 Session configuration，还可能包含权限选项和 Git worktree 设置。</p><p>一次运行大致可以看成下面这条链路：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">调度时间到达</span><br><span class="line">      ↓</span><br><span class="line">加载保存的 Prompt 和 Session configuration</span><br><span class="line">      ↓</span><br><span class="line">连接目标工作区或选择 No workspace</span><br><span class="line">      ↓</span><br><span class="line">启动 Agent 会话</span><br><span class="line">      ↓</span><br><span class="line">按权限读取文件、运行命令或修改内容</span><br><span class="line">      ↓</span><br><span class="line">保存本次运行结果</span><br></pre></td></tr></table></figure><p>VS Code 文档明确提醒，Automation 可以按照所选 Agent 的权限读取文件、运行命令和进行修改。保存 Automation 不会绕过组织策略，也不能保证未来每次运行都不需要审批。换句话说，调度计划只是“何时启动”，不是“自动获得一切权限”。</p><h2 id="运行环境决定它会不会真的按时执行"><a href="#运行环境决定它会不会真的按时执行" class="headerlink" title="运行环境决定它会不会真的按时执行"></a>运行环境决定它会不会真的按时执行</h2><p>本地 Automation 有一个与云端定时任务不同的前提：运行它的机器和 Agent 必须可用。官方文档说明，Agent Host-backed 的计划需要 Agent Host 进程保持运行；其他计划则需要 VS Code 窗口保持运行。关闭应用或让机器睡眠后，不能假设任务仍会像服务器上的 Cron 一样正常执行。</p><p>另外还有几个容易被忽略的调度行为：</p><ul><li>每日和每周计划使用本地时区；</li><li>中断后，错过的计划可能触发一次 catch-up，但不保证每次错过的执行都被补回；</li><li>同一个 Automation 一次只运行一个会话，不会因为计划重叠而并行启动多个相同任务；</li><li>每次执行都会消耗所选 Agent 和模型对应的使用额度。</li></ul><p>因此，开发团队在设计计划时要先回答两个问题：这台机器是否真的会在目标时间在线，以及漏跑一次任务会不会影响业务。如果任务具有生产影响，就应该准备一个有稳定运行时的替代方案，而不是把本地 VS Code 当成高可用调度器。</p><h2 id="第一次不要直接设置成每天运行"><a href="#第一次不要直接设置成每天运行" class="headerlink" title="第一次不要直接设置成每天运行"></a>第一次不要直接设置成每天运行</h2><p>官方文档建议先把 Schedule 设为 Manual，检查首次运行结果后，再改成 Hourly、Daily 或 Weekly。这个顺序非常重要，因为 Agent 的错误往往不是“任务没启动”，而是任务启动后理解错范围、调用了不合适的命令，或者生成了没人会消费的输出。</p><p>可以先用一个只读、范围明确的任务做验证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">汇总当前仓库最近 24 小时的提交和 Pull Request。</span><br><span class="line">只读取 Git 历史、Pull Request 元数据和已有文档，不修改文件、不运行发布命令。</span><br><span class="line">输出：变更摘要、可能需要人工关注的问题、信息来源和未确定事项。</span><br><span class="line">如果没有足够信息，明确写出“无法判断”，不要猜测。</span><br></pre></td></tr></table></figure><p>首次运行重点检查四件事：</p><ol><li>Agent 是否打开了预期的工作区。</li><li>读取范围是否超出了任务需要。</li><li>输出是否带有可核对的提交、Issue 或文件引用。</li><li>任务失败、无数据和权限不足时，结果是否清楚表达了状态。</li></ol><p>只有当这四点都稳定后，才适合加入周期计划。</p><h2 id="Prompt-要写成可重复执行的任务"><a href="#Prompt-要写成可重复执行的任务" class="headerlink" title="Prompt 要写成可重复执行的任务"></a>Prompt 要写成可重复执行的任务</h2><p>一次性对话可以依赖上下文，定时 Automation 则不能假设上一次会话的记忆还在。Prompt 最好明确写出输入范围、输出格式、禁止操作和失败处理。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">任务：生成过去 24 小时的仓库变更摘要。</span><br><span class="line"></span><br><span class="line">范围：当前工作区的 Git 提交、Pull Request 和 Issue。</span><br><span class="line">输出：</span><br><span class="line">1. 变更摘要，按主题分组；</span><br><span class="line">2. 需要人工确认的风险；</span><br><span class="line">3. 引用的提交或链接；</span><br><span class="line">4. 没有数据或无法判断的项目。</span><br><span class="line"></span><br><span class="line">约束：只读，不修改文件，不安装依赖，不推送代码，不访问生产凭据。</span><br><span class="line">重复运行：只覆盖本次报告，不修改历史报告。</span><br></pre></td></tr></table></figure><p>其中“重复运行”尤其重要。周期任务可能因为手动触发、catch-up 或重复计划而多次运行。输出文件应使用稳定的日期键或写入独立目录，避免每次执行都追加一份重复内容；如果任务会写文件，最好先读取当前状态，再根据差异决定是否修改。</p><h2 id="工作区、权限和隔离怎么选"><a href="#工作区、权限和隔离怎么选" class="headerlink" title="工作区、权限和隔离怎么选"></a>工作区、权限和隔离怎么选</h2><p>VS Code Automations 创建表单允许选择目标工作区，也允许选择 No workspace。一个实用的选择规则是：</p><table><thead><tr><th>任务类型</th><th>建议配置</th></tr></thead><tbody><tr><td>只做资料汇总或一般问答</td><td>No workspace，或只读工作区</td></tr><tr><td>读取仓库并生成报告</td><td>指定工作区，限制写入权限</td></tr><tr><td>自动修复代码</td><td>使用独立 Git worktree，要求测试和人工审查</td></tr><tr><td>需要发布或改动线上资源</td><td>不要直接交给无人值守 Automation</td></tr></tbody></table><p>如果所选 Agent 支持，Git 工作区可以选择 New Worktree 和基准分支。这样任务产生的修改不会直接落在开发者当前分支上，尤其适合自动修复、批量重构和文档更新。</p><p>“有独立 worktree”不代表风险自动消失。Agent 仍可能运行命令、读取环境变量或访问网络，因此要同时收紧权限级别，并检查组织策略和审批规则。对于包含密钥、客户数据或内部配置的仓库，不要因为任务只是“整理 Issue”就默认开放完整文件和网络访问。</p><h2 id="适合优先自动化的三类工作"><a href="#适合优先自动化的三类工作" class="headerlink" title="适合优先自动化的三类工作"></a>适合优先自动化的三类工作</h2><h3 id="变更摘要和交接信息"><a href="#变更摘要和交接信息" class="headerlink" title="变更摘要和交接信息"></a>变更摘要和交接信息</h3><p>每天汇总过去一段时间的提交、Pull Request 和 Issue，输出面向开发者的变更摘要。这类任务结果容易人工核对，失败时也不会直接改变系统状态。</p><h3 id="Issue-初筛"><a href="#Issue-初筛" class="headerlink" title="Issue 初筛"></a>Issue 初筛</h3><p>让 Agent 按既定标签和模板检查新 Issue，提取复现步骤、影响范围和缺失信息。自动化只负责整理和提出问题，不要在没有人工确认时自动关闭 Issue 或修改优先级。</p><h3 id="文档和工程卫生检查"><a href="#文档和工程卫生检查" class="headerlink" title="文档和工程卫生检查"></a>文档和工程卫生检查</h3><p>周期检查过期链接、缺失变更记录、重复配置或明显的文档漂移。第一阶段只生成报告，确认误报率后，再考虑让 Agent 在 worktree 中提交修改。</p><p>相反，自动发布、删除数据、修改生产配置、轮换凭据和直接合并代码，都不适合一开始交给本地预览功能承担。它们需要更明确的身份、审批、审计和恢复机制。</p><h2 id="如何验收一个-Automation"><a href="#如何验收一个-Automation" class="headerlink" title="如何验收一个 Automation"></a>如何验收一个 Automation</h2><p>可以用一周的试运行建立最小验收表：</p><ul><li>计划是否在预期时区触发；</li><li>机器休眠或 VS Code 关闭后，任务如何表现；</li><li>运行失败时是否产生明确的错误记录；</li><li>输出是否重复、遗漏或引用过期信息；</li><li>Agent 是否执行了 Prompt 之外的命令；</li><li>任务每次运行的模型、时间和使用量是否可追踪；</li><li>产生代码修改时，是否留在 worktree 而不是当前工作分支。</li></ul><p>这些问题比“Prompt 写得像不像人话”更能决定周期任务是否值得长期保留。对于真正重要的任务，还应把结果发送到已有的监控、Issue 或审批流程，而不是只留在某个本地 VS Code 窗口里。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>VS Code 1.137 的 Agent Automations 让开发者可以把一些重复的工程工作交给周期运行的 Agent，小时、天、周三种调度粒度覆盖了从频繁巡检到周报整理的常见场景。它最有价值的地方，是把 Prompt、工作区、模型和权限配置放在同一个可重复运行的任务里。</p><p>但它仍然是 Preview 功能，也依赖本地运行环境。更重要的是，每次自动运行都可能拥有真实的文件和命令权限。先用 Manual 验证，再使用最小权限、独立 worktree 和明确的输出约束，最后才设置周期计划，才能让 Agent Automations 成为工程流程的一部分，而不是一个会在夜里悄悄改文件的黑盒定时器。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://github.blog/changelog/2026-09-10-github-copilot-weekly-releases-september-7">GitHub Copilot weekly releases — September 7 - GitHub Changelog</a></li><li><a href="https://code.visualstudio.com/updates/v1_137">VS Code 1.137 release notes</a></li><li><a href="https://code.visualstudio.com/docs/agents/run/automations">Automations - Visual Studio Code Docs</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/14/2026-VSCode137Agent%E8%87%AA%E5%8A%A8%E5%8C%96%E4%BB%BB%E5%8A%A1/</id>
    <link href="https://code.idea4j.com/2026/09/14/2026-VSCode137Agent%E8%87%AA%E5%8A%A8%E5%8C%96%E4%BB%BB%E5%8A%A1/"/>
    <published>2026-09-14T00:01:00.000Z</published>
    <summary>VS Code 1.137 将 Agent Automations 带入 Public Preview，可以按小时、天或周运行保存的 Agent 任务。本文梳理它的调度模型、运行条件、权限风险和适合落地的工程实践。</summary>
    <title>VS Code 1.137 的 Agent Automations：定时任务进入预览，权限边界要先于 Prompt</title>
    <updated>2026-09-14T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="人工智能" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="GitHub Copilot" scheme="https://code.idea4j.com/tags/GitHub-Copilot/"/>
    <category term="AI Agent" scheme="https://code.idea4j.com/tags/AI-Agent/"/>
    <category term="DevOps" scheme="https://code.idea4j.com/tags/DevOps/"/>
    <category term="GitHub" scheme="https://code.idea4j.com/tags/GitHub/"/>
    <category term="Pull Request" scheme="https://code.idea4j.com/tags/Pull-Request/"/>
    <category term="Code Review" scheme="https://code.idea4j.com/tags/Code-Review/"/>
    <content>
      <![CDATA[<p>GitHub 在 2026 年 9 月 11 日更新了 Copilot Code Review。现在，当后续提交真正处理了 Copilot 之前指出的问题时，相关评论可以自动解决；应用 Copilot Autofix 建议时，系统会生成更贴合修改内容的提交信息。与此同时，审查 Agent 获得了 Copilot SDK 中更完整的 Shell 工具，并在 Lite 工作量级别使用多 Agent 协作完成审查。</p><p>这几项变化把 Copilot Code Review 往“能验证、能收尾的审查流程”推进了一步。它不再只是给出一串静态建议，而是可以尝试运行构建、测试或针对性脚本，再把已经处理的评论从当前待办中移开。不过，自动解决评论不等于自动证明代码正确，多 Agent 也不等于可以取消人工审查。真正需要调整的是团队如何看待评论状态、验证证据和 Agent 的执行边界。</p><span id="more"></span><h2 id="先看这次更新了什么"><a href="#先看这次更新了什么" class="headerlink" title="先看这次更新了什么"></a>先看这次更新了什么</h2><table><thead><tr><th>更新</th><th>直接影响</th></tr></thead><tbody><tr><td>评论自动解决</td><td>后续提交处理了底层问题后，Copilot 评论可以自动标记为已解决</td></tr><tr><td>Autofix 智能提交信息</td><td>应用 Copilot 修复建议时，提交信息会根据修改内容生成</td></tr><tr><td>更完整的 Shell 工具</td><td>审查 Agent 可以尝试运行构建、测试和针对性脚本来验证代码</td></tr><tr><td>Lite 模式多 Agent</td><td>Lite 审查由多个 Agent 分别分析，再合并成一份审查结果</td></tr></tbody></table><p>这些能力属于 Copilot Code Review 的工作流更新，不能简单理解成一次模型版本替换。前两项改变评论和提交的交互方式，后两项改变审查 Agent 获取证据和组织分析的方式。</p><h2 id="自动解决评论，解决的是待办噪声"><a href="#自动解决评论，解决的是待办噪声" class="headerlink" title="自动解决评论，解决的是待办噪声"></a>自动解决评论，解决的是待办噪声</h2><p>以往的代码审查流程里，一个评论是否已经处理，通常需要作者修改代码后手动点击 Resolve，或者由审查者再次确认。Copilot 现在会观察后续提交，当它判断相关提交已经处理了此前评论指出的底层问题时，自动解决这条评论。</p><p>这对频繁迭代的 Pull Request 很有帮助。作者可能连续推送多个小提交，审查者不必在每次更新后重新清理已经失效的评论，页面也更容易聚焦在尚未处理的问题上。</p><p>但评论状态只是流程状态，不是正确性证明。自动解决并不意味着：</p><ul><li>修改一定覆盖了所有边界条件；</li><li>测试一定覆盖了评论涉及的路径；</li><li>审查者一定同意这条问题已经关闭；</li><li>后续提交没有引入新的相关问题。</li></ul><p>更稳妥的做法，是把“自动解决”当成待办整理，把“是否接受修复”留给代码差异、测试结果和人工判断。涉及安全、数据一致性或权限边界的评论，仍应保留人工复核，即使页面上已经显示为 resolved。</p><h2 id="Autofix-的提交信息也变得更具体"><a href="#Autofix-的提交信息也变得更具体" class="headerlink" title="Autofix 的提交信息也变得更具体"></a>Autofix 的提交信息也变得更具体</h2><p>当开发者应用 Copilot Autofix 建议时，Copilot 会为这次修改生成更贴合内容的提交信息。它的价值比较朴素，却很实用：如果一次 Pull Request 里连续应用了多个修复建议，提交历史不再全部停留在“fix review comment”这类模糊描述上。</p><p>提交信息最终仍然属于项目历史的一部分，团队不应该把生成结果当成无需检查的元数据。建议保留两步：先查看 Copilot 实际修改的文件和范围，再确认提交信息是否准确表达了原因和影响。如果团队要求 Conventional Commits、变更单号或安全修复标识，提交信息模板仍应由仓库规范约束。</p><p>可以把这项能力放在一个小范围流程里：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Copilot 提出修复建议</span><br><span class="line">        ↓</span><br><span class="line">作者查看 diff 和测试结果</span><br><span class="line">        ↓</span><br><span class="line">应用 Autofix</span><br><span class="line">        ↓</span><br><span class="line">检查生成的提交信息</span><br><span class="line">        ↓</span><br><span class="line">进入正常代码审查</span><br></pre></td></tr></table></figure><p>这样既保留了自动化带来的便利，也不会让提交历史失去可追溯性。</p><h2 id="Shell-工具让审查从“看代码”走向“找证据”"><a href="#Shell-工具让审查从“看代码”走向“找证据”" class="headerlink" title="Shell 工具让审查从“看代码”走向“找证据”"></a>Shell 工具让审查从“看代码”走向“找证据”</h2><p>GitHub 表示，Copilot Code Review 现在可以使用 Copilot SDK 提供的完整 Shell 工具集，并运行在 Copilot Agent Firewall 后面。审查 Agent 因此有更多办法验证代码，例如运行构建命令、执行测试、运行针对性脚本，以及从可用工具和 API 获取信息。</p><p>这改变了代码审查的一个基本限制。只阅读 diff 时，Agent 只能根据静态上下文推断修改是否可靠；能够运行验证命令后，它至少有机会得到真实的编译错误、测试失败和工具输出。</p><p>不过，“能运行命令”不等于“已经完成可靠验证”。验证结果还取决于几个条件：</p><ol><li>项目是否能在隔离环境中稳定构建，依赖是否可以获取。</li><li>测试是否足够覆盖本次修改，而不是只跑到一组很快但无关的单元测试。</li><li>脚本是否依赖本地密钥、生产服务或不可重复的外部状态。</li><li>工具调用是否有明确的超时、网络和文件系统边界。</li><li>审查结果是否记录了运行过的命令和实际输出。</li></ol><p>Agent Firewall 提供了重要的执行边界，但平台团队仍应检查它允许哪些工具、是否允许联网、命令失败如何呈现，以及构建过程中产生的文件和凭据如何清理。对于需要访问内部 API 的项目，更要先确认数据暴露范围，再考虑让审查流程使用这些工具。</p><h2 id="Lite-模式为什么要用多个-Agent"><a href="#Lite-模式为什么要用多个-Agent" class="headerlink" title="Lite 模式为什么要用多个 Agent"></a>Lite 模式为什么要用多个 Agent</h2><p>这次更新还改变了 Lite effort level 的分析方式。GitHub 表示，Lite 审查现在由多个 Agent 分别提供视角，再把发现合并成一份结果，而不是只让一个 Agent 从头分析到尾。</p><p>多 Agent 的价值在于分散注意力。一个 Agent 可能主要关注错误处理，另一个可能更容易发现权限、输入校验或测试缺口，合并结果后，审查覆盖面有机会变大。GitHub 还表示，这种方式在相同甚至更低成本下，可以让 Lite 审查更全面、更准确。</p><p>这里的关键字是“有机会”。多 Agent 也会带来新的工程问题：不同 Agent 可能重复报告同一个问题，判断标准可能不一致，合并器需要去重和排序，审查者还要知道一条结论到底来自多少独立分析。最终结果仍然需要用测试和人工复核校验。</p><p>如果团队要评估这次更新，建议记录至少三类数据：</p><ul><li>新增发现数量，以及去重后的有效发现数量；</li><li>被人工标记为误报、重复或无须修改的评论数量；</li><li>审查耗时、修复耗时和后续回退或重新打开的评论数量。</li></ul><p>只看“每个 Pull Request 产生了多少评论”没有意义。一个更严格的 Lite 审查可能先报出更多候选问题，但如果有效发现率和修复后的回归率更好，整体流程反而可能更省时间。</p><h2 id="如何把它接入现有-Pull-Request-流程"><a href="#如何把它接入现有-Pull-Request-流程" class="headerlink" title="如何把它接入现有 Pull Request 流程"></a>如何把它接入现有 Pull Request 流程</h2><p>不建议一开始就把 Copilot 的自动解决或 Agent 验证结果设置成强制合并条件。更合适的接入顺序是先观察，再收紧边界。</p><h3 id="第一步，保留人工基线"><a href="#第一步，保留人工基线" class="headerlink" title="第一步，保留人工基线"></a>第一步，保留人工基线</h3><p>选取一组类型稳定的仓库，记录人工审查周期、评论数量、修复时间和测试失败情况。基线不需要复杂，但必须能和启用后的同类 Pull Request 做比较。</p><h3 id="第二步，验证命令和工具边界"><a href="#第二步，验证命令和工具边界" class="headerlink" title="第二步，验证命令和工具边界"></a>第二步，验证命令和工具边界</h3><p>确认常用构建和测试命令在审查环境里能重复运行。对需要真实凭据、生产数据或写入外部系统的脚本，先改成模拟服务或只读模式。审查 Agent 的工具权限应小于部署 Agent，不能因为想获得更多上下文就开放无关的写权限。</p><h3 id="第三步，区分评论状态和修复结论"><a href="#第三步，区分评论状态和修复结论" class="headerlink" title="第三步，区分评论状态和修复结论"></a>第三步，区分评论状态和修复结论</h3><p>把自动解决看作评论清理信号，把测试结果和代码差异作为修复依据。安全告警、数据迁移和认证授权相关评论，即使自动解决，也应该进入人工复核列表。</p><h3 id="第四步，观察-Lite-多-Agent-的真实收益"><a href="#第四步，观察-Lite-多-Agent-的真实收益" class="headerlink" title="第四步，观察 Lite 多 Agent 的真实收益"></a>第四步，观察 Lite 多 Agent 的真实收益</h3><p>比较启用前后的有效发现率、重复评论、误报和修复回退，而不是只比较评论总量。如果结果只是评论变多，开发者却花更多时间筛选，团队就需要调整审查范围或提示策略。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Copilot Code Review 的这次更新，分别从评论生命周期、提交历史、验证工具和分析组织方式补上了几个实际工作流中的空缺。自动解决能减少重复待办，Autofix 提交信息能改善记录，Shell 工具让审查有机会运行真实验证，Lite 多 Agent 则试图在较低成本下扩大分析视角。</p><p>这些能力的共同前提是证据边界要清楚。评论被自动解决，只说明 Copilot 认为底层问题已经处理；命令运行成功，只说明这组命令在当前环境中通过；多 Agent 得到相同结论，也不等于结论天然正确。把这些结果放回测试、代码差异和人工审查的闭环里，自动化才会减少噪声，而不是增加另一层需要盯着的提示。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://github.blog/changelog/2026-09-11-auto-resolution-and-analysis-updates-in-copilot-code-review">Auto-resolution and analysis updates in Copilot code review - GitHub Changelog</a></li><li><a href="https://docs.github.com/en/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review">Use code review with GitHub Copilot - GitHub Docs</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/13/2026-GitHub-Copilot%E4%BB%A3%E7%A0%81%E5%AE%A1%E6%9F%A5%E8%87%AA%E5%8A%A8%E8%A7%A3%E5%86%B3%E4%B8%8E%E5%A4%9AAgent%E5%88%86%E6%9E%90/</id>
    <link href="https://code.idea4j.com/2026/09/13/2026-GitHub-Copilot%E4%BB%A3%E7%A0%81%E5%AE%A1%E6%9F%A5%E8%87%AA%E5%8A%A8%E8%A7%A3%E5%86%B3%E4%B8%8E%E5%A4%9AAgent%E5%88%86%E6%9E%90/"/>
    <published>2026-09-13T00:01:00.000Z</published>
    <summary>GitHub Copilot Code Review 新增评论自动解决、Autofix 智能提交信息、Shell 工具验证和 Lite 模式多 Agent 分析。本文梳理这些变化对代码审查流程和安全边界的影响。</summary>
    <title>GitHub Copilot Code Review 更新：评论自动解决与 Lite 模式多 Agent 分析</title>
    <updated>2026-09-13T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="人工智能" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="GitHub Copilot" scheme="https://code.idea4j.com/tags/GitHub-Copilot/"/>
    <category term="AI Agent" scheme="https://code.idea4j.com/tags/AI-Agent/"/>
    <category term="DevOps" scheme="https://code.idea4j.com/tags/DevOps/"/>
    <category term="GitHub" scheme="https://code.idea4j.com/tags/GitHub/"/>
    <category term="API" scheme="https://code.idea4j.com/tags/API/"/>
    <category term="VS Code" scheme="https://code.idea4j.com/tags/VS-Code/"/>
    <content>
      <![CDATA[<p>GitHub 在 2026 年 9 月 11 日宣布，Copilot 使用指标报告正式纳入 VS Code Agents 窗口的活动数据。企业和组织现在可以在 1 天和 28 天报告中看到专用 Agents 窗口的活跃用户、会话数和用户消息数，也可以在用户级报告中判断某个用户是否使用过这个窗口。</p><p>这项更新对平台团队的意义，不是多了几列报表，而是终于能把“大家是否在用 Agent”拆成更具体的行为数据。不过，VS Code Agents 只是一个明确的产品入口，不能代表用户所有的 Copilot Agent 活动，更不能直接等同于代码质量或开发效率。要让这些指标真正有用，先要弄清楚它统计了什么，再决定拿它回答什么问题。</p><span id="more"></span><h2 id="这次新增了哪些字段"><a href="#这次新增了哪些字段" class="headerlink" title="这次新增了哪些字段"></a>这次新增了哪些字段</h2><p>新增指标同时出现在企业级和组织级报告中，并覆盖 1 天和 28 天两个时间窗口。用户级报告也会提供对应的使用标记和计数。</p><table><thead><tr><th>报告层级</th><th>字段</th><th>含义</th></tr></thead><tbody><tr><td>企业或组织汇总</td><td><code>daily_active_vscode_agent_users</code></td><td>每天使用专用 VS Code Agents 窗口的去重用户数，可选字段</td></tr><tr><td>企业或组织汇总</td><td><code>totals_by_vscode_agent</code></td><td>按 VS Code Agents 汇总的 <code>session_count</code> 和 <code>total_user_messages</code></td></tr><tr><td>用户级报告</td><td><code>used_vscode_agent</code></td><td>用户是否使用过专用 VS Code Agents 窗口，可选指示字段</td></tr><tr><td>用户级报告</td><td><code>totals_by_vscode_agent</code></td><td>单个用户的会话数和用户消息数，可选字段</td></tr></tbody></table><p>这里有两个关键词值得留意。第一，字段是可选的；如果对应数据暂时不可用，字段可能缺失或为 <code>null</code>。第二，统计对象是专用的 VS Code Agents 窗口，不包含编辑器窗口里的 Agent Mode，也不自动并入通用 Copilot 使用汇总。</p><p>因此，历史报表解析器不能假设这些字段永远存在，也不能把空值当成“用户没有使用”。更稳妥的做法是区分三种状态：字段存在且有数值、字段存在但为 <code>null</code>、字段完全不存在。它们可能分别代表有数据、当前无法提供数据，以及旧报告或不适用的报告版本。</p><h2 id="统计入口在哪里"><a href="#统计入口在哪里" class="headerlink" title="统计入口在哪里"></a>统计入口在哪里</h2><p>GitHub Copilot 使用指标 API 返回的是报告下载链接和报告覆盖的时间范围。企业级报告的入口包括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">GET /enterprises/&#123;enterprise&#125;/copilot/metrics/reports/enterprise-1-day?day=YYYY-MM-DD</span><br><span class="line">GET /enterprises/&#123;enterprise&#125;/copilot/metrics/reports/enterprise-28-day/latest</span><br></pre></td></tr></table></figure><p>第一类接口用于取某一天的企业报告，第二类接口用于取最近一个完整 28 天周期的报告。组织级报告和用户级报告也在同一组 Copilot usage metrics API 文档中提供，具体路径和权限应以当前文档为准。</p><p>这类接口的使用方式和普通实时监控 API 不一样。自动化程序拿到报告链接后，还需要下载并解析报告文件，再把数据写入内部的数据仓库或指标系统。建议给下载、解析和入库分别记录状态，避免“接口调用成功”被误认为“数据已经成功更新”。</p><h2 id="权限和策略是前置条件"><a href="#权限和策略是前置条件" class="headerlink" title="权限和策略是前置条件"></a>权限和策略是前置条件</h2><p>企业报告不是所有 Copilot 用户都能读取。GitHub 文档列出的企业侧访问者包括企业所有者、计费管理者，以及拥有细粒度 <code>View Enterprise Copilot Metrics</code> 权限的授权用户。组织侧则面向组织所有者和拥有 <code>View Copilot Metrics</code> 权限的自定义组织或企业角色。</p><p>企业级 API 还需要打开 Copilot usage metrics 策略。GitHub 文档要求企业把这项策略设置为 <code>Enabled everywhere</code>，否则相关接口不会按预期提供报告。</p><p>在权限设计上，可以把读取报告的服务单独放在平台数据任务中：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">GitHub API 凭据</span><br><span class="line">        ↓ 仅获取使用指标报告</span><br><span class="line">报告下载与校验</span><br><span class="line">        ↓</span><br><span class="line">数据仓库 / 仪表盘</span><br><span class="line">        ↓</span><br><span class="line">团队级汇总和治理分析</span><br></pre></td></tr></table></figure><p>不要把企业级指标令牌直接放进普通构建任务，也不要把用户级报告无条件暴露给所有项目负责人。用户级数据涉及员工使用行为，应该先确认组织的隐私政策、访问审批、保留期限和展示粒度，再决定是否进入团队仪表盘。</p><h2 id="如何读懂-Agent-活跃度"><a href="#如何读懂-Agent-活跃度" class="headerlink" title="如何读懂 Agent 活跃度"></a>如何读懂 Agent 活跃度</h2><p>单看 <code>daily_active_vscode_agent_users</code>，只能回答“有多少人打开并使用过专用 Agents 窗口”。它适合观察入口采用率，不适合单独证明 Agent 帮助开发者写出了更好的代码。</p><p>更有意义的分析可以分成三层：</p><h3 id="入口采用"><a href="#入口采用" class="headerlink" title="入口采用"></a>入口采用</h3><p>把活跃用户数和具备 Copilot 许可、使用 VS Code 的目标人群放在一起比较，得到专用 Agents 窗口的触达比例。如果只看活跃用户的绝对值，很容易把团队规模增长误判成产品采用率提升。</p><h3 id="使用深度"><a href="#使用深度" class="headerlink" title="使用深度"></a>使用深度</h3><p>再观察 <code>session_count</code> 和 <code>total_user_messages</code>。会话数增加，可能说明用户反复使用 Agent；消息数增加，可能说明任务更复杂，也可能只是用户在反复追问。两者都需要和任务类型、仓库范围及时间窗口一起解释。</p><h3 id="工程结果"><a href="#工程结果" class="headerlink" title="工程结果"></a>工程结果</h3><p>最后把使用数据和代码审查周期、测试通过率、返工次数、缺陷修复时间等工程指标放在同一张分析表里。这里的重点是寻找相关性和变化趋势，不是把某个团队的消息数直接换算成“生产力分数”。</p><p>一个简单的看板可以同时显示：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">目标用户规模 → Agents 活跃用户 → Agents 会话数 → 工程结果变化</span><br></pre></td></tr></table></figure><p>这条链路中每一层都可能掉下去。有人拥有许可但没有使用，有人使用了 Agent 但没有形成稳定的开发习惯，也有人使用频繁却没有改善交付结果。把它们拆开，平台团队才知道应该优化培训、产品入口，还是代码审查和测试流程。</p><h2 id="1-天报告和-28-天报告应该怎么配合"><a href="#1-天报告和-28-天报告应该怎么配合" class="headerlink" title="1 天报告和 28 天报告应该怎么配合"></a>1 天报告和 28 天报告应该怎么配合</h2><p>1 天报告适合看发布、培训或策略调整后的短期变化，例如某个团队在启用 VS Code Agents 后的第一周是否开始使用。它的波动会比较大，不适合直接拿来做团队间排名。</p><p>28 天报告适合看一段完整周期内的采用趋势，减少单日请假、迭代节奏和节假日带来的噪声。两类报告应该使用相同的字段口径和过滤规则，否则今天看到的增长可能只是统计周期变化。</p><p>建议在数据仓库中保留这几个维度：</p><ul><li>报告类型和覆盖日期；</li><li>企业、组织和团队标识；</li><li>VS Code Agents 的专用字段；</li><li>字段缺失或为 <code>null</code> 的原始状态；</li><li>报告下载时间、解析器版本和数据校验结果。</li></ul><p>特别是解析器版本。GitHub 已经声明这些字段是可选的，未来还可能增加或调整报告内容。如果只把最终数值写进数据库，不保留原始文件和解析版本，后面很难解释历史图表为什么发生变化。</p><h2 id="这组数据不能说明什么"><a href="#这组数据不能说明什么" class="headerlink" title="这组数据不能说明什么"></a>这组数据不能说明什么</h2><p>这次更新很容易被包装成“Agent 生产力仪表盘”，但官方公告本身只承诺活动数据。它不能直接说明：</p><ul><li>生成的代码是否通过了安全审查；</li><li>一个会话节省了多少时间；</li><li>消息越多是否代表任务越复杂；</li><li>没有使用 VS Code Agents 的用户是否使用了其他 Copilot 能力；</li><li>团队之间谁的开发效率更高。</li></ul><p>尤其要避免把用户级报告用于简单的个人排名。使用频率会受到岗位、项目类型、IDE 习惯和数据敏感性影响。更适合的用途，是发现采用障碍、评估培训效果、观察团队级流程变化，并把结论交给开发者和负责人共同复核。</p><h2 id="给平台团队的落地清单"><a href="#给平台团队的落地清单" class="headerlink" title="给平台团队的落地清单"></a>给平台团队的落地清单</h2><h3 id="先让数据管道对可选字段保持兼容"><a href="#先让数据管道对可选字段保持兼容" class="headerlink" title="先让数据管道对可选字段保持兼容"></a>先让数据管道对可选字段保持兼容</h3><p>将字段定义为可选，缺失和 <code>null</code> 原样保留，解析失败则进入重试队列。不要为了让下游图表有值而把未知状态填成零。</p><h3 id="把专用窗口和其他-Agent-入口分开"><a href="#把专用窗口和其他-Agent-入口分开" class="headerlink" title="把专用窗口和其他 Agent 入口分开"></a>把专用窗口和其他 Agent 入口分开</h3><p>数据模型中保留 <code>vscode_agents</code> 的独立维度，不要把它和编辑器窗口 Agent Mode 或通用 Copilot 汇总混成一个“Agent 使用量”。口径越清楚，后续比较越可靠。</p><h3 id="先观察趋势，再决定是否做干预"><a href="#先观察趋势，再决定是否做干预" class="headerlink" title="先观察趋势，再决定是否做干预"></a>先观察趋势，再决定是否做干预</h3><p>用 1 天报告定位异常，用 28 天报告确认趋势。培训、模板和内部推广应该有明确的前后对照周期，并同时检查代码审查和测试结果有没有变化。</p><h3 id="给用户级数据设置访问边界"><a href="#给用户级数据设置访问边界" class="headerlink" title="给用户级数据设置访问边界"></a>给用户级数据设置访问边界</h3><p>用户级指标应当最小化展示，优先使用团队汇总和匿名化趋势。需要下钻时，保留审批记录、访问日志和数据保留期限。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>GitHub Copilot 使用指标新增 VS Code Agents 数据，让企业第一次可以更细地观察专用 Agent 入口的采用和使用深度。<code>daily_active_vscode_agent_users</code> 适合看入口采用，<code>session_count</code> 和 <code>total_user_messages</code> 可以帮助理解使用强度，用户级字段则适合做有边界的分群分析。</p><p>真正值得做的工作，是把这些字段接入一条可靠的数据管道，再与代码审查、测试和交付结果放在一起观察。只要记住它们记录的是活动，不是生产力本身，这次更新就能成为治理 Copilot 使用的一个实用起点。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://github.blog/changelog/2026-09-11-add-vs-code-agents-to-copilot-usage-metrics">Add VS Code Agents to Copilot usage metrics - GitHub Changelog</a></li><li><a href="https://docs.github.com/rest/copilot/copilot-usage-metrics">GitHub Copilot usage metrics - GitHub Docs</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/12/2026-GitHub-Copilot%E4%BD%BF%E7%94%A8%E6%8C%87%E6%A0%87%E6%94%AF%E6%8C%81VSCodeAgents/</id>
    <link href="https://code.idea4j.com/2026/09/12/2026-GitHub-Copilot%E4%BD%BF%E7%94%A8%E6%8C%87%E6%A0%87%E6%94%AF%E6%8C%81VSCodeAgents/"/>
    <published>2026-09-12T00:01:00.000Z</published>
    <summary>GitHub Copilot 使用指标报告新增 VS Code Agents 窗口的活跃用户、会话数和消息数。本文梳理新增字段、API 获取方式，以及如何避免把 Agent 使用量直接当成生产力结论。</summary>
    <title>GitHub Copilot 使用指标新增 VS Code Agents：团队该如何读懂 Agent 活跃度</title>
    <updated>2026-09-12T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="安全" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="GitHub" scheme="https://code.idea4j.com/tags/GitHub/"/>
    <category term="AI Scan" scheme="https://code.idea4j.com/tags/AI-Scan/"/>
    <category term="Code Scanning" scheme="https://code.idea4j.com/tags/Code-Scanning/"/>
    <category term="Pull Request" scheme="https://code.idea4j.com/tags/Pull-Request/"/>
    <category term="DevSecOps" scheme="https://code.idea4j.com/tags/DevSecOps/"/>
    <category term="API" scheme="https://code.idea4j.com/tags/API/"/>
    <content>
      <![CDATA[<p>GitHub 在 2026 年 9 月 10 日宣布，AI Scan for pull requests 的组织级和仓库级 REST API 进入 Public Preview。团队现在可以用接口读取或更新 AI Scan 的启用状态，再把它接入内部的仓库治理、项目初始化和安全合规流程。</p><p>这项更新的价值不在于多了两个 API 路径，而在于安全检测的开关开始具备“平台配置”的形态。组织可以规定哪些仓库允许运行 AI Scan，仓库又可以按项目需要单独开启或关闭。对维护大量仓库的 DevSecOps 团队来说，接入自动化后，不必再逐个打开 GitHub 设置页面。</p><span id="more"></span><h2 id="AI-Scan-到底检测什么"><a href="#AI-Scan-到底检测什么" class="headerlink" title="AI Scan 到底检测什么"></a>AI Scan 到底检测什么</h2><p>GitHub 文档把这项能力称为 AI-powered security detections。它使用独立的 AI 扫描引擎，在 Pull Request 中寻找 CodeQL 尚未覆盖的语言和框架安全问题，作为 CodeQL 的补充。</p><p>它和传统的代码扫描告警有几个重要区别：</p><table><thead><tr><th>特性</th><th>AI Scan for pull requests</th></tr></thead><tbody><tr><td>扫描范围</td><td>Pull Request，以及创建后每次新增提交</td></tr><tr><td>结果位置</td><td>Pull Request 的 Conversation 和 Files changed 页面</td></tr><tr><td>仓库安全列表</td><td>不会作为长期 backlog 告警出现在仓库安全视图中</td></tr><tr><td>合并行为</td><td>结果属于建议，不会自动阻止合并</td></tr><tr><td>构建要求</td><td>不要求项目提供构建系统</td></tr><tr><td>与 CodeQL 的关系</td><td>独立运行，补充 CodeQL 覆盖范围</td></tr></tbody></table><p>AI Scan 会直接分析 Pull Request 中的代码，也可以通过代码搜索获取仓库上下文。它使用自己的专用提示，不会读取 <code>/.github/copilot-instructions.md</code> 或 <code>/CLAUDE.md</code> 这类自定义指令文件。结果会标记“AI”标识，便于审查者和 CodeQL 告警区分。</p><p>这决定了它更适合充当代码审查时的第二双眼睛。它的结果需要开发者判断，不能把“没有发现 AI Scan 问题”理解成一次完整的安全证明。</p><h2 id="新-API-解决的是批量治理问题"><a href="#新-API-解决的是批量治理问题" class="headerlink" title="新 API 解决的是批量治理问题"></a>新 API 解决的是批量治理问题</h2><p>GitHub 本次开放了两组同构接口：组织级接口决定组织是否允许 Pull Request 扫描，仓库级接口决定具体仓库是否启用。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">GET   /orgs/&#123;org&#125;/code-scanning/ai-scan</span><br><span class="line">PATCH /orgs/&#123;org&#125;/code-scanning/ai-scan</span><br><span class="line"></span><br><span class="line">GET   /repos/&#123;owner&#125;/&#123;repo&#125;/code-scanning/ai-scan</span><br><span class="line">PATCH /repos/&#123;owner&#125;/&#123;repo&#125;/code-scanning/ai-scan</span><br></pre></td></tr></table></figure><p>更新接口使用 <code>pr_scan</code> 字段表达状态：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;pr_scan&quot;</span><span class="punctuation">:</span> <span class="string">&quot;enabled&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>读取接口会返回 <code>enabled</code> 或 <code>disabled</code>。如果需要关闭，也应明确提交 <code>&quot;pr_scan&quot;: &quot;disabled&quot;</code>，而不是依赖仓库创建时的默认值。</p><p>组织级状态和仓库级状态不是简单的两层覆盖。官方公告明确说明，组织禁用时，仓库级设置不能绕过组织限制。也就是说，仓库 API 可以表达“这个项目想启用”，但不能越过组织级的总开关。</p><p>可以把最终效果理解成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">组织允许 AI Scan  +  仓库启用 AI Scan  =  该仓库可以运行</span><br><span class="line">组织禁用 AI Scan  +  仓库启用 AI Scan  =  仍然不能运行</span><br></pre></td></tr></table></figure><p>这个约束很适合平台治理。安全团队可以控制组织边界，业务团队可以在边界内管理仓库级选择，自动化脚本也能在执行变更前先读取两层状态，避免误以为 PATCH 成功就一定会开始扫描。</p><h2 id="接入自动化前，先把状态模型想清楚"><a href="#接入自动化前，先把状态模型想清楚" class="headerlink" title="接入自动化前，先把状态模型想清楚"></a>接入自动化前，先把状态模型想清楚</h2><p>最直接的接法是给新仓库初始化流程增加一步：创建仓库后读取组织和仓库的 AI Scan 状态，按团队策略更新仓库状态。对于已有仓库，则可以定期巡检，把状态和仓库的语言、负责人、风险等级、是否存放生产代码等信息放在一起，供安全团队复核。</p><p>一个比较稳妥的流程如下：</p><ol><li>读取组织级状态。如果组织层是 <code>disabled</code>，记录原因并停止向下修改。</li><li>读取仓库级状态，和配置中心中的期望状态比较。</li><li>只有在期望值与实际值不一致时，才调用仓库级 PATCH 接口。</li><li>重新读取一次状态，保存审计记录，包括仓库名、操作者、时间、旧值和新值。</li><li>将 AI Scan 的 Pull Request 结果交给正常的代码审查流程，而不是直接当成自动合并条件。</li></ol><p>这里有一个容易被忽略的细节：开关治理和告警处置是两套流程。API 只能管理“是否启用”，不能替团队决定某条发现是否真实、是否需要修复，也不能把 advisory 结果自动变成合并阻断规则。若组织确实需要阻断合并，应另外设计基于检查状态、人工审批和风险分级的策略，并清楚区分 AI Scan 与 CodeQL 的结果来源。</p><h2 id="权限和预览状态不能省略"><a href="#权限和预览状态不能省略" class="headerlink" title="权限和预览状态不能省略"></a>权限和预览状态不能省略</h2><p>AI Scan API 目前处于 Public Preview，接口和行为仍可能变化。文档列出的仓库级更新接口需要细粒度令牌的仓库 <code>Administration</code> 写权限；读取接口则需要相应的读取权限。经典令牌和 OAuth App 的权限要求也应以当前 REST API 文档为准，不能直接复用一个拥有全部仓库权限的通用令牌。</p><p>建议把权限拆开：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">仓库发现：读取组织和仓库清单</span><br><span class="line">AI Scan 配置：仅更新目标仓库的 Administration 权限</span><br><span class="line">审计存储：写入内部配置中心或日志系统</span><br></pre></td></tr></table></figure><p>配置服务不需要读取源代码，也不应该拥有修改代码、合并 Pull Request 或发布软件包的权限。对于自托管自动化 Runner，还要限制令牌的环境暴露范围，避免把一个可以修改安全配置的凭据注入到所有构建任务中。</p><p>官方文档还列出了产品侧的使用条件：AI-powered security detections 在 Public Preview 期间需要 GitHub Advanced Security 许可和 GitHub Copilot 许可；仓库需要启用 CodeQL Default Setup，并主动选择加入 AI 检测。GitHub Enterprise Server 当前不在这次公告的支持范围内。</p><h2 id="它和-CodeQL-是什么关系"><a href="#它和-CodeQL-是什么关系" class="headerlink" title="它和 CodeQL 是什么关系"></a>它和 CodeQL 是什么关系</h2><p>AI Scan 并没有替代 CodeQL。两者的运行方式和结果模型都不同：CodeQL 可以建立数据库并持续维护代码扫描告警，AI Scan 只针对 Pull Request 提供额外的安全发现；AI Scan 的结果也不会变成仓库安全视图中的 backlog 告警。</p><p>另一个值得注意的行为是，AI Scan 与 CodeQL 的状态相互独立。即使 CodeQL Default Setup 处于等待或失败状态，AI Scan 仍可能运行。因此，团队在排查 Pull Request 上的安全结果时，需要同时查看两类检查的状态和时间，不能只等 CodeQL 完成后再判断 AI Scan 是否生效。</p><p>这也意味着指标不能只统计“代码扫描是否通过”。平台团队至少应该区分：</p><ul><li>CodeQL 是否完成，以及是否产生持久化告警；</li><li>AI Scan 是否运行，以及是否产生 Pull Request 建议；</li><li>开发者是否对 AI Scan 结果进行了确认或反馈；</li><li>安全问题是否最终进入修复、复核和关闭流程。</li></ul><h2 id="现在适合做什么"><a href="#现在适合做什么" class="headerlink" title="现在适合做什么"></a>现在适合做什么</h2><p>如果组织已经购买了对应许可，并且仓库规模足够大，今天可以先做一个小范围试点。选择一组语言和框架不同的仓库，使用 API 统一读取状态，再让项目负责人决定哪些仓库启用。试点期间重点观察发现质量、审查耗时、误报反馈和权限审计，不要先把结果设置成强制合并门槛。</p><p>如果组织还没有准备好运行 AI Scan，也可以先把 API 接入仓库治理系统，但只做读取和审计。这样可以先了解当前状态分布，等许可、代码审查流程和告警分级准备好后，再分批打开开关。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>GitHub 为 AI Scan 增加组织级和仓库级 API，让 Pull Request 安全检测从单个仓库的页面设置，变成可以被平台工程管理的配置。它带来的直接收益是批量启用、状态巡检和审计更容易实现；真正的接入难点则在于如何处理组织与仓库两层状态、如何区分 AI Scan 与 CodeQL 的结果，以及如何把建议纳入人工复核和修复闭环。</p><p>在 Public Preview 阶段，比较合适的做法是先读取、再小范围启用，保留回滚和审计记录。把 AI Scan 当作 Pull Request 中的一层补充检查，而不是唯一的安全结论，才能让这项能力真正服务于代码审查流程。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://github.blog/changelog/2026-09-10-ai-scan-for-pull-request-apis-in-public-preview">AI Scan for pull request APIs in public preview - GitHub Changelog</a></li><li><a href="https://docs.github.com/code-security/concepts/code-scanning/ai-powered-security-detections">AI-powered security detections in pull requests - GitHub Docs</a></li><li><a href="https://docs.github.com/rest/code-scanning/code-scanning?apiVersion=2026-03-10">Code scanning REST API - GitHub Docs</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/11/2026-GitHub-AI-Scan%E7%BB%84%E7%BB%87%E7%BA%A7API/</id>
    <link href="https://code.idea4j.com/2026/09/11/2026-GitHub-AI-Scan%E7%BB%84%E7%BB%87%E7%BA%A7API/"/>
    <published>2026-09-11T00:01:00.000Z</published>
    <summary>GitHub 为 AI Scan 开放组织级和仓库级 REST API，可以批量读取或更新 Pull Request 安全检测开关。本文梳理它与 CodeQL 的关系、权限边界和接入时的实践要点。</summary>
    <title>GitHub AI Scan 开放组织级 API：把 Pull Request 安全检测纳入平台治理</title>
    <updated>2026-09-11T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="安全" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E5%AE%89%E5%85%A8/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="Rust" scheme="https://code.idea4j.com/tags/Rust/"/>
    <category term="供应链安全" scheme="https://code.idea4j.com/tags/%E4%BE%9B%E5%BA%94%E9%93%BE%E5%AE%89%E5%85%A8/"/>
    <category term="GitHub" scheme="https://code.idea4j.com/tags/GitHub/"/>
    <category term="CodeQL" scheme="https://code.idea4j.com/tags/CodeQL/"/>
    <category term="ARM64" scheme="https://code.idea4j.com/tags/ARM64/"/>
    <category term="代码扫描" scheme="https://code.idea4j.com/tags/%E4%BB%A3%E7%A0%81%E6%89%AB%E6%8F%8F/"/>
    <content>
      <![CDATA[<p>GitHub 在 2026 年 9 月 9 日发布 CodeQL 2.27.0。这个版本最值得开发者关注的变化有三项：CodeQL CLI 和 bundle 开始原生支持 Linux ARM64，Rust 增加 <code>rust/command-line-injection</code> 安全查询，Code Scanning Default Setup 可以使用组织的私有注册表配置获取自定义查询和查询包。</p><p>它们分别解决了扫描基础设施、Rust 应用安全和企业内部规则分发的问题。对使用 ARM64 自托管 Runner、维护 Rust 服务，或把 CodeQL 接入私有依赖体系的团队来说，这次升级不只是查询数量增加，还会改变扫描环境和权限配置的边界。</p><span id="more"></span><h2 id="先看-2-27-0-带来了什么"><a href="#先看-2-27-0-带来了什么" class="headerlink" title="先看 2.27.0 带来了什么"></a>先看 2.27.0 带来了什么</h2><p>CodeQL 2.27.0 的 Default suite 现在包含 498 个安全查询，覆盖 170 个 CWE；Extended suite 另外启用 131 个查询，覆盖 32 个 CWE。本次版本新增了 1 个安全查询，核心变化可以归纳为：</p><table><thead><tr><th>变化</th><th>对工程的直接影响</th></tr></thead><tbody><tr><td>Linux ARM64 成为一等平台</td><td>ARM64 自托管 Runner 可以直接运行 CodeQL CLI 和 bundle</td></tr><tr><td>新增 Rust 命令行注入查询</td><td>扫描不受控命令行参数进入进程执行路径的风险</td></tr><tr><td>私有注册表配置接入 Default Setup</td><td>组织可以从私有 Git 源或 Docker Registry 获取自定义查询和查询包</td></tr><tr><td>CodeQL CLI 更倾向平台专用包</td><td>安装脚本需要从通用压缩包切换到平台包</td></tr><tr><td>GitHub Actions 查询和多语言模型更新</td><td>可能出现新的告警，也可能减少旧的误报</td></tr></tbody></table><p>因此，升级后的第一步不应该是直接把告警数量和上一个版本比较。要先确认 Runner 架构、查询包来源、组织权限和自定义模型是否都在新版本的支持范围内。</p><h2 id="Linux-ARM64-支持，解决的是扫描基础设施问题"><a href="#Linux-ARM64-支持，解决的是扫描基础设施问题" class="headerlink" title="Linux ARM64 支持，解决的是扫描基础设施问题"></a>Linux ARM64 支持，解决的是扫描基础设施问题</h2><p>CodeQL 2.27.0 把 <code>linux-arm64</code> 作为一等平台，提供平台专用的 CLI 和 bundle 下载包。过去使用 ARM64 机器做安全扫描时，团队往往需要额外准备 x86 构建节点、模拟层，或者自行维护一套能运行的工具链。现在可以让扫描任务直接运行在 Linux ARM64 主机上。</p><p>这对自托管 Runner 尤其有用。很多低功耗服务器、云端 ARM 实例和内部构建集群已经是 ARM64 架构，但编译、测试和安全扫描经常因为工具链不齐而被迫回到 x86。CodeQL 本身能够在 ARM64 上运行后，团队可以把代码构建和数据库提取放在同一类机器上，少处理一层架构差异。</p><p>不过，官方也明确说明 ARM64 二进制只通过平台专用下载包提供，不包含在组合版 <code>codeql.zip</code>、<code>codeql-bundle.tar.gz</code> 或 <code>codeql-bundle.tar.zst</code> 中。安装脚本如果仍然无条件下载通用包，就会出现“版本已经支持 ARM64，但 Runner 仍然找不到可执行文件”的问题。</p><p>一个更稳妥的下载逻辑是先识别运行环境，再选择对应资产：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">系统：Linux</span><br><span class="line">架构：aarch64 / arm64</span><br><span class="line">资产：codeql-linux-arm64.zip</span><br><span class="line">     或 codeql-bundle-linux-arm64.tar.gz</span><br></pre></td></tr></table></figure><p>不要只看操作系统名称。<code>linux-amd64</code> 和 <code>linux-arm64</code> 在 GitHub Actions 的 Runner 标签、容器基础镜像和缓存目录上都可能有不同约定。安装后应该显式运行 <code>codeql version</code>，再执行一次最小数据库创建任务，确认 CLI、查询包和语言提取器都能正常工作。</p><h2 id="Rust-命令行注入查询为什么重要"><a href="#Rust-命令行注入查询为什么重要" class="headerlink" title="Rust 命令行注入查询为什么重要"></a>Rust 命令行注入查询为什么重要</h2><p>Rust 的内存安全特性不能自动阻止命令注入。只要应用把外部输入拼进 shell 命令，攻击者仍然可能改变命令的含义。一个简化的风险示意是：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">use</span> std::process::Command;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">run_tool</span>(user_argument: <span class="type">String</span>) &#123;</span><br><span class="line">    <span class="comment">// 仅作风险示意：不要把未验证输入交给 shell 解释器。</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">_</span> = Command::<span class="title function_ invoke__">new</span>(<span class="string">&quot;sh&quot;</span>)</span><br><span class="line">        .<span class="title function_ invoke__">arg</span>(<span class="string">&quot;-c&quot;</span>)</span><br><span class="line">        .<span class="title function_ invoke__">arg</span>(<span class="built_in">format!</span>(<span class="string">&quot;tool &#123;&#125;&quot;</span>, user_argument))</span><br><span class="line">        .<span class="title function_ invoke__">status</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>真正的问题不在 <code>Command</code> 这个类型本身，而在于不可信输入是否进入了可解释的命令行。修复通常需要避免调用 shell、把程序名和参数分开传递，并对业务允许的参数做白名单约束。仅仅把字符串中的几个特殊字符替换掉，往往难以覆盖不同 shell 和不同调用链上的语义差异。</p><p>CodeQL 2.27.0 新增的 <code>rust/command-line-injection</code> 查询，目标就是发现这类不受控制的命令行。它把一个过去需要安全工程师手工检查的数据流问题，加入了 Rust 默认安全检查的范围。团队需要注意两件事：第一，新增查询会带来新的告警，告警不等于已经确认存在可利用漏洞；第二，查询无法替代代码审查，业务边界、输入来源和部署环境仍然需要人工判断。</p><h3 id="升级后如何处理-Rust-告警"><a href="#升级后如何处理-Rust-告警" class="headerlink" title="升级后如何处理 Rust 告警"></a>升级后如何处理 Rust 告警</h3><p>可以按下面的顺序处理：</p><ol><li>先确认输入来源。HTTP 参数、消息队列内容、配置文件和用户上传数据都可能是外部输入，但信任边界取决于实际部署方式。</li><li>再确认执行方式。直接调用固定二进制并把参数作为独立参数传递，与 <code>sh -c</code>、<code>cmd /C</code> 或其他 shell 解释器的风险不同。</li><li>最后判断业务约束。即使输入不是公网可控，也要检查是否能被低权限用户、租户或被入侵的上游服务影响。</li></ol><p>如果团队有自定义 Rust 数据扩展模型，还要留意 2.27.0 的兼容性变化。官方变更日志提到，Rust trait item 的规范路径改为 <code>&lt;crate::Trait&gt;::item</code>。引用旧路径的自定义模型需要同步更新，否则扫描结果可能缺少预期的数据流关系。</p><h2 id="私有查询包开始进入-Default-Setup"><a href="#私有查询包开始进入-Default-Setup" class="headerlink" title="私有查询包开始进入 Default Setup"></a>私有查询包开始进入 Default Setup</h2><p>很多组织的安全规则不会全部放在公开查询包中。团队可能有内部框架、私有依赖或专门针对业务协议的查询，这些内容通常存放在私有 Git 仓库或私有 Docker Registry。CodeQL 2.27.0 允许 Code Scanning Default Setup 使用组织级私有注册表配置，在获取自定义 queries 或 packs 时完成认证。</p><p>这个变化让 Default Setup 更适合企业内部规则分发，但也引入了清晰的权限边界。组织需要配置合适的 <code>Git Source</code> 或 <code>Docker Registry</code> 私有注册表，并确认扫描任务只获得读取查询包所需的权限。不要为了让扫描通过，把一个可以写入源码仓库或发布包的凭据直接交给所有 Runner。</p><p>可以把权限拆成三层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">代码仓库：扫描任务读取待分析代码</span><br><span class="line">查询来源：扫描任务读取私有查询和模型包</span><br><span class="line">结果上传：扫描任务写入代码扫描结果</span><br></pre></td></tr></table></figure><p>三者不应该共享同一个高权限 token。尤其是自托管 Runner，扫描过程会接触源代码、依赖和分析结果，Runner 被其他任务复用时，还要检查工作目录清理、凭据注入和网络出口策略。</p><h2 id="其他变化也可能改变告警基线"><a href="#其他变化也可能改变告警基线" class="headerlink" title="其他变化也可能改变告警基线"></a>其他变化也可能改变告警基线</h2><p>2.27.0 并不只针对 ARM64 和 Rust。升级时还应注意这些变化：</p><ul><li>C&#x2F;C++ 查询把 PostgreSQL <code>libpq</code> 的 <code>PQexec</code>、<code>PQexecParams</code>、<code>PQprepare</code>、<code>PQsendQuery</code>、<code>PQsendQueryParams</code> 和 <code>PQsendPrepare</code> 纳入 SQL 注入 sink。使用这些接口的项目可能出现此前没有的告警。</li><li>Java&#x2F;Kotlin 增加了 Micronaut 的建模，覆盖 HTTP 控制器、WebSocket、配置注入、数据访问和安全注解等场景。</li><li>GitHub Actions 查询修正了 <code>author_association</code> 在事件 payload 不包含对应字段时的判断。某些原来被错误认为具有保护作用的条件，升级后可能触发新的告警。</li><li>Java 9 和 Java 10 的语言支持已经标记为弃用，官方计划在 2027 年 1 月移除；Java 7 和 Java 8 仍会继续支持。</li><li>通用多平台 <code>codeql.zip</code> 已被弃用，未来应改用 <code>codeql-PLATFORM.zip</code> 这样的平台专用包。当前版本还会发出提醒，可以用 <code>CODEQL_ALLOW_ALL_PLATFORMS_DIST=true</code> 暂时压制提示，但这不是长期迁移方案。</li></ul><p>这些改动说明安全扫描结果不是一个永远不变的数字。查询、框架模型、事件语义和提取器都会更新，团队需要把升级后的告警重新分级，而不是简单地把新增告警全部标成误报。</p><h2 id="给安全和平台团队的升级清单"><a href="#给安全和平台团队的升级清单" class="headerlink" title="给安全和平台团队的升级清单"></a>给安全和平台团队的升级清单</h2><h3 id="1-先做架构和安装包矩阵"><a href="#1-先做架构和安装包矩阵" class="headerlink" title="1. 先做架构和安装包矩阵"></a>1. 先做架构和安装包矩阵</h3><p>把 Runner 的系统、架构、容器镜像和 CodeQL 下载资产列成矩阵。至少覆盖 Linux AMD64、Linux ARM64 和本地开发机使用的架构，避免 CI 能跑但本地无法复现，或者 ARM64 Runner 被错误安装了组合包。</p><h3 id="2-用基线区分新发现和真实回归"><a href="#2-用基线区分新发现和真实回归" class="headerlink" title="2. 用基线区分新发现和真实回归"></a>2. 用基线区分新发现和真实回归</h3><p>在升级前保存一次扫描结果，升级后分别统计新告警、关闭告警和位置变化。对 Rust 命令行注入、C&#x2F;C++ PostgreSQL sink 和 GitHub Actions author association 变化，单独建立复核清单，不要直接用总数判断版本好坏。</p><h3 id="3-给私有查询包配置最小读取权限"><a href="#3-给私有查询包配置最小读取权限" class="headerlink" title="3. 给私有查询包配置最小读取权限"></a>3. 给私有查询包配置最小读取权限</h3><p>查询包的读取凭据只应该能够访问必要的 Git 源或 Registry。扫描任务失败时，先检查注册表配置、Runner 网络和查询包版本，再考虑扩大权限。权限扩大应有期限和审计记录。</p><h3 id="4-检查自定义模型的升级兼容性"><a href="#4-检查自定义模型的升级兼容性" class="headerlink" title="4. 检查自定义模型的升级兼容性"></a>4. 检查自定义模型的升级兼容性</h3><p>Rust trait path、Ruby 控制流图以及部分语言框架模型都有变化。自定义 query、model pack 和扩展库应该放在一个最小测试仓库中，升级 CodeQL 后先验证查询是否还能得到预期路径，再推广到所有项目。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>CodeQL 2.27.0 的意义在于，安全扫描开始更贴近实际工程的两端：一端是 ARM64 自托管基础设施，另一端是 Rust、私有框架和内部查询规则这些具体的开发场景。Linux ARM64 支持可以减少架构迁移成本，Rust 命令行注入查询补上了一个重要的安全检查面，私有注册表配置则让组织规则更容易进入默认扫描流程。</p><p>升级时最重要的动作不是立刻追求更少的告警，而是让告警变化可解释。先固定运行环境，再保存版本基线，最后复核新增查询和权限边界，团队才知道这次升级到底发现了什么，也知道哪些问题值得进入修复计划。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://github.blog/changelog/2026-09-09-codeql-2-27-0-adds-support-for-linux-arm64">CodeQL 2.27.0 adds support for Linux ARM64 - GitHub Changelog</a></li><li><a href="https://codeql.github.com/docs/codeql-overview/codeql-changelog/codeql-cli-2.27.0/">CodeQL 2.27.0 官方变更日志</a></li><li><a href="https://docs.github.com/code-security/code-scanning/introduction-to-code-scanning/about-code-scanning">About code scanning - GitHub Docs</a></li><li><a href="https://docs.github.com/code-security/code-scanning/automatically-scanning-your-code-for-vulnerabilities-and-errors/customizing-code-scanning">Customizing code scanning - GitHub Docs</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/10/2026-CodeQL227LinuxARM64%E4%B8%8ERust%E5%AE%89%E5%85%A8%E6%9F%A5%E8%AF%A2/</id>
    <link href="https://code.idea4j.com/2026/09/10/2026-CodeQL227LinuxARM64%E4%B8%8ERust%E5%AE%89%E5%85%A8%E6%9F%A5%E8%AF%A2/"/>
    <published>2026-09-10T00:01:00.000Z</published>
    <summary>CodeQL 2.27.0 将 Linux ARM64 作为一等平台，新增 Rust 命令行注入查询，并让 Code Scanning Default Setup 使用组织级私有注册表配置获取自定义查询和查询包。</summary>
    <title>CodeQL 2.27.0：Linux ARM64 原生运行与 Rust 命令行注入检测值得升级吗</title>
    <updated>2026-09-10T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="云原生" scheme="https://code.idea4j.com/categories/%E4%BA%91%E5%8E%9F%E7%94%9F/"/>
    <category term="Kubernetes" scheme="https://code.idea4j.com/categories/%E4%BA%91%E5%8E%9F%E7%94%9F/Kubernetes/"/>
    <category term="AI" scheme="https://code.idea4j.com/tags/AI/"/>
    <category term="Kubernetes" scheme="https://code.idea4j.com/tags/Kubernetes/"/>
    <category term="调度" scheme="https://code.idea4j.com/tags/%E8%B0%83%E5%BA%A6/"/>
    <category term="批处理" scheme="https://code.idea4j.com/tags/%E6%89%B9%E5%A4%84%E7%90%86/"/>
    <category term="Gang Scheduling" scheme="https://code.idea4j.com/tags/Gang-Scheduling/"/>
    <category term="PodGroup" scheme="https://code.idea4j.com/tags/PodGroup/"/>
    <content>
      <![CDATA[<p>Kubernetes 官方在 2026 年 9 月 8 日发布文章，介绍 v1.37 在 Workload-Aware Scheduling（工作负载感知调度）上的进展。Workload、PodGroup、工作负载感知抢占，以及 PodGroup 共享 DRA ResourceClaim 都进入 Beta；同时新增的 CompositePodGroup API 让一个分布式任务可以用树形结构表达多层拓扑、成组调度和抢占策略。</p><p>这组变化和普通的调度器参数调整不太一样。它试图解决的是：一个 AI 训练任务或批处理任务由多个 Pod 组成时，如何把这些 Pod 当成一个有约束的工作负载来排队、放置和驱逐。对已经使用 JobSet、LeaderWorkerSet 或自定义控制器的团队来说，v1.37 值得先在测试集群里验证，但不能把 Beta 和 Alpha API 直接当成生产默认能力。</p><span id="more"></span><h2 id="为什么单个-Pod-调度不够用了"><a href="#为什么单个-Pod-调度不够用了" class="headerlink" title="为什么单个 Pod 调度不够用了"></a>为什么单个 Pod 调度不够用了</h2><p>Kubernetes 的基础调度单位是 Pod。这个模型对 Web 服务很自然：每个副本可以独立启动，先到的 Pod 先占用资源通常也没有问题。</p><p>AI 训练、分布式计算和一部分批处理任务则不同。一个任务可能包含 driver、worker 和参数服务器，只有当足够数量的 worker 同时就绪时，任务才有意义。如果调度器先放置了少量 worker，剩余 Pod 长时间排队，已经运行的部分只是在占用资源，并不能让任务真正开始。</p><p>Gang scheduling 的思路是把一组 Pod 当成一个整体，只有同时满足最低数量时才允许这组 Pod 被调度。Kubernetes v1.37 把 Workload 和 PodGroup API 推进到 <code>scheduling.k8s.io/v1beta1</code>，并让它们与 gang scheduling 一起进入 Beta。它不保证所有复杂调度问题都被解决，但至少把“这几个 Pod 属于同一个调度单元”提升成了 Kubernetes API 能理解的对象。</p><h2 id="v1-37-的核心对象如何配合"><a href="#v1-37-的核心对象如何配合" class="headerlink" title="v1.37 的核心对象如何配合"></a>v1.37 的核心对象如何配合</h2><p>可以把这条链路理解成三层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Workload：描述长期有效的调度模板</span><br><span class="line">    ↓</span><br><span class="line">PodGroup：描述一次运行实例的调度策略和状态</span><br><span class="line">    ↓</span><br><span class="line">Pods：由工作负载控制器创建，并引用叶子 PodGroup</span><br></pre></td></tr></table></figure><p><code>Workload</code> 保存的是策略模板，例如 worker 至少需要 4 个 Pod 一起就绪；<code>PodGroup</code> 是某一次任务对应的运行时调度单元，记录调度策略和状态；Job 等工作负载控制器负责把模板转换成实际的 PodGroup 和 Pod。</p><p>一个最小的 PodGroup 示例是：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">scheduling.k8s.io/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PodGroup</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">training-workers</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">schedulingPolicy:</span></span><br><span class="line">    <span class="attr">gang:</span></span><br><span class="line">      <span class="attr">minCount:</span> <span class="number">4</span></span><br></pre></td></tr></table></figure><p>这个示例只表达了最低同时调度数量。实际接入时，Pod 还需要通过调度组字段引用 PodGroup，工作负载控制器也要负责维护这组对象。Kubernetes 文档目前明确说明，Workload&#x2F;PodGroup 功能默认关闭，集群管理员需要在相关组件上启用 <code>GenericWorkload</code> feature gate，并确保 <code>scheduling.k8s.io/v1beta1</code> API 可用。</p><p>因此，Beta 的含义是 API 和实现已经进入更适合试用的阶段，不代表升级集群后所有 Job 会自动获得 gang scheduling。是否启用、由哪个控制器创建 Workload，以及失败后的重试和清理逻辑，仍然属于平台团队需要负责的部分。</p><h2 id="PodGroup-进入调度队列，改变了什么"><a href="#PodGroup-进入调度队列，改变了什么" class="headerlink" title="PodGroup 进入调度队列，改变了什么"></a>PodGroup 进入调度队列，改变了什么</h2><p>v1.37 的一个实现层变化是，PodGroup 不再只是 Pod 旁边的描述对象。以前，即使多个 Pod 属于同一个 PodGroup，调度队列仍然会把它们分别排队；现在，顶层 PodGroup 会成为队列中的对象，组内 Pod 共享队列行为。</p><p>这个变化的价值在于，调度器有了更明确的机会去回答“整组工作负载什么时候有资格尝试调度”。如果仍然把成员 Pod 分开排队，队列顺序和资源碎片会很容易把一个本来可以运行的任务拆散。把组放进队列后，后续的队列策略也有了稳定的扩展点。</p><p>v1.37 还允许修改 gang 的 <code>minCount</code>。这为弹性批处理留下了空间：集群资源紧张时，控制器可以降低最低可运行数量；资源恢复后，再把任务规模扩展回来。这里要特别注意，修改 <code>minCount</code> 只改变调度约束，不会自动为业务完成数据一致性、检查点恢复或 worker 数量伸缩。控制器需要把调度状态和任务本身的容错机制连接起来。</p><h2 id="工作负载感知抢占不再只看单个-Pod"><a href="#工作负载感知抢占不再只看单个-Pod" class="headerlink" title="工作负载感知抢占不再只看单个 Pod"></a>工作负载感知抢占不再只看单个 Pod</h2><p>工作负载感知抢占（Workload-Aware Preemption，WAP）解决的是另一类问题：当一个高优先级工作负载无法放入集群时，调度器应该如何选择低优先级工作负载作为牺牲者，才能为整个任务腾出空间。</p><p>Kubernetes v1.37 将单独的 <code>WorkloadAwarePreemption</code> feature gate 合并进 <code>GenericWorkload</code>。官方文章还提到两项值得关注的调整：</p><ul><li>评估抢占后能否容纳新工作负载时，调度算法不再为每个被恢复的牺牲者重复完整运行一次，而是先完成一次调度，再检查哪些牺牲者可以保留。</li><li>默认抢占开始理解 PodGroup 的 <code>disruptionMode</code>。当工作负载声明 <code>all</code> 时，调度器不会只驱逐组内一个 Pod，破坏这个工作负载的完整性。</li></ul><p>v1.37 同时调整了 <code>disruptionMode</code> 的字段命名，让 PodGroup 与 CompositePodGroup 使用一致的 <code>all</code> 和 <code>single</code> 表达。早期试用过 alpha API 的团队，需要认真核对从 <code>v1alpha2</code> 到 <code>v1alpha3</code> 的迁移变化，不能只替换 <code>apiVersion</code> 后直接应用旧清单。</p><h2 id="CompositePodGroup：从平面分组走向层级调度"><a href="#CompositePodGroup：从平面分组走向层级调度" class="headerlink" title="CompositePodGroup：从平面分组走向层级调度"></a>CompositePodGroup：从平面分组走向层级调度</h2><p>扁平的 PodGroup 可以表达“4 个 worker 一起调度”，但复杂任务常常还有更高一层的约束。例如，一个训练任务需要同时包含两个 worker 组；整个任务要放在同一个可用区，每个 worker 组又要尽量放在同一个机架。此时只有一层 PodGroup 不够表达。</p><p>Kubernetes v1.37 引入 <code>CompositePodGroup</code>，它把工作负载组织成树形结构：非叶子节点是 CompositePodGroup，叶子节点是 PodGroup。父节点可以约束子组的成组调度和拓扑，子节点继续表达各自的 Pod 数量和策略。调度器会把这棵层级结构当成一个有整体约束的工作负载来处理。</p><p>一个两层结构可以抽象成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">训练任务</span><br><span class="line">└── CompositePodGroup：至少两个子组同时可运行</span><br><span class="line">    ├── PodGroup：worker-a，至少 4 个 Pod</span><br><span class="line">    └── PodGroup：worker-b，至少 4 个 Pod</span><br></pre></td></tr></table></figure><p>CompositePodGroup 在 v1.37 仍是 Alpha，API 位于 <code>scheduling.k8s.io/v1alpha3</code>，默认关闭，需要单独启用 <code>CompositePodGroup</code> feature gate。它适合在专门的调度测试集群里验证，不适合在没有回滚方案的生产集群中直接扩大使用范围。</p><h2 id="DRA-资源也可以按工作负载共享"><a href="#DRA-资源也可以按工作负载共享" class="headerlink" title="DRA 资源也可以按工作负载共享"></a>DRA 资源也可以按工作负载共享</h2><p>对于 GPU、网络设备或其他由 Dynamic Resource Allocation（DRA）管理的设备，资源申请以前更容易落在单个 Pod 上。v1.37 将 PodGroup 共享 DRA ResourceClaim 推进到 Beta，PodGroup 可以引用一个 ResourceClaim，让组内多个 Pod 共享这项资源声明。</p><p>这和简单地给每个 Pod 都写一份设备申请不同。共享的资源生命周期与调度组绑定，更符合某些需要共同使用设备或设备分区的工作负载。平台团队仍然需要验证 DRA 驱动、设备分配策略和 PodGroup 的实际行为，尤其要确认失败重试、驱逐和任务清理时资源是否按预期释放。</p><h2 id="给平台团队的升级清单"><a href="#给平台团队的升级清单" class="headerlink" title="给平台团队的升级清单"></a>给平台团队的升级清单</h2><h3 id="先确认-feature-gate-和-API-版本"><a href="#先确认-feature-gate-和-API-版本" class="headerlink" title="先确认 feature gate 和 API 版本"></a>先确认 feature gate 和 API 版本</h3><p>Workload 和 PodGroup 的文档状态是 Beta，但默认关闭；CompositePodGroup 是 Alpha，也默认关闭。升级前应明确由谁为 <code>kube-apiserver</code>、<code>kube-scheduler</code> 和相关控制器配置 feature gate，并在测试集群确认所有组件看到的是同一组能力。</p><h3 id="把调度失败当成正常路径测试"><a href="#把调度失败当成正常路径测试" class="headerlink" title="把调度失败当成正常路径测试"></a>把调度失败当成正常路径测试</h3><p>至少准备三类场景：资源足够时整组同时运行，资源不足时整组保持等待，以及高优先级工作负载触发抢占。检查 PodGroup 的状态、事件、队列行为和被驱逐工作负载，不要只观察最终有没有 Pod 进入 Running。</p><h3 id="评估控制器接入成本"><a href="#评估控制器接入成本" class="headerlink" title="评估控制器接入成本"></a>评估控制器接入成本</h3><p>Kubernetes v1.37 同时提供新的控制器集成 API 和 <code>workloadbuilder</code> Go 库，帮助集群外部控制器接入 Workload-Aware Scheduling。对于已经维护 JobSet、LeaderWorkerSet 或自定义 CRD 的团队，这比每个控制器单独复制一套调度逻辑更容易长期维护，但接入前仍要用自己的工作负载做兼容性和性能测试。</p><h3 id="设计好弹性和中断策略"><a href="#设计好弹性和中断策略" class="headerlink" title="设计好弹性和中断策略"></a>设计好弹性和中断策略</h3><p><code>minCount</code> 可变并不等于任务可以无损缩容，<code>disruptionMode: all</code> 也不等于任务永远不会被抢占。应该把检查点、重试、优先级、抢占策略和拓扑约束放到同一份运行手册里，避免调度器的决定与应用的恢复能力互相冲突。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Kubernetes v1.37 的重点，是让调度器开始理解“一个任务由多组相互依赖的 Pod 组成”。Workload 和 PodGroup 进入 Beta，为常见的成组调度提供了更稳定的 API；CompositePodGroup 则把层级化拓扑和策略带进了 Alpha 阶段。它们能减少自定义调度控制器需要承担的基础工作，但也把队列、公平性、抢占和失败恢复这些问题更明确地暴露出来。</p><p>对使用 Kubernetes 承载 AI 训练和复杂批处理的团队来说，合适的下一步是建立一个小型验证集群，围绕 API 版本、feature gate、调度队列和抢占行为做可重复测试。等这些基础行为稳定后，再决定哪些工作负载值得迁移到原生 Workload-Aware Scheduling 上。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://kubernetes.io/blog/2026/09/08/kubernetes-v1-37-advancing-workload-aware-scheduling/">Kubernetes v1.37: Advancing Workload-Aware Scheduling</a></li><li><a href="https://kubernetes.io/releases/1.37/">Kubernetes 1.37 Release History</a></li><li><a href="https://kubernetes.io/docs/concepts/workloads/workload-api/">Workload API</a></li><li><a href="https://kubernetes.io/docs/concepts/workloads/podgroup-api/">PodGroup API</a></li><li><a href="https://kubernetes.io/docs/concepts/workloads/compositepodgroup-api/">CompositePodGroup API</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/09/2026-Kubernetes137%E5%B7%A5%E4%BD%9C%E8%B4%9F%E8%BD%BD%E6%84%9F%E7%9F%A5%E8%B0%83%E5%BA%A6/</id>
    <link href="https://code.idea4j.com/2026/09/09/2026-Kubernetes137%E5%B7%A5%E4%BD%9C%E8%B4%9F%E8%BD%BD%E6%84%9F%E7%9F%A5%E8%B0%83%E5%BA%A6/"/>
    <published>2026-09-09T00:01:00.000Z</published>
    <summary>Kubernetes v1.37 将 Workload、PodGroup、工作负载感知抢占和 PodGroup 共享 DRA 资源声明推进到 Beta，并引入层级化的 CompositePodGroup。本文梳理 API 关系、特性门控和升级时需要注意的边界。</summary>
    <title>Kubernetes v1.37：工作负载感知调度进入 Beta，AI 批处理开始拥有原生编排入口</title>
    <updated>2026-09-09T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="Rust" scheme="https://code.idea4j.com/categories/Rust/"/>
    <category term="软件开发" scheme="https://code.idea4j.com/categories/Rust/%E8%BD%AF%E4%BB%B6%E5%BC%80%E5%8F%91/"/>
    <category term="Rust" scheme="https://code.idea4j.com/tags/Rust/"/>
    <category term="调试器" scheme="https://code.idea4j.com/tags/%E8%B0%83%E8%AF%95%E5%99%A8/"/>
    <category term="GDB" scheme="https://code.idea4j.com/tags/GDB/"/>
    <category term="LLDB" scheme="https://code.idea4j.com/tags/LLDB/"/>
    <category term="调试体验" scheme="https://code.idea4j.com/tags/%E8%B0%83%E8%AF%95%E4%BD%93%E9%AA%8C/"/>
    <content>
      <![CDATA[<p>Rust 官方在 2026 年 9 月 7 日公布了首份调试专项调查。调查收到 2,300 多份回复，结果显示，受访者里目前使用 Rust 调试器的比例只有略高于 46%；在真正使用调试器的人中，异步代码、宏和复杂类型的可读性仍然是明显短板。对 Rust 开发者来说，这份报告最有价值的地方不在于又多了一组社区统计，而在于它把“调试体验不好”拆成了可以验证、可以改进的工程问题。</p><span id="more"></span><h2 id="先看调查告诉了我们什么"><a href="#先看调查告诉了我们什么" class="headerlink" title="先看调查告诉了我们什么"></a>先看调查告诉了我们什么</h2><p>这份调查不是对所有 Rust 开发者的普查。超过 80% 的受访者自评为中级或高级用户，样本本身更接近活跃的 Rust 社区。因此，下面的数据适合用来识别工具链痛点，不适合直接推断整个 Rust 用户群体的比例。</p><table><thead><tr><th>观察</th><th>调查结果</th></tr></thead><tbody><tr><td>当前使用 Rust 调试器</td><td>略高于 46%</td></tr><tr><td>使用调试器进行逐行执行</td><td>约 87%</td></tr><tr><td>使用调试器查看卡住或崩溃进程的堆栈</td><td>略高于一半</td></tr><tr><td>使用调试器调试异步代码</td><td>约四分之一</td></tr><tr><td>调试时遇到值表示不佳</td><td>略高于 74%</td></tr><tr><td>调试时无法打印变量</td><td>略高于 55%</td></tr></tbody></table><p>还有一个很有意思的交叉结果：44% 的受访者会调试 Rust 与其他语言组合的程序，其中最常见的是 C，比例略高于 70%；其次是 C++，约 43%；Python 约 20%。这说明 Rust 的调试问题经常发生在 FFI、系统组件或已有工程的边界上，不能只把它当成单语言 IDE 功能来处理。</p><h2 id="为什么大家仍然更喜欢日志和-dbg"><a href="#为什么大家仍然更喜欢日志和-dbg" class="headerlink" title="为什么大家仍然更喜欢日志和 dbg!"></a>为什么大家仍然更喜欢日志和 <code>dbg!</code></h2><p>调查中，超过 81% 的受访者表示，在不使用调试器时，主要原因是日志或打印更简单、更快。这个结果并不等于调试器没有用。小问题用一行 <code>println!</code> 或 <code>dbg!</code> 解决，确实比配置调试会话更省事；真正的问题出现在故障需要暂停现场、观察多个变量、追踪异步状态，或者跨越 Rust 与 C&#x2F;C++ 边界的时候。</p><p>调试器要赢得这些场景，首先得降低启动成本。开发者需要知道如何生成带调试信息的构建、如何让 IDE 找到正确的 GDB 或 LLDB、如何处理容器和交叉编译产生的路径差异，以及如何在优化级别和可观察性之间取舍。如果这条链路太长，打印自然会成为默认选项。</p><p>对团队而言，一个实用的做法是把调试配置当成项目的一部分，而不是每个人本地的隐性知识。可以在仓库里固定开发构建 profile，提供 VS Code、Visual Studio 或命令行的启动配置，并用一个最小示例验证断点、堆栈和变量查看都能工作。新人第一次进入项目时，应该能在几分钟内启动一个可调试的程序。</p><h2 id="真正难的是异步代码和复杂值"><a href="#真正难的是异步代码和复杂值" class="headerlink" title="真正难的是异步代码和复杂值"></a>真正难的是异步代码和复杂值</h2><p>在遇到逐行执行问题的受访者中，异步代码是最常见的场景，比例略高于 28%；宏相关代码约 23%。异步代码的执行路径会在 <code>await</code> 处挂起和恢复，调试器看到的调用栈未必对应开发者脑中那条业务路径。宏则会把源码位置、展开后的代码和生成的调试信息叠在一起，单步执行时很容易出现“跳到不认识的地方”。</p><p>这两个问题的共同点是，调试器展示的是机器和编译器留下的执行信息，而开发者需要的是源代码层面的因果关系。改善它们不能只靠增加断点按钮，还需要更准确的调试信息、稳定的源码映射，以及 IDE 对异步任务和宏展开的专门呈现。</p><p>因此，排查 Rust 异步故障时，可以先把问题缩小为三个可观察对象：任务在哪里创建，在哪个 <code>await</code> 前后改变状态，最终由哪个线程或运行时唤醒。为关键状态增加结构化日志和请求标识，仍然很有必要；调试器适合补上“暂停后观察现场”这一环，而不是替代所有运行时观测。</p><h2 id="值看不懂，调试器就很难留下来"><a href="#值看不懂，调试器就很难留下来" class="headerlink" title="值看不懂，调试器就很难留下来"></a>值看不懂，调试器就很难留下来</h2><p>调查中略高于 74% 的受访者遇到过值表示不佳的问题，略高于 55% 的受访者遇到过变量无法打印。报告还提到，<code>enum</code>、<code>std::collections::HashMap</code> 和 <code>Vec</code> 是经常被抱怨的标准库类型。</p><p>这类问题会直接改变开发者的调试习惯。断点本身可能已经命中，但如果变量窗口只能显示指针、内部布局或一大串难以阅读的字段，开发者还是会退回日志。对业务类型来说，情况更明显：一个包含缓存、状态机或智能指针的结构体，默认展示通常无法表达它在业务上的含义。</p><p>Rust 提供了 <code>debugger_visualizer</code> 属性，用来把 Natvis 文件或 GDB pretty printer 嵌入调试信息。最小示例可以写成：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#![debugger_visualizer(natvis_file = <span class="string">&quot;Widget.natvis&quot;</span>)]</span></span><br><span class="line"><span class="meta">#![debugger_visualizer(gdb_script_file = <span class="string">&quot;widget.py&quot;</span>)]</span></span><br></pre></td></tr></table></figure><p>Natvis 主要面向 Windows 调试器，例如 Visual Studio 和 WinDbg；GDB pretty printer 则是结构化的 Python 脚本。Rust Reference 规定，这个属性可以放在模块或 crate 根上，也可以重复使用来加载多个可视化文件。</p><p>不过，调查发现，接近 62% 的受访者既是库作者，又不知道这个属性。知道它但没有使用的库作者中，约一半表示没有时间维护可视化属性，接近一半表示不知道如何编写可视化脚本。这里存在一个很现实的维护问题：可视化文件和类型实现一样，都是库的调试接口，类型布局变化后也需要同步检查。</p><h2 id="给库作者和团队的三个实践建议"><a href="#给库作者和团队的三个实践建议" class="headerlink" title="给库作者和团队的三个实践建议"></a>给库作者和团队的三个实践建议</h2><h3 id="1-把调试体验纳入验收条件"><a href="#1-把调试体验纳入验收条件" class="headerlink" title="1. 把调试体验纳入验收条件"></a>1. 把调试体验纳入验收条件</h3><p>新类型加入公共 API 时，除了检查编译、测试和文档，还可以问一句：开发者在断点处能看懂它吗？对集合、句柄、状态机和 FFI 包装类型，应该提供一个最小调试示例，确认 GDB、LLDB 或目标 IDE 至少有一种可读的展示方式。</p><h3 id="2-用真实故障测试调试配置"><a href="#2-用真实故障测试调试配置" class="headerlink" title="2. 用真实故障测试调试配置"></a>2. 用真实故障测试调试配置</h3><p>不要只验证程序能启动。可以准备一个包含异步任务、宏调用和跨语言边界的调试样例，定期在 CI 或开发环境中验证断点、堆栈、源码映射和变量打印。工具链升级后，先跑这个样例，比等到线上故障时才发现调试器失效更划算。</p><h3 id="3-让日志、追踪和调试器互相补位"><a href="#3-让日志、追踪和调试器互相补位" class="headerlink" title="3. 让日志、追踪和调试器互相补位"></a>3. 让日志、追踪和调试器互相补位</h3><p>日志适合回答“系统在长时间运行中发生了什么”，追踪适合回答“一个请求经过了哪些组件”，调试器适合回答“程序暂停在这里时，内存和控制流到底是什么状态”。三者解决的问题不同。把请求 ID、任务 ID 和关键状态统一起来，开发者才能从线上线索回到本地现场，而不是在三套工具之间重新猜测。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Rust 这份调试调查给出的信号很具体：很多开发者不是不需要调试器，而是调试器还没有在复杂值、异步任务、宏展开和跨语言工程中提供足够清晰的反馈。短期内，团队可以先从可复现的开发配置、带调试信息的构建和关键类型的可视化做起；长期来看，编译器、调试信息格式、IDE 和库作者需要一起把“能停下来”推进到“停下来之后看得懂”。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://blog.rust-lang.org/2026/09/07/rust-debugging-survey-2026-results/">Rust debugging survey 2026 results</a></li><li><a href="https://raw.githubusercontent.com/rust-lang/surveys/main/surveys/2026/debugging/report/debugging-survey-2026-report.pdf">Rust Debugging Survey 2026 完整报告（PDF）</a></li><li><a href="https://doc.rust-lang.org/reference/attributes/debugger.html">The <code>debugger_visualizer</code> attribute - The Rust Reference</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/08/2026-Rust%E8%B0%83%E8%AF%95%E8%B0%83%E6%9F%A5%E4%B8%8E%E8%B0%83%E8%AF%95%E5%99%A8%E4%BD%93%E9%AA%8C/</id>
    <link href="https://code.idea4j.com/2026/09/08/2026-Rust%E8%B0%83%E8%AF%95%E8%B0%83%E6%9F%A5%E4%B8%8E%E8%B0%83%E8%AF%95%E5%99%A8%E4%BD%93%E9%AA%8C/"/>
    <published>2026-09-08T00:01:20.000Z</published>
    <summary>Rust 官方发布 2026 调试调查结果，基于 2300 多份回复分析调试器使用、异步代码、可视化和混合语言开发中的痛点。</summary>
    <title>Rust 2026 调试调查：超过一半的开发者不常用调试器，问题出在体验</title>
    <updated>2026-09-08T00:01:20.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="操作系统" scheme="https://code.idea4j.com/categories/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    <category term="开源软件" scheme="https://code.idea4j.com/categories/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E5%BC%80%E6%BA%90%E8%BD%AF%E4%BB%B6/"/>
    <category term="Linux" scheme="https://code.idea4j.com/tags/Linux/"/>
    <category term="内核" scheme="https://code.idea4j.com/tags/%E5%86%85%E6%A0%B8/"/>
    <category term="7.3-rc2" scheme="https://code.idea4j.com/tags/7-3-rc2/"/>
    <category term="回归测试" scheme="https://code.idea4j.com/tags/%E5%9B%9E%E5%BD%92%E6%B5%8B%E8%AF%95/"/>
    <category term="BPF" scheme="https://code.idea4j.com/tags/BPF/"/>
    <category term="存储驱动" scheme="https://code.idea4j.com/tags/%E5%AD%98%E5%82%A8%E9%A9%B1%E5%8A%A8/"/>
    <content>
      <![CDATA[<p>Linux 7.3-rc2 在北京时间 9 月 7 日凌晨发布。它不是稳定版，也不是一次面向普通用户的功能升级，而是 Linux 7.3 开发周期中的第二个候选版本。</p><p>对大多数桌面用户来说，现在没有必要为了追新版本替换正在使用的内核。对内核开发者、发行版维护者、硬件驱动作者和运行自定义内核的团队来说，rc2 却是一个值得拿来做回归测试的时间点：第一轮合并已经过去，接下来更重要的是确认驱动修复、并发修复和子系统调整没有破坏已有行为。</p><h2 id="rc2-到底意味着什么"><a href="#rc2-到底意味着什么" class="headerlink" title="rc2 到底意味着什么"></a>rc2 到底意味着什么</h2><p>Linux 的开发过程通常先进入合并窗口，接纳新功能和大块改动；合并窗口关闭后，内核进入候选版本阶段。候选版本主要用于修复回归、完善边界情况和稳定已有代码，版本号从 <code>-rc1</code>、<code>-rc2</code> 逐步向正式版靠近。</p><p>这次 <code>v7.3-rc2</code> 与 <code>v7.3-rc1</code> 相隔约一周。kernel.org 将它标记为 mainline 测试版本，并提供了完整源码包，以及从 <code>v7.3-rc1</code> 到 <code>v7.3-rc2</code> 的增量补丁。Linux 主仓库的比较结果显示，这一周合入了 681 个提交；这个数字描述的是代码集成规模，不等同于 681 个面向用户的新功能。</p><p>因此，读 rc2 的正确方式不是问“它增加了什么杀手级特性”，而是问：</p><ul><li>哪些硬件和子系统的旧行为刚刚被修正？</li><li>我的工作负载是否覆盖这些变更的边界？</li><li>如果出现回归，我能否把问题缩小到某个版本或提交？</li></ul><h2 id="从比较结果里能看到哪些修复方向"><a href="#从比较结果里能看到哪些修复方向" class="headerlink" title="从比较结果里能看到哪些修复方向"></a>从比较结果里能看到哪些修复方向</h2><p>候选版本没有一份像稳定版那样面向普通用户的功能清单。结合内核主线的比较记录，可以看到本轮包含存储、跟踪、BPF、音频、显示和文件系统等方向的修复。下面几类变化很适合作为测试切入点。</p><h3 id="存储：限制-NVMe-请求，避免-PRP-链越界"><a href="#存储：限制-NVMe-请求，避免-PRP-链越界" class="headerlink" title="存储：限制 NVMe 请求，避免 PRP 链越界"></a>存储：限制 NVMe 请求，避免 PRP 链越界</h3><p><code>megaraid_sas</code> 驱动修正了 NVMe 请求大小与 PRP 链帧容量之间的关系。此前，块层的默认最大请求大小变化后，一次请求可能需要更多 PRP 表项；当这些表项超过驱动分配的帧边界时，可能出现越界写入。</p><p>这类问题有两个麻烦之处。第一，硬件和请求大小必须同时满足条件，普通磁盘读写不一定马上触发。第二，越界区域如果刚好映射到可写内存，系统可能不会立刻崩溃，而是先悄悄破坏相邻对象。</p><p>如果团队使用 MegaRAID 控制器、NVMe 后端或高吞吐存储，测试不能只做一次启动和简单的文件复制。更有价值的组合是：</p><ul><li>大块顺序读写与随机读写；</li><li>多队列并发 I&#x2F;O 和长时间压力测试；</li><li>文件系统检查、重启和设备异常恢复；</li><li>对比 rc1、rc2 与当前稳定内核的 dmesg、I&#x2F;O 错误和延迟。</li></ul><p>这类修复也提醒驱动作者，块层默认值变化后，驱动内部对请求大小的假设必须重新检查。一个上游看似通用的容量调整，可能让下游硬件路径暴露出此前没有遇到的边界。</p><h3 id="跟踪：初始化顺序和对象生命周期仍然是高风险区域"><a href="#跟踪：初始化顺序和对象生命周期仍然是高风险区域" class="headerlink" title="跟踪：初始化顺序和对象生命周期仍然是高风险区域"></a>跟踪：初始化顺序和对象生命周期仍然是高风险区域</h3><p>本轮比较中还包含 ftrace 初始化同步以及 trace 选项文件引用生命周期方面的修复。内核跟踪系统会被调试工具、性能分析器和生产诊断脚本同时使用，许多问题只在“一个线程打开文件、另一个线程移除实例”的并发场景中出现。</p><p>这类修复的测试重点不应只是“能不能打开 tracing”。更适合做压力循环：反复创建和删除 trace 实例，同时启停事件、读取 options 文件，并让多个线程并行执行。测试结束后还要检查是否出现 use-after-free 报告、锁依赖告警、软锁死或异常的引用计数。</p><p>内核调试功能有一个常见误区：平时不用，就以为相关代码不会影响系统。实际上，跟踪接口常常与调度、锁和对象生命周期相连；即使业务程序没有主动启用它们，内核配置和模块加载方式仍可能改变执行路径。</p><h3 id="BPF：验证器修复不能只看“程序能否加载”"><a href="#BPF：验证器修复不能只看“程序能否加载”" class="headerlink" title="BPF：验证器修复不能只看“程序能否加载”"></a>BPF：验证器修复不能只看“程序能否加载”</h3><p>BPF 相关提交修正了回溯分析在处理回调时清理外层寄存器状态的问题，并补充了对应的自测试。BPF 验证器的工作不是运行一段程序后再观察结果，而是在加载前分析寄存器、栈和控制流，证明程序满足安全约束。</p><p>验证器的一个小错误，可能导致两种相反的结果：合法程序被错误拒绝，或者本来应该保留的状态被错误清除，导致分析结果不可靠。后者尤其需要谨慎，因为“程序成功加载”并不等于验证器覆盖了所有应有的路径。</p><p>使用 BPF 的团队应把自测试、实际程序和内核版本一起纳入矩阵。可以重点覆盖 <code>bpf_loop()</code>、回调、尾调用、map 访问和不同 JIT 架构，观察加载结果、验证日志和运行时行为是否一致。不要只在 x86_64 上通过后就认为其他架构也没有问题。</p><h3 id="音频与显示：硬件回归往往只发生在特定组合"><a href="#音频与显示：硬件回归往往只发生在特定组合" class="headerlink" title="音频与显示：硬件回归往往只发生在特定组合"></a>音频与显示：硬件回归往往只发生在特定组合</h3><p>候选版本还包含 Realtek HDA 针对特定 Acer Aspire A515-57G 机型的冷启动耳机检测修复，以及 AMD 显示混合模式属性相关调整。这种补丁看起来很窄，却是内核维护中很重要的一类工作：同一套驱动代码在不同 PCI 标识、固件版本、启动顺序下可能走不同分支。</p><p>对硬件厂商和发行版维护者来说，测试表不能只写“音频正常”“画面正常”。更具体的检查包括：</p><ul><li>耳机在冷启动前已插入、系统启动后再插入，两种情况分别验证；</li><li>休眠、恢复、重启和切换输出设备；</li><li>多显示器、不同刷新率和显示器热插拔；</li><li>记录 PCI ID、固件版本、桌面协议和内核配置。</li></ul><p>当问题只在冷启动或恢复路径中出现时，用户很难用一句“偶发”描述清楚。测试记录越具体，内核开发者越容易复现和定位。</p><h2 id="为什么候选版更适合做回归测试"><a href="#为什么候选版更适合做回归测试" class="headerlink" title="为什么候选版更适合做回归测试"></a>为什么候选版更适合做回归测试</h2><p>候选版的意义，在于它把“新功能快速合入”和“行为稳定下来”分成了不同阶段。到了 rc2，开发者已经可以围绕相对稳定的接口和配置开展测试，同时还有机会在正式版前反馈问题。</p><p>这对不同角色的价值不一样：</p><table><thead><tr><th>角色</th><th>关注点</th><th>适合的动作</th></tr></thead><tbody><tr><td>驱动作者</td><td>设备初始化、请求边界、并发和错误恢复</td><td>在真实硬件上跑定向压力测试</td></tr><tr><td>发行版维护者</td><td>配置、模块、安装和升级</td><td>用发行版配置构建并跑自动化测试</td></tr><tr><td>云平台或基础设施团队</td><td>I&#x2F;O、网络、eBPF 和监控</td><td>在隔离节点进行工作负载回放</td></tr><tr><td>普通桌面用户</td><td>外设、图形、休眠和音频</td><td>等待发行版打包或稳定版发布</td></tr></tbody></table><p>候选版本不适合直接替换生产内核，除非团队已经有完整的启动回滚、节点隔离和监控机制。测试的目标是尽早发现问题，不是为了让生产环境承担验证成本。</p><h2 id="一套可执行的-rc2-回归流程"><a href="#一套可执行的-rc2-回归流程" class="headerlink" title="一套可执行的 rc2 回归流程"></a>一套可执行的 rc2 回归流程</h2><h3 id="1-先保留可回滚入口"><a href="#1-先保留可回滚入口" class="headerlink" title="1. 先保留可回滚入口"></a>1. 先保留可回滚入口</h3><p>在测试机器上保留当前稳定内核，并确认引导菜单可以选择旧版本。云主机或裸机环境还要确认远程控制台、串口或带外管理可用。内核测试最怕“升级成功，但无法远程进入机器”。</p><h3 id="2-用现有配置生成最小变更"><a href="#2-用现有配置生成最小变更" class="headerlink" title="2. 用现有配置生成最小变更"></a>2. 用现有配置生成最小变更</h3><p>尽量从正在运行的内核配置开始，使用 <code>make olddefconfig</code> 处理新增选项，再编译 rc2。这样出现差异时，问题更可能来自候选版本身，而不是同时更换了大量配置。</p><p>常见流程可以写成：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">cp /boot/config-$(uname -r) .config</span><br><span class="line">make olddefconfig</span><br><span class="line">make -j&quot;$(nproc)&quot;</span><br><span class="line">make modules</span><br></pre></td></tr></table></figure><p>这只是构建示例，安装和启动方式应按发行版的打包策略执行。不要在不了解引导配置的情况下直接覆盖生产内核。</p><h3 id="3-让测试贴近真实工作负载"><a href="#3-让测试贴近真实工作负载" class="headerlink" title="3. 让测试贴近真实工作负载"></a>3. 让测试贴近真实工作负载</h3><p>存储团队应回放真实 I&#x2F;O 模式，BPF 团队应使用真实程序和自测试，桌面团队应覆盖冷启动、休眠和外设热插拔。单纯执行 <code>uname -r</code> 或成功启动，只能证明内核能启动，不能证明变更没有影响业务。</p><h3 id="4-对比三个版本"><a href="#4-对比三个版本" class="headerlink" title="4. 对比三个版本"></a>4. 对比三个版本</h3><p>至少保留稳定版、rc1 和 rc2 的测试结果。对比启动时间、系统调用错误、I&#x2F;O 延迟、网络吞吐、功耗、dmesg 警告和测试失败项。这样可以判断问题是在 rc1 已经出现，还是 rc2 新引入，避免把旧问题误报成新回归。</p><h3 id="5-报告问题时给出可复现条件"><a href="#5-报告问题时给出可复现条件" class="headerlink" title="5. 报告问题时给出可复现条件"></a>5. 报告问题时给出可复现条件</h3><p>一份有用的内核回归报告，应包含内核版本和提交、硬件型号、固件、发行版、配置文件、复现步骤、预期与实际结果，以及必要的日志。对这次 rc2 来说，说明问题是否只出现在特定队列深度、启动顺序、BPF 程序或显示器组合中，比一句“升级后坏了”更有帮助。</p><h2 id="该不该现在升级"><a href="#该不该现在升级" class="headerlink" title="该不该现在升级"></a>该不该现在升级</h2><p>可以按使用场景做判断：</p><ul><li><strong>内核开发者和驱动作者：</strong> 值得在测试机上跟进，尤其是存储、BPF、跟踪、音频和显示相关项目。</li><li><strong>发行版维护者：</strong> 可以开始构建和跑自动化回归，为后续稳定版打包做准备。</li><li><strong>基础设施团队：</strong> 只在隔离节点或灰度环境验证，不要把候选版直接铺到整个集群。</li><li><strong>普通桌面用户：</strong> 没有明确问题需要验证时，继续使用发行版提供的稳定内核更合适。</li></ul><p>rc2 的发布不代表 7.3 已经完成，也不代表所有问题都已修复。它提供的是一份更接近正式版的测试材料，以及一个让问题在稳定版之前被看见的窗口。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Linux 7.3-rc2 的价值不在于“今天多了哪些功能”，而在于它让内核生态进入了下一轮验证阶段。存储请求边界、跟踪对象生命周期、BPF 验证器状态和特定硬件启动顺序，都说明稳定性往往藏在很窄的路径里。</p><p>如果你的项目依赖自定义内核、eBPF、特定存储控制器或复杂外设，今天可以做的事情很具体：保留可回滚内核，用现有配置构建 rc2，回放真实工作负载，并把稳定版、rc1、rc2 的结果放在一起比较。候选版不是用来炫耀版本号的，它的作用是让下一次正式发布少一些意外。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://www.kernel.org/">kernel.org：Linux 7.3-rc2 发布信息、源码与增量补丁</a></li><li><a href="https://git.kernel.org/torvalds/t/linux-7.3-rc2.tar.gz">Linux 7.3-rc2 源码包</a></li><li><a href="https://github.com/torvalds/linux/compare/v7.3-rc1...v7.3-rc2">Linux 内核主线：v7.3-rc1 与 v7.3-rc2 的比较</a></li><li><a href="https://docs.kernel.org/kbuild/kconfig.html">Linux 内核构建文档</a></li><li><a href="https://docs.kernel.org/process/2.Process.html">Linux 内核开发流程文档</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/07/2026-Linux73rc2%E5%86%85%E6%A0%B8%E5%9B%9E%E5%BD%92%E6%B5%8B%E8%AF%95/</id>
    <link href="https://code.idea4j.com/2026/09/07/2026-Linux73rc2%E5%86%85%E6%A0%B8%E5%9B%9E%E5%BD%92%E6%B5%8B%E8%AF%95/"/>
    <published>2026-09-07T00:01:00.000Z</published>
    <summary>Linux 7.3-rc2 已在 9 月 7 日凌晨发布。本文解释候选版本处于什么阶段，梳理其中值得硬件和内核开发者关注的修复方向，并给出一套不影响生产环境的回归测试思路。</summary>
    <title>Linux 7.3-rc2 发布：现在值得测试的不是新功能，而是回归</title>
    <updated>2026-09-07T00:01:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>markee</name>
      <email>wangjinguang66@163.com</email>
    </author>
    <category term="人工智能" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/"/>
    <category term="软件工程" scheme="https://code.idea4j.com/categories/%E4%BA%BA%E5%B7%A5%E6%99%BA%E8%83%BD/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    <category term="GitHub Copilot" scheme="https://code.idea4j.com/tags/GitHub-Copilot/"/>
    <category term="AI Agent" scheme="https://code.idea4j.com/tags/AI-Agent/"/>
    <category term="LLM" scheme="https://code.idea4j.com/tags/LLM/"/>
    <category term="HydraFusion" scheme="https://code.idea4j.com/tags/HydraFusion/"/>
    <category term="多模型编排" scheme="https://code.idea4j.com/tags/%E5%A4%9A%E6%A8%A1%E5%9E%8B%E7%BC%96%E6%8E%92/"/>
    <category term="成本治理" scheme="https://code.idea4j.com/tags/%E6%88%90%E6%9C%AC%E6%B2%BB%E7%90%86/"/>
    <content>
      <![CDATA[<p>以前谈模型路由，通常是在几个模型之间选一个：简单问题交给便宜模型，复杂问题交给更强模型。9 月 4 日，GitHub 介绍了 Project HydraFusion，把选择范围又向前推进了一层：编程智能体不只选择“用哪个模型”，还会在运行时选择“用什么流程解决”。</p><p>HydraFusion 是 GitHub Copilot 中的研究预览。它会先理解任务，再在单模型、级联和批评修订等执行模式之间做选择，必要时调用多个模型完成一次任务。GitHub 公布的受控离线评测显示，经过调优的 HydraFusion 配置在不同基准上相对 Claude Opus 5 的预估成本降低了 36% 至 67%，但质量并非每个基准都更高。这个结果值得关注，恰恰是因为它把重点从“某个模型分数最高”转向了“怎样组织一次完整执行”。</p><h2 id="背景：模型选择已经不够描述一次编码任务"><a href="#背景：模型选择已经不够描述一次编码任务" class="headerlink" title="背景：模型选择已经不够描述一次编码任务"></a>背景：模型选择已经不够描述一次编码任务</h2><p>一个真实的编码任务往往不是一问一答。智能体需要阅读仓库、理解约束、修改文件、运行测试，再根据失败信息继续修正。不同任务的难点也不一样：有些只需要一次直接生成，有些适合先让快速模型给出草稿，有些则值得让另一个模型独立检查后再修改。</p><p>如果所有请求都交给最强模型，质量可能比较稳定，但成本和延迟会一起上升。如果所有请求都交给便宜模型，又容易在跨文件修改、调试和边界条件上失手。简单的“按问题分类选模型”只能处理第一层选择，无法回答下面这些问题：</p><ul><li>这次任务是否值得再做一次独立检查？</li><li>草稿质量不够时，应当升级到更强模型，还是让原模型修订？</li><li>多调用一次模型带来的收益，是否值得额外成本和等待？</li></ul><p>HydraFusion 把这些问题视为执行计划问题。它的目标不是永远调用更多模型，而是在预计能达到质量要求的前提下，选择复杂度最低的工作流。</p><h2 id="HydraFusion-的三种执行模式"><a href="#HydraFusion-的三种执行模式" class="headerlink" title="HydraFusion 的三种执行模式"></a>HydraFusion 的三种执行模式</h2><p>GitHub 目前公开了三种基本模式。它们不是三个需要开发者手动编排的 API，而是 HydraFusion 在运行时可选择的执行结构。</p><h3 id="1-Single：一个模型直接完成"><a href="#1-Single：一个模型直接完成" class="headerlink" title="1. Single：一个模型直接完成"></a>1. Single：一个模型直接完成</h3><p>Single 模式只选择一个模型处理任务，适合目标明确、上下文较小、一次完成概率较高的请求。它的成本和延迟最容易预测，也是其他模式的基线。</p><h3 id="2-Cascade：先快后强，按质量门槛升级"><a href="#2-Cascade：先快后强，按质量门槛升级" class="headerlink" title="2. Cascade：先快后强，按质量门槛升级"></a>2. Cascade：先快后强，按质量门槛升级</h3><p>Cascade 模式先让更高效的模型生成方案，再由质量门判断是否接受。如果草稿没有达到要求，流程才升级到更强的模型。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">任务</span><br><span class="line">  │</span><br><span class="line">  ▼</span><br><span class="line">高效模型生成草稿</span><br><span class="line">  │</span><br><span class="line">  ├── 达到质量门槛 ──&gt; 返回结果</span><br><span class="line">  │</span><br><span class="line">  └── 未达到门槛 ──&gt; 更强模型接管</span><br></pre></td></tr></table></figure><p>它的关键不在“先用便宜模型”这句话，而在质量门。没有可靠的质量判断，级联就只是把一次调用变成两次调用，成本增加了，错误却未必减少。对于代码任务，测试结果、补丁是否完整、是否触碰了无关文件，都可以成为质量信号的一部分。</p><h3 id="3-Critique：独立检查后再修订"><a href="#3-Critique：独立检查后再修订" class="headerlink" title="3. Critique：独立检查后再修订"></a>3. Critique：独立检查后再修订</h3><p>Critique 模式先由一个模型生成结果，再让另一个独立的、只读的批评步骤检查同一结果，最后让起草模型修订一次。这个结构类似代码评审，但评审者不是直接修改仓库，而是提供反馈。</p><p>把批评步骤设为只读很重要。一个检查模型如果能同时修改文件，就很难判断最终变化来自原始解法还是评审过程，也可能在检查阶段引入新的副作用。独立评审的意义正在于隔离观察和修改。</p><p>三种模式可以简单表示为：</p><table><thead><tr><th>模式</th><th>额外步骤</th><th>适合解决的问题</th><th>主要代价</th></tr></thead><tbody><tr><td>Single</td><td>无</td><td>目标清楚、一次完成的任务</td><td>对复杂错误缺少复核</td></tr><tr><td>Cascade</td><td>质量门和可能的升级</td><td>大部分简单任务，少量复杂任务</td><td>质量门判断错误会影响收益</td></tr><tr><td>Critique</td><td>独立检查和一次修订</td><td>需要减少遗漏的代码修改</td><td>多一次或多几次模型调用</td></tr></tbody></table><h2 id="研究预览如何使用"><a href="#研究预览如何使用" class="headerlink" title="研究预览如何使用"></a>研究预览如何使用</h2><p>GitHub 表示，HydraFusion 目前可以在 GitHub Copilot 计划中通过 Copilot CLI 的 <code>/experimental</code> 入口试用。基本步骤是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">/experimental on</span><br><span class="line">/model</span><br><span class="line">选择 HydraFusion (Research Preview)</span><br></pre></td></tr></table></figure><p>官方建议从一个范围明确、但确实需要多步处理的编码任务开始体验。HydraFusion 使用的模型按各自的标准模型费率计费，实际费用取决于它在一次任务中调用了哪些模型以及调用了多少次。研究预览中的模型池、工作流、名称和产品行为都可能发生变化，因此不应把当前结果直接当成长期 SLA 或固定成本承诺。</p><h2 id="真正难的是编排控制，不是画出流程图"><a href="#真正难的是编排控制，不是画出流程图" class="headerlink" title="真正难的是编排控制，不是画出流程图"></a>真正难的是编排控制，不是画出流程图</h2><p>多模型工作流看起来像把几个模型串起来，但要让它在代码仓库里可靠运行，必须同时处理成本、权限、状态和失败。GitHub 在介绍 HydraFusion 时列出了五项运行原则，这些原则也适用于自行构建的复合智能体。</p><h3 id="完整记录每一段成本"><a href="#完整记录每一段成本" class="headerlink" title="完整记录每一段成本"></a>完整记录每一段成本</h3><p>一次任务的成本不能只记录最后返回结果的模型。草稿、批评、修订、升级、重试和回退都属于同一次执行。若只按请求记录总价，团队会知道“这次很贵”，却不知道贵在质量门触发、重复重试，还是某个模型频繁升级。</p><p>更可用的账单维度应该至少包括：任务 ID、执行模式、每一段使用的模型、输入输出 token、耗时、重试次数和最终结果。只有这样，路由策略才有可能被真正优化。</p><h3 id="给每一段设置时间和取消边界"><a href="#给每一段设置时间和取消边界" class="headerlink" title="给每一段设置时间和取消边界"></a>给每一段设置时间和取消边界</h3><p>级联和批评流程天然比单次调用更慢。每个工作流步骤都需要明确的超时、取消和最大调用次数，否则一个失败的质量门可能不断触发升级，最后把成本和延迟一起推高。</p><p>这也影响用户体验。用户取消任务后，后续的批评、修订或回退不应继续运行。取消信号必须能穿过整个执行图，而不能只停止最外层的界面等待。</p><h3 id="把检查步骤放进隔离上下文"><a href="#把检查步骤放进隔离上下文" class="headerlink" title="把检查步骤放进隔离上下文"></a>把检查步骤放进隔离上下文</h3><p>批评模型应当在只读、无工具或受限工具的上下文里工作，求解模型再使用共享工作区和正常的权限控制。这样做的目的不是让模型“更聪明”，而是让评审结论不会和评审者自己的修改混在一起。</p><p>对于涉及密钥、生产配置和内部文档的仓库，隔离还可以减少不必要的数据暴露。哪些上下文能被草稿模型看到，哪些只能提供给只读检查器，都应该成为执行计划的一部分。</p><h3 id="验证失败时不要应用半成品补丁"><a href="#验证失败时不要应用半成品补丁" class="headerlink" title="验证失败时不要应用半成品补丁"></a>验证失败时不要应用半成品补丁</h3><p>如果工作流被取消、校验失败或中途异常，系统不应把已经生成的一部分补丁直接写入仓库。HydraFusion 把“失败时不应用补丁”作为安全原则，避免一个未完成的复合流程把半成品带进开发分支。</p><p>对普通 Agent 来说，这意味着修改文件之前应保留可回滚边界，应用补丁之后再统一做语法检查、测试和差异审查。模型说“完成”不是提交条件，验证通过才是。</p><h3 id="执行前验证路由定义和模型可用性"><a href="#执行前验证路由定义和模型可用性" class="headerlink" title="执行前验证路由定义和模型可用性"></a>执行前验证路由定义和模型可用性</h3><p>运行时编排依赖多个外部条件：工作流定义是否合法，模型绑定是否存在，回退路径是否可用，当前计划是否仍然有权限执行。把这些问题推迟到任务中途，用户看到的往往只是一次模糊的“Agent 失败”。</p><p>执行前做一次计划校验，可以把配置错误和模型故障分开，也能在模型临时不可用时选择明确的降级策略。</p><h2 id="评测结果应该怎样读"><a href="#评测结果应该怎样读" class="headerlink" title="评测结果应该怎样读"></a>评测结果应该怎样读</h2><p>GitHub 在 TerminalBench 2.1、DeepSWE、CheckpointBench 以及基于真实 Copilot 会话的内部基准上，对 HydraFusion 和其他模型进行了受控离线评测。对比使用 Claude Opus 5 和 GPT-5.6 Sol 等模型作为基线，所有策略使用相同的任务输入、工具、执行限制、价格假设和评分条件；成本还包括草稿、批评、修订、升级、重试和回退。</p><p>官方给出的结果如下。质量分数是相对 Opus 5 的变化，成本是完整工作流的预估成本变化：</p><table><thead><tr><th>基准</th><th align="right">成本变化</th><th align="right">质量变化</th></tr></thead><tbody><tr><td>TerminalBench 2.1</td><td align="right">降低 67%</td><td align="right">提高 4.9 个百分点</td></tr><tr><td>DeepSWE</td><td align="right">降低 36%</td><td align="right">降低 1.5 个百分点</td></tr><tr><td>CheckpointBench</td><td align="right">降低 65%</td><td align="right">降低 0.1 个百分点</td></tr></tbody></table><p>这张表最容易被误读成“HydraFusion 便宜且质量更高”。事实更接近于：在 GitHub 选择的模型池、工作流配置、价格假设和离线任务上，最佳调优配置展示了很好的质量成本折中。DeepSWE 的质量下降说明，成本优势并不自动等于任务能力更强；研究预览还需要通过真实开发者工作负载继续验证。</p><p>另一个值得注意的细节是，GitHub 不只报告单一模型的调用价格，而是把每个工作流环节都计入成本。这种计算方式更接近企业真正关心的账单，也提醒我们：复合智能体的优化目标应该是“完成一个可验证任务的总成本”，不能只比较某个模型每百万 token 的单价。</p><h2 id="它和普通模型路由有什么区别"><a href="#它和普通模型路由有什么区别" class="headerlink" title="它和普通模型路由有什么区别"></a>它和普通模型路由有什么区别</h2><p>普通模型路由解决的是选择问题：根据任务特征把请求发给模型 A、B 或 C。HydraFusion 多解决了一层执行问题：一次任务是否需要草稿、升级、独立批评和修订，以及这些步骤应当如何连接。</p><p>两者的区别可以用一个简单的例子说明。普通路由可能把“修改一个跨文件 bug”发给最强模型；HydraFusion 可能先让高效模型处理，再根据测试和质量信号决定是否升级，或者安排一个只读模型检查补丁。最终选择的不是一个孤立模型，而是一段有状态的执行图。</p><p>这也是它更难工程化的原因。路由策略需要了解工具权限、仓库状态、测试结果、价格和取消信号。模型越多，流程越复杂；流程越复杂，越需要明确的状态机和可观测性。</p><h2 id="对开发团队的实践启示"><a href="#对开发团队的实践启示" class="headerlink" title="对开发团队的实践启示"></a>对开发团队的实践启示</h2><h3 id="先记录总成本，再谈“智能路由”"><a href="#先记录总成本，再谈“智能路由”" class="headerlink" title="先记录总成本，再谈“智能路由”"></a>先记录总成本，再谈“智能路由”</h3><p>如果团队准备自己实现类似的多模型编排，第一步应该是记录每个任务的完整执行轨迹：调用了谁、调用几次、在哪一步升级、最终是否通过测试。没有这条数据，所谓“便宜模型先行”只能停留在直觉层面。</p><h3 id="把质量门设成可验证信号"><a href="#把质量门设成可验证信号" class="headerlink" title="把质量门设成可验证信号"></a>把质量门设成可验证信号</h3><p>代码任务中的质量门可以包括测试是否通过、静态检查是否新增问题、补丁是否只触碰目标文件、是否完成用户要求的验收项。模型自评可以作为信号，但不应是唯一信号。一个能被测试和差异审查验证的门槛，才有机会支撑级联流程。</p><h3 id="评审者应当与修改权限隔离"><a href="#评审者应当与修改权限隔离" class="headerlink" title="评审者应当与修改权限隔离"></a>评审者应当与修改权限隔离</h3><p>独立检查模型不必拥有写文件和执行任意命令的能力。让它只读取必要上下文并返回结构化意见，再由主执行器决定是否修订，可以减少评审阶段的副作用，也便于审计“谁提出了什么意见、谁应用了什么变化”。</p><h3 id="先把失败路径设计好"><a href="#先把失败路径设计好" class="headerlink" title="先把失败路径设计好"></a>先把失败路径设计好</h3><p>复合 Agent 的成功路径很容易展示，真正决定可用性的却是取消、超时、模型不可用、测试失败和回退。每条路径都应该有明确结果：保留原状、回滚临时改动、转交人工，或返回可重试状态，而不是把一个半完成目录留给用户。</p><h3 id="用自己的任务集复核公开基准"><a href="#用自己的任务集复核公开基准" class="headerlink" title="用自己的任务集复核公开基准"></a>用自己的任务集复核公开基准</h3><p>TerminalBench、DeepSWE 和 CheckpointBench 可以帮助比较方向，但不能替代团队自己的任务集。真实项目的语言、仓库结构、测试耗时、内部依赖和权限边界都可能不同。上线前至少应使用脱敏后的历史任务做离线回放，再进行小范围、可观测的在线试用。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>HydraFusion 的新意不只在于一次 Copilot 模型更新，而在于它把“模型能力”与“执行结构”拆开了。一个任务是否值得批评、升级和修订，和应该由哪个模型完成，是两个不同层次的决策。</p><p>这条路的收益很诱人：简单任务少走几步，复杂任务在关键位置增加检查，整体成本还有机会下降。但它的前提也很具体：每一段调用可计量，每一次升级有边界，检查过程与修改权限隔离，失败时不留下半成品。对开发团队来说，这些工程约束比“模型会自动选择”更值得先实现。</p><p>HydraFusion 仍然是研究预览，公开评测也有明确的任务集和假设条件。把它当作一个值得观察的架构信号，比把一张成本对比表当作普遍结论更稳妥：下一阶段的编程智能体，竞争点可能不只是模型本身，还包括谁能把多个模型组织成一条可验证、可取消、算得清的工作流。</p><h2 id="参考来源"><a href="#参考来源" class="headerlink" title="参考来源"></a>参考来源</h2><ul><li><a href="https://github.blog/ai-and-ml/github-copilot/project-hydrafusion-frontier-quality-via-multi-model-orchestration/">GitHub Blog：Project HydraFusion: Frontier quality via multi-model orchestration</a></li><li><a href="https://docs.github.com/copilot/how-tos/copilot-cli/use-copilot-cli/overview">GitHub Docs：GitHub Copilot CLI overview</a></li><li><a href="https://docs.github.com/en/copilot/reference/copilot-billing/models-and-pricing">GitHub Docs：Copilot models and pricing</a></li></ul>]]>
    </content>
    <id>https://code.idea4j.com/2026/09/06/2026-GitHub-Copilot-HydraFusion%E5%A4%9A%E6%A8%A1%E5%9E%8B%E7%BC%96%E6%8E%92/</id>
    <link href="https://code.idea4j.com/2026/09/06/2026-GitHub-Copilot-HydraFusion%E5%A4%9A%E6%A8%A1%E5%9E%8B%E7%BC%96%E6%8E%92/"/>
    <published>2026-09-06T00:01:00.000Z</published>
    <summary>GitHub 发布 HydraFusion 研究预览，让 Copilot 在运行时选择单模型、级联或批评修订工作流。本文分析它的编排方式、工程控制和受控评测结果。</summary>
    <title>GitHub HydraFusion：编程智能体开始在运行时选择“怎样解决”</title>
    <updated>2026-09-06T00:01:00.000Z</updated>
  </entry>
</feed>
